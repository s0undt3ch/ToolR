# Toolr command-authoring reference: testing example

<!-- generated by `cargo xtask build-skill-refs`; do not edit by hand -->

Copied verbatim from `tests/skill_examples/test_authoring_examples.py`, a real test that runs under `uv run pytest`. If this drifts from the skill's prose, the source moved and `cargo xtask build-skill-refs` needs a re-run — the example is never hand-edited here.

```python
def deploy(ctx: Context) -> None:
    result = ctx.run("git", "status", "--porcelain", capture_output=True)
    if result.stdout.read().strip():
        ctx.error("working tree is dirty — commit or stash before deploying")
        ctx.exit(1)


def test_deploy_checks_git_status(tmp_path):
    run = RunMock()
    run.mock.return_value = make_command_result(stdout="")
    ctx = make_context(repo_root=tmp_path, run=run)

    deploy(ctx)

    run.assert_called_once_with(
        ("git", "status", "--porcelain"),
        stream_output=True,
        capture_output=True,
        timeout_secs=None,
        no_output_timeout_secs=None,
    )
```
