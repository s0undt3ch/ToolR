{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"Home","text":"In-project CLI tooling support <p> Pronounced /\u02c8tu\u02d0l\u0259r/ (tool-er) </p> <p>ToolR is a tool similar to invoke and the next generation of python-tools-scripts.</p> <p>The goal is to quickly enable projects to write a Python module under the project's <code>tools/</code> sub-directory and it automatically becomes a sub command to the <code>toolr</code> CLI.</p>"},{"location":"#start-here","title":"Start here","text":"<ul> <li>Just want to run commands in a repo that already has <code>tools/</code>?   See Installation.</li> <li>Adding <code>tools/</code> support to your own repo?   Head to the Quickstart.</li> <li>Curious about how it works?   Read How toolr is laid out.</li> </ul>"},{"location":"#need-help","title":"Need help?","text":"<p>File an issue on GitHub.</p>"},{"location":"changelog/","title":"Changelog","text":"<p>All notable changes to this project will be documented in this file.</p> <p>This project uses git-cliff to automatically generate changelog entries from Conventional Commits, and this project adheres to Semantic Versioning.</p>"},{"location":"changelog/#0112-dev309-2026-05-18","title":"0.11.2-dev309 - 2026-05-18","text":""},{"location":"changelog/#notes","title":"Notes","text":"<p>This release lands the Rust front-end rewrite together with a workspace split and a distribution-channel reshuffle. The argparse-driven Python CLI is fully retired; the <code>toolr</code> command is now a native Rust binary, and the PyPI footprint splits into two packages so the CLI and the Python runtime can be installed independently.</p> <p>If you only invoke <code>toolr ...</code> from a project that already ships a <code>tools/</code> directory, the smallest migration is:</p> <ol> <li>Install the new CLI binary (<code>pip install toolr</code>, <code>installation/install.sh</code>,    mise, or a GitHub release archive \u2014 see below).</li> <li>Run <code>toolr project init</code> from your repo root. This scaffolds the    new <code>tools/pyproject.toml</code> (with <code>toolr-py</code> already declared) and a    <code>tools/uv.lock</code> alongside your existing <code>tools/*.py</code> scripts.</li> <li>Move any Python dependencies your <code>tools/*.py</code> previously pulled in    (e.g. via the project's main <code>pyproject.toml</code> dev group) into the    <code>[project.dependencies]</code> list inside the new <code>tools/pyproject.toml</code>,    then run <code>toolr project deps sync</code>.</li> <li>Commit <code>tools/pyproject.toml</code> and <code>tools/uv.lock</code> to git. Both    are part of the per-project tools venv contract \u2014 without them,    collaborators and CI can't reproduce your tools venv.</li> </ol> <p>The sections below spell out every other place this rewrite is visible from the outside.</p>"},{"location":"changelog/#breaking-changes","title":"\u26a0 Breaking changes","text":""},{"location":"changelog/#python-m-toolr-is-gone","title":"<code>python -m toolr</code> is gone","text":"<ul> <li>What changed: the <code>[project.scripts] toolr</code> console entry   point and <code>toolr/__main__.py</code> have been removed. The Python   package no longer ships a CLI at all \u2014 invoking <code>python -m toolr</code>   fails with <code>No module named toolr.__main__</code>.</li> <li>Migration: install the new Rust CLI (via <code>pip install toolr</code>,   <code>install.sh</code>, mise, or a GitHub release archive \u2014 see below) and   invoke it as <code>toolr &lt;args&gt;</code>. The argument surface is unchanged;   only the entry point moved.</li> </ul>"},{"location":"changelog/#pip-install-toolr-no-longer-makes-import-toolr-work","title":"<code>pip install toolr</code> no longer makes <code>import toolr</code> work","text":"<ul> <li>What changed: PyPI now hosts two packages. <code>toolr</code> is a   binary-only wheel (<code>bindings = \"bin\"</code>) \u2014 it drops the <code>toolr</code>   executable into the wheel's <code>scripts/</code> directory and has no   Python source. The Python runtime (<code>import toolr</code>, <code>Context</code>,   <code>command_group</code>, <code>toolr.utils</code>, the <code>_rust_utils</code> extension)   lives in a separate package, <code>toolr-py</code>.</li> <li>Migration: if your project's <code>tools/*.py</code> scripts do   <code>from toolr import ...</code>, declare <code>toolr-py</code> as a dependency of   the tools venv. The fastest path is <code>toolr project init</code> from   your repo root \u2014 it scaffolds <code>tools/pyproject.toml</code> with   <code>toolr-py</code> already declared and a matching <code>tools/uv.lock</code>.   Both files belong in git. The CLI on <code>PATH</code> will then find   <code>toolr-py</code> when it shells out to execute a command. See the   installation docs   for the full layout.</li> </ul>"},{"location":"changelog/#mise-plugin-external-mise-toolr-repo-retired","title":"mise plugin: external <code>mise-toolr</code> repo retired","text":"<ul> <li>What changed: the mise plugin used to be hosted out-of-tree   at <code>s0undt3ch/mise-toolr</code>. It is now self-contained at   <code>installation/mise/</code> inside this repo, and the external   <code>mise-toolr</code> repository is retired. The old   <code>mise plugin add toolr https://github.com/s0undt3ch/mise-toolr</code>   installation stops working.</li> <li>Migration:</li> </ul> <pre><code>mise plugin remove toolr   # if previously installed from the old path\nmise plugin add toolr https://github.com/s0undt3ch/ToolR.git#installation/mise\nmise use --global toolr@latest\n</code></pre>"},{"location":"changelog/#the-argparse-python-cli-internals-were-deleted","title":"The argparse Python CLI internals were deleted","text":"<ul> <li>What changed: <code>toolr/__main__.py</code>, <code>toolr/_parser.py</code>, and   <code>toolr/_registry.py</code> have been deleted. Anything that imported   <code>Parser</code>, <code>CommandRegistry</code>, or other internals from those   modules will break.</li> <li>Migration: the user-facing decorator surface   (<code>command_group</code>, <code>@command</code>, <code>CommandGroup</code>,   <code>MANIFEST_SCHEMA_VERSION</code>) is preserved. It has moved to   <code>toolr._decorators</code> and is re-exported from the top-level   package, so the public form continues to work:</li> </ul> <pre><code>from toolr import command_group, command, CommandGroup\n</code></pre> <p>If you were reaching into <code>toolr._registry</code> or <code>toolr._parser</code>   directly, there is no replacement \u2014 the Rust binary owns   manifest discovery, argument parsing, and dispatch now. The   Python runtime is invoked per-command via a JSON spec file   (<code>toolr._runner</code>).</p>"},{"location":"changelog/#testingpy-import-surface-tightened","title":"<code>testing.py</code> import surface tightened","text":"<ul> <li>What changed: <code>toolr.testing</code> previously re-exposed a few   helpers that leaned on the now-deleted <code>_parser</code> /   <code>_registry</code> modules. Those have been replaced or removed as   part of the three-way test prune; the supported public surface   is whatever <code>toolr.testing</code> exports today.</li> <li>Migration: if your test suite imported internal helpers   from <code>toolr.testing</code> and the import now fails, lean on the   documented <code>Context</code> / <code>command_group</code> factories or open an   issue describing the use case.</li> </ul>"},{"location":"changelog/#rich-argparse-is-no-longer-pulled-in","title":"<code>rich-argparse</code> is no longer pulled in","text":"<ul> <li>What changed: <code>rich-argparse</code> was only used by the deleted   <code>_parser.py</code>. It has been dropped from <code>toolr-py</code>'s   dependencies. Anything that relied on toolr transitively   bringing it into the tools venv will need to declare it   explicitly.</li> <li>Migration: add <code>rich-argparse</code> to your own   <code>pyproject.toml</code> if you depend on it for non-toolr code.</li> </ul>"},{"location":"changelog/#new-features","title":"\ud83d\ude80 New features","text":"<ul> <li>Rust CLI binary. <code>toolr</code> is now a native binary built from a   Cargo workspace. Manifest discovery, argument parsing, help   rendering, and command dispatch all run in Rust. Python is only   involved at execution time (per-command subprocess via   <code>toolr._runner</code>), so cold-start latency drops dramatically and   shell completion is no longer gated on Python import overhead.</li> <li>Three install channels, one source of truth. The same   <code>toolr</code> binary ships through:<ul> <li><code>pip install toolr</code> (a new <code>py3-none-&lt;plat&gt;</code> binary wheel),</li> <li><code>curl ... | sh</code> via <code>installation/install.sh</code>,</li> <li>mise via <code>installation/mise/</code>,</li> <li>GitHub Release archives (<code>toolr-&lt;version&gt;-&lt;target&gt;.tar.gz</code>,   with <code>.sha256</code> siblings).</li> </ul> </li> </ul> <p>All four are produced from the same workspace build and share a   single version number. - <code>toolr-py</code> PyPI package. A standalone wheel providing   <code>import toolr</code> for user tool scripts \u2014 declared in   <code>tools/pyproject.toml</code>, materialised into the tools venv by   <code>uv sync</code>. Decouples \"what CLI you have on PATH\" from \"what   Python bindings your tool scripts pin.\" - Python 3.14 support. Added to the test matrix and the   <code>toolr-py</code> classifier list. - Per-project <code>tools/</code> venv with uv. The Rust binary   materialises (and, if needed, bootstraps) a <code>tools/</code> venv via   <code>uv</code> before each execute. Includes missing-dependency   diagnostics, manifest caching, and cache pruning. See the   rebuilt installation /   usage docs for the   end-to-end story. - Native shell completion. Generated by the Rust frontend   (clap-based), available for the usual shells. - In-repo mise plugin smoke test. The plugin lives at   <code>installation/mise/</code> and is covered by the same end-to-end   smoke harness as the other install channels. - SLSA build provenance on every shipped artifact. Every   wheel (<code>toolr-*.whl</code>, <code>toolr_py-*.whl</code>), sdist, per-triple   binary archive (<code>toolr-&lt;version&gt;-&lt;triple&gt;.tar.gz</code> /   <code>.zip</code>), and the release notes / patch files carry a   cryptographically signed attestation generated by   <code>actions/attest-build-provenance</code>. Verify any of them with:</p> <pre><code>gh attestation verify &lt;file&gt; --owner s0undt3ch\n</code></pre> <p><code>install.sh</code> already passes <code>--verify-attestation=require</code> to   reject any archive whose attestation does not validate. See   GitHub's artifact attestations   docs   for the full verification model.</p>"},{"location":"changelog/#migration-cheat-sheet","title":"\ud83d\uddfa Migration cheat-sheet","text":"If you used... Replace with... <code>python -m toolr ...</code> <code>toolr ...</code> (install the CLI via pip / install.sh / mise / release archive) <code>pip install toolr</code> to get <code>import toolr</code> <code>pip install toolr-py</code> (or run <code>toolr project init</code> to scaffold <code>tools/pyproject.toml</code> + <code>tools/uv.lock</code> with <code>toolr-py</code> declared) <code>mise plugin add toolr https://github.com/s0undt3ch/mise-toolr</code> <code>mise plugin add toolr https://github.com/s0undt3ch/ToolR.git#installation/mise</code> <code>from toolr._registry import command_group</code> <code>from toolr import command_group</code> <code>from toolr._registry import CommandGroup</code> <code>from toolr import CommandGroup</code> <code>from toolr._parser import Parser</code> (and friends) No replacement \u2014 the Rust binary owns parsing now Relying on <code>rich-argparse</code> via toolr Depend on <code>rich-argparse</code> directly"},{"location":"changelog/#internal-for-contributors","title":"\ud83e\uddf1 Internal \u2014 for contributors","text":"<p>These changes affect anyone hacking on toolr itself but are invisible to end users:</p> <ul> <li>Cargo workspace split. Three crates under <code>crates/</code>:<ul> <li><code>toolr-core</code> \u2014 private library (no pyo3, no clap). Manifest   discovery, AST parsing, manifest cache, venv plumbing.</li> <li><code>toolr</code> \u2014 the binary crate (<code>bindings = \"bin\"</code>). Depends on   <code>toolr-core</code> plus <code>clap</code> and <code>termimad</code>.</li> <li><code>toolr-py</code> \u2014 the pyo3 dynlib + Python source   (<code>bindings = \"pyo3\"</code>, <code>module-name = \"toolr.utils._rust_utils\"</code>).</li> </ul> </li> <li>Python source location. Moved from <code>python/toolr/</code> to   <code>crates/toolr-py/python/toolr/</code>. The repo-root <code>python/</code>   directory is gone.</li> <li>Two PyPI wheels, one workspace version. <code>toolr</code> and   <code>toolr-py</code> are released together at the same   <code>[workspace.package] version</code>. Both have their own   <code>pyproject.toml</code> and <code>cibuildwheel</code> matrices; CI fans the   wheel builds out per crate and reassembles them at release.</li> <li>Root <code>pyproject.toml</code> is dev-tooling only. It retains   <code>[tool.ruff]</code>, <code>[tool.mypy]</code>, <code>[tool.pytest.ini_options]</code>,   <code>[tool.uv]</code>, <code>[tool.uv.workspace]</code>, <code>[dependency-groups]</code> \u2014   no <code>[build-system]</code>, no <code>[project]</code>, no <code>[tool.maturin]</code>.</li> <li>No <code>python</code> feature flag. The   <code>[features] python = [\"pyo3\"]</code> dance and the   <code>#[cfg(feature = \"python\")]</code> annotations are gone. pyo3 lives   exclusively in <code>crates/toolr-py/</code> as a non-optional   dependency.</li> <li><code>tools/version.py</code> simplified. Cargo.toml writes go   through <code>cargo set-version</code> (via cargo-edit) instead of   hand-rolled regex edits.</li> <li><code>rich</code> is a direct <code>toolr-py</code> dependency. Previously   transitive through <code>rich-argparse</code>.</li> <li><code>UNRELEASED.md</code> \u2192 release notes pipeline. The file you're   reading is now part of every release: <code>_prepare-release.yml</code>   strips this comment header, exports the body as   <code>TOOLR_RELEASE_NOTES</code>, and the cliff template renders it as   a <code>### Notes</code> section in both the GitHub release body and   <code>CHANGELOG.md</code> under the new version's heading.</li> <li>Dogfooding tools venv. <code>tools/pyproject.toml</code> declares   <code>toolr-py</code> as a workspace dependency \u2014 the repo's own   <code>tools/*.py</code> scripts run against the same Python runtime   users get from PyPI.</li> </ul>"},{"location":"changelog/#features","title":"\ud83d\ude80 Features","text":"<ul> <li>(rust) Add toolr binary target with placeholder main (<code>27d1279</code>)</li> <li>(rust) Wire toolr binary to clap for --help and --version (<code>bbdbb2b</code>)</li> <li>(manifest) Add manifest data model with serde round-trip tests (<code>9b8ecfc</code>)</li> <li>(manifest) Add manifest load/write with schema version validation (<code>0e6283d</code>)</li> <li>(discovery) Walk upward to locate project root from cwd (<code>5108aad</code>)</li> <li>(hash) Deterministic hashing over tools/*/.py contents (<code>b7db766</code>)</li> <li>(parser) Wire ruff_python_parser for AST access (<code>b47a1c1</code>)</li> <li>(parser) Extract command_group() calls from module ASTs (<code>435ce60</code>)</li> <li>(parser) Extract @group.command decorated functions (<code>bdb42e9</code>)</li> <li>(parser) Extract function signatures into Command arguments (<code>41f0cb7</code>)</li> <li>(parser) Populate command summary, description, and arg help from docstrings (<code>0411a02</code>)</li> <li>(parser) Extract typing.Literal values into allowed_values (<code>14ec3ba</code>)</li> <li>(parser) Resolve local enum.Enum members for allowed_values (<code>689db73</code>)</li> <li>(parser) Build complete static manifest from tools directory (<code>cf158c4</code>)</li> <li>(cli) Build subcommand tree from manifest and stub execution (<code>bb47f9b</code>)</li> <li>(cli) Add hidden __build-static-manifest dev command (<code>79847b4</code>)</li> <li>(runner) Add msgspec schema for toolr._runner spec payload (<code>5672c30</code>)</li> <li>(runner) Load + validate spec JSON from TOOLR_SPEC_FILE (<code>7b02436</code>)</li> <li>(runner) Dispatch into target function via Context (<code>ea01f6d</code>)</li> <li>(execute) Add ExecutionSpec serde types matching runner schema (<code>df94fc4</code>)</li> <li>(execute) Write spec JSON to private tempfile with drop-cleanup (<code>9f1dfa1</code>)</li> <li>(execute) Minimal python interpreter resolver (TOOLR_PYTHON or PATH) (<code>a2a45b0</code>)</li> <li>(execute) Spawn python -m toolr._runner with inherited stdio (<code>16dab01</code>)</li> <li>(execute) Build ExecutionSpec from clap ArgMatches (<code>70b9efb</code>)</li> <li>(execute) Wire Python runner into dispatch with exit-code propagation (<code>db98c55</code>)</li> <li>(execute) Forward SIGINT/SIGTERM to Python runner subprocess (<code>6541cf9</code>)</li> <li>(uv) Add uv module skeleton with discovery types (<code>c56cc21</code>)</li> <li>(uv) Probe uv --version on PATH and managed paths (<code>79cc988</code>)</li> <li>(uv) Decision logic for consent-based uv install (<code>6410d7e</code>)</li> <li>(uv) Download and install uv to $XDG_DATA_HOME/toolr/bin (<code>d578125</code>)</li> <li>(uv) Unified ensure_uv entrypoint and hidden install command (<code>99aed4d</code>)</li> <li>(venv) Parse [tool.toolr] config from tools/pyproject.toml (<code>3fe9fe2</code>)</li> <li>(venv) Stable repo-key hash for cache-slot disambiguation (<code>9c27607</code>)</li> <li>(venv) Resolve cache vs in-tree venv path (<code>9a73832</code>)</li> <li>(venv) Run uv sync with mtime-based freshness check (<code>f9d7e94</code>)</li> <li>(venv) Validate toolr Python package presence in venv (<code>8b94b81</code>)</li> <li>(venv) Best-effort editable installs with warn-on-fail (<code>e47104c</code>)</li> <li>(cli) Reserve <code>toolr project</code> namespace and stub subcommands (<code>24aafdb</code>)</li> <li>(project) Implement <code>toolr project deps sync</code> (<code>b111f83</code>)</li> <li>(project) Implement <code>toolr project venv path</code> (<code>7ce4b36</code>)</li> <li>(project) Implement <code>toolr project venv shell</code> (<code>4b3a845</code>)</li> <li>(dispatch) Spawn the runner via the tools venv python (<code>2a000a3</code>)</li> <li>(complete) Scaffold shell-completion module (<code>5badb46</code>)</li> <li>(complete) Prefix-match groups, commands, flags, and allowed values (<code>5412dc4</code>)</li> <li>(complete) Tab-time hash check with cache + fresh-reparse fallback (<code>88ee2b3</code>)</li> <li>(cli) Add hidden __complete subcommand backing shell scripts (<code>b92783e</code>)</li> <li>(complete) Embed bash completion script with self completion print bash (<code>2c31758</code>)</li> <li>(complete) Embed zsh completion script (<code>4db30ee</code>)</li> <li>(complete) Embed fish completion script (<code>f980645</code>)</li> <li>(complete) Install completion scripts into shell-standard locations (<code>c8bbc9f</code>)</li> <li>(third_party) Add fragment model and site-packages glob helper (<code>910a73b</code>)</li> <li>(third_party) Parse + validate manifest fragments with schema-version guard (<code>179acd2</code>)</li> <li>(third_party) Add schema-version migration framework with identity step (<code>f5950f9</code>)</li> <li>(third_party) Merge fragments into Manifest with conflict resolution (<code>f94f431</code>)</li> <li>(third_party) Add discover_and_merge orchestrator with fail-fast on bad fragments (<code>89e3fd9</code>)</li> <li>(parser) Add build_static_manifest_with_venv merging third-party fragments (<code>436ec74</code>)</li> <li>(toolr.build) Programmatic build_manifest API for third-party packages (<code>eb00bc0</code>)</li> <li>(toolr.build) Add python -m toolr.build CLI with --check drift detection (<code>13604ab</code>)</li> <li>(toolr.build) Validate fragment schema before writing (<code>5348a50</code>)</li> <li>(cli) Add toolr self build-manifest Rust wrapper around python -m toolr.build (<code>5463e85</code>)</li> <li>(dynamic) Define dynamic-layer payload schema and tests (<code>b5cf221</code>)</li> <li>(introspect) Add dynamic-manifest helper skeleton with empty-payload smoke test (<code>f112c3a</code>)</li> <li>(introspect) Walk tools.* and emit registry-derived groups and commands (<code>f8ddd35</code>)</li> <li>(introspect) Load toolr.commands entry points so legacy packages appear in dynamic layer (<code>ab81b41</code>)</li> <li>(dynamic) Compute dynamic-layer hash over venv dist-info names (<code>f684346</code>)</li> <li>(dynamic) Spawn introspect helper and decode payload with schema check (<code>dfb673d</code>)</li> <li>(dynamic) Merge dynamic payload into static manifest with static-wins policy (<code>96fe2f8</code>)</li> <li>(dynamic) Add rebuild_manifest_full and rebuild_dynamic_only orchestration (<code>ba62143</code>)</li> <li>(cli) Add toolr project manifest rebuild command (<code>fb77f69</code>)</li> <li>(dispatch) Auto-rebuild dynamic manifest layer when venv state changes (<code>ce239be</code>)</li> <li>(pre-commit) Ship toolr-manifest hook config for downstream consumers (<code>325c6ac</code>)</li> <li>(deps-check) Add filesystem probe for top-level imports (<code>d6f5f7b</code>)</li> <li>(deps-check) Add pre-flight check_imports with structured error (<code>8b80c21</code>)</li> <li>(cli) Run pre-flight missing-deps check before runner spawn (<code>f9cccbc</code>)</li> <li>(cli) Add TOOLR_NO_PREFLIGHT_DEPS escape hatch (<code>8968356</code>)</li> <li>(deps-check) Add post-mortem ImportError interceptor (<code>12ed37d</code>)</li> <li>(runner) Intercept ImportError tracebacks and append sync hint (<code>0abbaaa</code>)</li> <li>(cache) Add Meta sidecar data model with serde round-trip tests (<code>9d156b3</code>)</li> <li>(cache) Drop meta.json sidecar on venv creation (<code>9ee0814</code>)</li> <li>(cache) Touch last_used_at on every invocation (<code>502b03c</code>)</li> <li>(cache) Enumerate cache entries with size accounting (<code>cf8cdda</code>)</li> <li>(cli) Add toolr self cache list with tabular output (<code>f1a5aa6</code>)</li> <li>(cache) Classify cache entries as keep, orphan, or stale (<code>a8c73ee</code>)</li> <li>(cli) Add toolr self cache prune with --all and --dry-run (<code>9e50b87</code>)</li> <li>(cache) Emit passive size-hint when cache exceeds thresholds (<code>36ebf4e</code>)</li> <li>(build) Pin maturin and add wheel-contents tests (<code>a34bd12</code>)</li> <li>(cli) Replace python -m toolr with binary-exec deprecation shim (<code>31cdb92</code>)</li> <li>(ci) Build per-platform Rust binary archives workflow (<code>7026d31</code>)</li> <li>(ci) Generate release-manifest.json for installer consumption (<code>f591157</code>)</li> <li>(install) Add cross-platform install.sh + install.ps1 scripts (<code>ca72140</code>)</li> <li>(mise) Add Rust-binary-fetching mise plugin (staging dir) (<code>d163773</code>)</li> <li>(mise) Stage mise plugin scripts under dist/mise-plugin/ (<code>9af5e82</code>)</li> <li>(cli) [breaking] Remove python -m toolr entry point (<code>0f76af7</code>)</li> <li>(ci) Always attest archives + verify provenance from install scripts (<code>df7c9f3</code>)</li> <li>(cli) Skeleton toolr project init subcommand (<code>479c1c7</code>)</li> <li>(init) Add scaffold template files and render helpers (<code>4b46671</code>)</li> <li>(init) Fill in example.py with four ctx-exercising commands (<code>92966a4</code>)</li> <li>(init) Scaffold tools/ with atomic file writes and refuse-without-force (<code>2c78866</code>)</li> <li>(init) Auto-run uv sync after scaffolding (skippable via --no-sync) (<code>1836631</code>)</li> <li>(args) List[T] / *args support, bool=False flags, hyphenated flags, msgspec coercion (<code>325bc00</code>)</li> <li>(types) Toolr.types namespace + rust SupportedType resolver foundation (<code>07a3994</code>)</li> <li>(types) Clap-side per-type value_parsers + manifest-build type validation (<code>7fd99e7</code>)</li> <li>(groups) Nested command groups (closes #193) (<code>5be44dd</code>)</li> <li>(complete) Tab-complete nested groups + their child commands (<code>5000a1e</code>)</li> <li>(types) Wire heterogeneous tuple[T1, T2, ...] arity into clap (<code>9cf6246</code>)</li> <li>(types) Arg() Path constraints + msgspec dec_hook for richer types (<code>5a53187</code>)</li> <li>(types) Add toolr.types.Version (PEP 440 via pep440_rs / packaging) (<code>518416b</code>)</li> <li>(types) Follow module-level type aliases through resolution (<code>b70ae97</code>)</li> <li>(help) Render docstring markdown via termimad for --help output (<code>355f825</code>)</li> <li>Parent=kwarg nesting + yellow/green help palette (<code>1d36f38</code>)</li> <li>(parser) Cross-file group bindings + parent= resolution (<code>c75f10e</code>) <li>@command(group=\"path\") string-path attachment + dotted command_group (<code>2e31e9f</code>)</li> <li>ToolrDeprecationWarning on legacy decorators (<code>cb81eaa</code>)</li> <li>(arg) Full arg() metadata + toolr.types.Count (closes #198) (<code>eff9c2f</code>)</li> <li>(cli) Output Options \u2014 timestamps + ctx.run timeout defaults (closes #191) (<code>e378635</code>)</li> <li>(retire) Remove Python CLI frontend (_parser, _registry) (<code>7d1c0d4</code>)</li> <li>(release) Wire UNRELEASED.md into cliff via TOOLR_RELEASE_NOTES (<code>3a023b2</code>)</li>"},{"location":"changelog/#bug-fixes","title":"\ud83d\udc1b Bug Fixes","text":"<ul> <li>(gitignore) Anchor MANIFEST pattern to repo root (<code>7de3ef9</code>)</li> <li>(discovery) Canonicalize start path and tighten error-payload assertions (<code>9f9e1e8</code>)</li> <li>(gitignore) Anchor venv/ pattern to repo root (<code>2ff6f30</code>)</li> <li>(ci) Resolve Windows compile error and typos CHANGELOG false positive (<code>c0f9a02</code>)</li> <li>(clippy) Use sort_by_key with Reverse in self_cache::run_list (<code>a60e4df</code>)</li> <li>(parser) Resolve enum-attribute defaults to their serialised value (#197) (<code>8e2889e</code>)</li> <li>(runner) Pass dec_hook to *args coercion; document arg() metadata gap (<code>448a68a</code>)</li> <li>(cli) --help shows summary+description; root-only -d/-q flags (<code>6a08334</code>)</li> <li>(cli) -h shows full description, matching argparse-era ergonomics (<code>4152589</code>)</li> <li>(cli) Parent listings show summary only; -h on leaves shows the full body (<code>b2e6d31</code>)</li> <li>(cli) Render Output Options help text through termimad (<code>83d5604</code>)</li> <li>(parser) Classify <code>Annotated[bool, arg(...)]</code> as a Flag (<code>8138fd1</code>)</li> <li>(ci) Pin COLUMNS=100 for snippet captures + correct plan-doc links (<code>56d3070</code>)</li> <li>(rust) Gate unix-only test helpers properly for Windows builds (<code>c661e7c</code>)</li> <li>(rust) Gate execute::signals test module behind cfg(unix) (<code>a7870de</code>)</li> <li>(tests) JSON-encode paths in cache fixture for Windows compatibility (<code>0baef9f</code>)</li> <li>(tests) Canonicalise the in-tree venv-path expected value (<code>cb4fa7c</code>)</li> <li>(tests) JSON-encode paths in self-cache-prune fixture too (<code>b4675ff</code>)</li> <li>(tests) Skip ancestor-walk tests when host has a tools/ at root (<code>bd11a62</code>)</li> <li>(tests) Skip silent_failure_when_no_tools_dir_anywhere when host has tools/ (<code>2c658a7</code>)</li> <li>(tests) Serialise execute::python tests that mutate TOOLR_PYTHON (<code>ed4d640</code>)</li> <li>(workspace-split) Stage 7 review fixes (<code>68d300a</code>)</li> <li>(tests) Patch correct getpass symbol; lift rich&lt;14.3 cap (<code>a1a034f</code>)</li> <li>(docs) Update mkdocs.yml paths to crates/toolr-py/python (<code>1f6e2dc</code>)</li> <li>(packaging) Tac portability + exclude pycache from toolr-py wheel (<code>363cb85</code>)</li> <li>(types) Sync _context.pyi with the .py source (Output Options fields) (<code>0ef3972</code>)</li> <li>(types) Move TypeVar F into TYPE_CHECKING block (ruff TC001) (<code>4d58ebf</code>)</li> <li>(version) Make dev-version cargo-compatible; fix Windows strftime (<code>fddb203</code>)</li> <li>(version) Realign workspace version with reality; bump patch for dev (<code>dc9d459</code>)</li> </ul>"},{"location":"changelog/#other","title":"\ud83d\udcbc Other","text":"<ul> <li>(wheels) Split pyproject.toml; add toolr binary-wheel + toolr-py pyo3-wheel (<code>e5d9b8f</code>)</li> <li>(tools) Declare toolr-py as dogfooding-tools dep (<code>d4ba5bd</code>)</li> <li>(tools) Inherit packaging from toolr-py; widen venv-cache hash (<code>89bc57d</code>)</li> <li>Install prebuilt toolr-py wheel instead of rebuilding it (<code>393cd25</code>)</li> <li>Call _test_distribution.yml instead of inlining dist tests (<code>9726a8d</code>)</li> <li>Un-deprecate @group.command, keep parent.command_group on track for 1.0 removal (<code>93986dd</code>)</li> <li>Exclude superpowers/ subtree from the published site (<code>f041644</code>)</li> </ul>"},{"location":"changelog/#refactor","title":"\ud83d\ude9c Refactor","text":"<ul> <li>(tests) Convert test_dispatch helpers to pytest factory fixtures (<code>8b60d8b</code>)</li> <li>(tests) Convert test_spec_loader helpers to pytest factory fixtures (<code>0856956</code>)</li> <li>(workspace) Move toolr-rust-utils crate under crates/toolr-core (<code>0e3d91f</code>)</li> <li>(workspace) Extract toolr binary into crates/toolr (<code>9bd0297</code>)</li> <li>(workspace) Extract toolr-py pyo3 crate; remove python feature flag (<code>00c0463</code>)</li> <li>(workspace) Move python/toolr/ into crates/toolr-py/python/ (<code>533ab81</code>)</li> <li>(install) Rename dist/ -&gt; installation/, mise-plugin/ -&gt; mise/ (<code>b4f1197</code>)</li> <li>(version) Delegate Cargo.toml writes to <code>cargo set-version</code> (<code>18e0e74</code>)</li> </ul>"},{"location":"changelog/#documentation","title":"\ud83d\udcda Documentation","text":"<ul> <li>(spec) Add Rust front-end design (<code>1263dec</code>)</li> <li>(spec) Add implementation roadmap (<code>ef9eee8</code>)</li> <li>(spec) Reorganize specs/ by topic with sequenced filenames (<code>d52994f</code>)</li> <li>(spec) Draft Plan 1 (Rust binary skeleton + static manifest layer) (<code>b21b5d3</code>)</li> <li>(roadmap) Mark Plan 1 as \ud83d\udd27 In Progress (<code>f7728b7</code>)</li> <li>(roadmap) Mark Plan 1 as \u2705 Done (<code>e08a977</code>)</li> <li>(spec) Draft Plans 2-9 for the Rust front-end rewrite (<code>8ec018b</code>)</li> <li>(roadmap) Mark Plan 2 as \ud83d\udd27 In Progress (<code>0d1ac8f</code>)</li> <li>(spec) Refactor plan-doc test snippets to use pytest factory fixtures (<code>8c513d0</code>)</li> <li>(toolr) Note toolr._runner module + ship-test (<code>7f088b3</code>)</li> <li>(roadmap) Mark Plan 2 as \u2705 Done (<code>c5993fa</code>)</li> <li>(roadmap) Mark Plan 3 as \ud83d\udd27 In Progress (<code>e1ca4e0</code>)</li> <li>(roadmap) Mark Plan 3 as \u2705 Done (<code>055cbc0</code>)</li> <li>(roadmap) Mark Plan 4 as done (<code>baf49cd</code>)</li> <li>(roadmap) Mark Plan 5 as done (<code>93b32e8</code>)</li> <li>(plans) Mark all completed steps in plans 4 and 5 as done (<code>c3f7af1</code>)</li> <li>(roadmap) Mark Plan 6 as done (<code>8fe6101</code>)</li> <li>(roadmap) Mark Plan 7 as done (<code>b89c77a</code>)</li> <li>(roadmap) Mark Plan 8 as done (<code>ebff6b9</code>)</li> <li>(changelog) Document Plan 9 distribution + breaking changes (<code>fb276c4</code>)</li> <li>(readme) Document new standalone install + pip wheel paths (<code>5e3c7f8</code>)</li> <li>(roadmap) Mark Plan 9 as done (<code>812de25</code>)</li> <li>(readme) Document SLSA attestation verification flow (<code>7763973</code>)</li> <li>(spec) Brainstorm design for docs overhaul + toolr project init (<code>b5e29b2</code>)</li> <li>(spec) Relocate regen-doc-snippets to .pre-commit-hooks/ and link issue #191 (<code>c43eec0</code>)</li> <li>(plans) Write Plan 10 (project init) and Plan 11 (docs overhaul) (<code>b08e90e</code>)</li> <li>(roadmap) Mark Plan 10 as done (<code>976e715</code>)</li> <li>Roadmap entry + nav skeleton for the docs overhaul (<code>ab9e816</code>)</li> <li>(tooling) Add regen-doc-snippets script + sample-repo fixture (<code>425fcde</code>)</li> <li>Quickstart page (install \u2192 init \u2192 run) (<code>90cb46f</code>)</li> <li>Rewrite installation page with new install matrix + SLSA section (<code>e4c8332</code>)</li> <li>Concepts page (orientation tour of toolr's moving pieces) (<code>3ae9c2b</code>)</li> <li>Writing commands chapter (port old usage + examples, restructure) (<code>5a73fae</code>)</li> <li>Project configuration page (tools/pyproject.toml + venv model) (<code>591e3da</code>)</li> <li>CLI reference (single page, every subcommand documented uniformly) (<code>57f009f</code>)</li> <li>Third-party packages page (static manifest convention + toolr.build) (<code>08fd639</code>)</li> <li>Internals chapter (manifest layers, cache, pre-commit, diagnostics) (<code>be3012e</code>)</li> <li>(reference) Prune private-module pages, keep only the public API surface (<code>8fbf2c5</code>)</li> <li>Update nav, third-party link, and snippet whitespace handling for the reference rename (<code>aac13ab</code>)</li> <li>Slim home page to a landing + audience-shortcut layout (<code>b71fa6d</code>)</li> <li>(roadmap) Mark Plan 11 as done (<code>e8cb029</code>)</li> <li>Rename writing-commands/limitations.md \u2192 known-bugs.md (<code>62d3282</code>)</li> <li>Supported-types matrix + manifest schema reference (<code>158a194</code>)</li> <li>Catch the writing-commands chapter up to the new decorator API (<code>61dbb11</code>)</li> <li>Lead with the string-path API, deprecate legacy in prose (<code>b08c273</code>)</li> <li>(spec) Add cargo workspace split design (<code>8a26dde</code>)</li> <li>(plan) Add Plan 12 (workspace split) under specs/rust-front-end/ (<code>f495f0e</code>)</li> <li>(mise) Salvage MISE_SUPPORT.md into docs/installation/mise.md (<code>24b4a24</code>)</li> <li>(install) Clarify the two PyPI packages \u2014 toolr (CLI) vs toolr-py (bindings) (<code>d07c72d</code>)</li> <li>(release) Populate UNRELEASED.md with rearchitecture notes (<code>ea7188c</code>)</li> </ul>"},{"location":"changelog/#testing","title":"\ud83e\uddea Testing","text":"<ul> <li>(project) Integration tests for venv path resolution (<code>8fc400f</code>)</li> <li>(project) End-to-end deps-sync + execute smoke (ignored by default) (<code>e5ac8ff</code>)</li> <li>(complete) End-to-end smoke tests against the real toolr binary (<code>ac7648c</code>)</li> <li>(third_party) Round-trip Python build_manifest into Rust-readable fragment (<code>bf35cc8</code>)</li> <li>(dynamic) End-to-end static-plus-dynamic merge integration test (<code>68f7527</code>)</li> <li>(cli) Cover pre-flight vs post-mortem split end-to-end (<code>30947b3</code>)</li> <li>(cache) End-to-end fixture run of list, prune, prune --all (<code>2771a60</code>)</li> <li>(build) Add editable-install test (xfail: maturin pyo3 limitation) (<code>5a2ef47</code>)</li> <li>(compat) Add end-to-end command_group compat test (xfail: maturin limitation) (<code>81ba0d6</code>)</li> <li>(init) End-to-end integration tests for toolr project init (<code>a90c788</code>)</li> <li>(retire) Three-way prune of Python frontend tests (<code>0d07fd1</code>)</li> <li>(distribution) Assert wheel shapes and cross-wheel install path (<code>b868709</code>)</li> <li>(distribution) Add maturin to dev deps so distribution tests run (<code>9061022</code>)</li> <li>(distribution) Consume pre-built wheels in a dedicated CI job (<code>7dcb5c1</code>)</li> </ul>"},{"location":"changelog/#miscellaneous-tasks","title":"\u2699\ufe0f Miscellaneous Tasks","text":"<ul> <li>(release) Update ToolR action versions in workflows (<code>ba95d8e</code>)</li> <li>Add nightly install-channel smoke tests (<code>32bc64f</code>)</li> <li>Wire doc-snippet drift check into pre-commit + CI (<code>90afb89</code>)</li> <li>Stop tracking tools/.toolr-manifest.json \u2014 it's a cache (<code>9a3051e</code>)</li> <li>(pre-commit) Install uv + sync dev venv for regen-doc-snippets hook (<code>8dffbda</code>)</li> <li>(release) Bump version to 0.20.0 (<code>43dbdaf</code>)</li> <li>(mise) Provision all CI tooling via jdx/mise-action (<code>ea23f9c</code>)</li> <li>(ci) Prefix reusable workflow filenames with underscore (<code>7cdf234</code>)</li> <li>(cache) Add CACHE_SEED with single-bump invalidation (<code>eee8674</code>)</li> <li>(workspace-split) Fan out wheel builds; rewire version bump and smoke (<code>e325843</code>)</li> <li>(workspace-split) Post-split carry-over cleanups (<code>e03c6b9</code>)</li> <li>(workspace-split) Final loose ends \u2014 roadmap status, init version lookup, binary-archive wiring (<code>cadb37e</code>)</li> <li>(coverage) Exclude toolr-py from cargo tarpaulin (<code>07ef1bf</code>)</li> <li>(release) Replace external ToolR bootstrap with local workspace (<code>31d4472</code>)</li> </ul>"},{"location":"changelog/#0111-2026-05-13","title":"0.11.1 - 2026-05-13","text":""},{"location":"changelog/#features_1","title":"\ud83d\ude80 Features","text":"<ul> <li>(logs) Include extra keywords in logs output (<code>695dc58</code>)</li> <li>(tests) Fuzzy testing (<code>f34c9cd</code>)</li> <li>(security) Add <code>SECURITY.md</code> file (<code>131b75a</code>)</li> </ul>"},{"location":"changelog/#bug-fixes_1","title":"\ud83d\udc1b Bug Fixes","text":"<ul> <li>(pypi) We can't have local version parts in PyPi (<code>fa3c515</code>)</li> <li>(signature) Return <code>VarArg</code> for <code>*args</code> (VAR_POSITIONAL) parameters (<code>f8a967d</code>)</li> <li>(signature) Match <code>KwArg</code> before <code>Arg</code> in <code>Signature.__call__</code> (<code>1228344</code>)</li> <li>(docs) Bump pymdown-extensions to 10.21.2 for pygments 2.20.0 compat (<code>7422193</code>)</li> <li>(docs) Use descriptive link text in SECURITY.md (<code>212454a</code>)</li> </ul>"},{"location":"changelog/#miscellaneous-tasks_1","title":"\u2699\ufe0f Miscellaneous Tasks","text":"<ul> <li>(release) Update ToolR action versions in workflows (<code>0d11014</code>)</li> <li>(ci) Switch to prek (<code>64c5997</code>)</li> <li>(pre-commit) Update pre-commit hook versions (<code>6438077</code>)</li> <li>(ci) Switch to <code>macos-15-intel</code> to build Intel wheels (<code>211b0fa</code>)</li> <li>(ci) Lock permissions on the build.yml workflow (<code>aabe4e5</code>)</li> <li>(ci) Lock <code>build.yml</code> GitHub Actions to SHA hashes (<code>08bdda3</code>)</li> <li>(ci) Only run attestations on the main repo (<code>0d2b361</code>)</li> <li>(ci) Improve <code>cibuildwheel`` build performance by adding cache ([</code>60a438f`](https://github.com/s0undt3ch/ToolR/commit/60a438f5c00b406f170ff7d07e4ac9968984c967))</li> <li>(ci) Fix the chicken &amp; egg issue with releases. (<code>77cc4f6</code>)</li> <li>(ci) Remove no longer required process (<code>821592f</code>)</li> <li>(ci) Update but still lock to the SHA (<code>57e3719</code>)</li> <li>(ci) When updating our own usage or toolr in GH Actions, lock it (<code>b666088</code>)</li> <li>(ci) Restrict GH Actions jobs permissions (<code>14bfc6d</code>)</li> <li>(pre-commit) Add pre-commit hook to lock GH Actions steps (<code>5d69b7a</code>)</li> <li>(ci) Switch <code>prepare-release</code> to a reusable workflow (<code>9e6c657</code>)</li> <li>(ci) Update actions versions (<code>2faf97c</code>)</li> <li>(ci) Fix auto version bump (<code>2a85e6c</code>)</li> <li>(docs) Add CONTRIBUTING document (<code>7691338</code>)</li> <li>(pre-commit) Swap markdownlint-cli2 for rumdl, add gitleaks (<code>a4f6bc5</code>)</li> </ul>"},{"location":"changelog/#new-contributors","title":"New Contributors","text":"<ul> <li>@step-security-bot made their first contribution</li> </ul>"},{"location":"changelog/#0110-2025-09-24","title":"0.11.0 - 2025-09-24","text":""},{"location":"changelog/#features_2","title":"\ud83d\ude80 Features","text":"<ul> <li>(docstrings) We now use a rust extension to parse the docstrings (<code>a2744f0</code>)</li> <li>(commands help) The command's help message is now formatted with Markdown (<code>d786915</code>)</li> </ul>"},{"location":"changelog/#miscellaneous-tasks_2","title":"\u2699\ufe0f Miscellaneous Tasks","text":"<ul> <li>(sync-rolling-tags) Fix <code>sync-rolling-tags</code> workflow trigger (<code>889e36f</code>)</li> <li>(ci) Sync'ing rolling tags is now done on demand (<code>773745b</code>)</li> <li>(ci) Fix sync-rolling-tags command (<code>59c2955</code>)</li> <li>(ci) More fixes to the sync-rolling-tags process (<code>45ac806</code>)</li> <li>(release) Update ToolR action versions in workflows (<code>6cc4bde</code>)</li> <li>(ci) Final sync-rolling-tags fix (<code>c48fcbe</code>)</li> <li>(ci) Refresh some caches (<code>a91de3d</code>)</li> </ul>"},{"location":"changelog/#0101-2025-09-19","title":"0.10.1 - 2025-09-19","text":""},{"location":"changelog/#bug-fixes_2","title":"\ud83d\udc1b Bug Fixes","text":"<ul> <li>(parent) Fix command nesting (<code>c223dfc</code>)</li> <li>(command) We now log the <code>.run()</code> cmdline at the <code>INFO</code> level (<code>1311f8d</code>)</li> <li>(tests) Fix <code>ctx.which</code> tests to make them less brittle. (<code>8a55cf3</code>)</li> </ul>"},{"location":"changelog/#miscellaneous-tasks_3","title":"\u2699\ufe0f Miscellaneous Tasks","text":"<ul> <li>(release) Don't update ToolR action usage in workflows (<code>fbe4992</code>)</li> <li>(release) Add workflow that updates ToolR versions in workflows (<code>9782974</code>)</li> </ul>"},{"location":"changelog/#0100-2025-09-17","title":"0.10.0 - 2025-09-17","text":""},{"location":"changelog/#miscellaneous-tasks_4","title":"\u2699\ufe0f Miscellaneous Tasks","text":"<ul> <li>(ci) Improve CI build matrix reporting (<code>777595c</code>)</li> <li>(ci) Prevent unnecessary branch builds on PRs (<code>4d7e911</code>)</li> <li>(release) Update all self ToolR actions usage on release (<code>38e2a8b</code>)</li> <li>(ci) Consistent workflow toolr output width (<code>4475406</code>)</li> </ul>"},{"location":"changelog/#090-2025-09-13","title":"0.9.0 - 2025-09-13","text":""},{"location":"changelog/#features_3","title":"\ud83d\ude80 Features","text":"<ul> <li>(cli) Provide a <code>Context</code> class</li> <li>(registry) Implemented the registry and it's tests</li> <li>(logging) Add <code>toolr.utils.logs</code> to improve logging support</li> <li>(cli) Provide the package CLI entry point</li> <li>(help) We now use <code>RichHelpFormatter</code> to render the help</li> <li>(docstrings) Parse docstrings to construct help</li> <li>(docs) Capture each parameter description from docstrings</li> <li>(coverage) Upload code coverage to codecov</li> <li>(ci) Upload test results to codecov</li> <li>(signatures) Add signature parsing</li> <li>(signature) Handle append action, including weird boolean append.</li> <li>(nargs) Support <code>nargs</code> and <code>*variable</code> in function signatures</li> <li>(docs) Documentation!</li> <li>(context) Implemented prompt support in <code>Context</code>.</li> <li>(github-actions) Allow setting ToolR from a github-action</li> <li>(signature) Add support for mutually exclusive groups</li> <li>(logging) Add <code>setup_logging</code> function.</li> </ul>"},{"location":"changelog/#bug-fixes_3","title":"\ud83d\udc1b Bug Fixes","text":"<ul> <li>(imports) Handle import errors when searching for tools</li> <li>(descriptions) Differentiate descriptions</li> <li>(docstring) Fix dosctring class reference</li> <li>(decorator) Fix decorator usage.</li> <li>(help) Parse each decorated command docstring to provide help</li> <li>(log) Only log the time on specific occasions.</li> <li>(tests) Fix tests according to latest code changes</li> <li>(tests) Fix rust tests on windows</li> <li>(scope) Let the codecov CLI tool find the coverage files</li> <li>(coverage) Don't track coverage in <code>if TYPE_CHECKING:</code> code blocks</li> <li>(signature) <code>dest</code> is always set to the name of the positional parameter</li> <li>(tests) Small refactor to improve testing</li> <li>(signature) On positional arguments, the name will always be the first alias</li> <li>(enums) Handle enums by name instead of by value</li> <li>(cli) Fix early verbose/debug output CLI parsing logic</li> <li>(tests) Skip problematic windows test</li> <li>(docs) Include missing docs examples</li> <li>(docs) Remove <code>uv run</code> prefix from examples</li> <li>(command) Command names from functions auto-naming</li> <li>(SignatureError) <code>SignatureError</code> exceptions now point to command</li> <li>(pypi) Fix PyPi packaging uploads</li> </ul>"},{"location":"changelog/#refactor_1","title":"\ud83d\ude9c Refactor","text":"<ul> <li>(toolr) Support 3rd-party commands</li> <li>(consoles) Name context consoles explicitly</li> <li>(3rd-party) Fix commands and command groups augment/overrides</li> <li>(consoles) Refactor consoles setup</li> </ul>"},{"location":"changelog/#miscellaneous-tasks_5","title":"\u2699\ufe0f Miscellaneous Tasks","text":"<ul> <li>(dependencies) Add <code>rich=-argparse</code> as a dependency</li> <li>(command) Rename <code>command.run_command</code> to <code>command.run</code></li> <li>(context) Make the <code>context</code> module \"private\".</li> <li>(ci) Define allowed concurrency</li> <li>(requiremenst) We no longer need to maintain separate requirements files</li> <li>(tools) Clean up the pre-existing tools directory</li> <li>(pre-commit) Update pre-commit hook versions</li> <li>(lint) Fix lint issues found with latest pre-commit hooks versions</li> <li>(cibuildweel) Bump <code>MACOSX_DEPLOYMENT_TARGET</code> to <code>11.0</code></li> <li>(cleanup) Remove <code>changelog.d/</code>, it won't be needed anymore</li> <li>(typing) Make the typing gods happier</li> <li>(msgspec) Replaced all usages of <code>dataclass</code> with <code>msgspec.Struct</code></li> <li>(pre-commit) Upgrade some pre-commit hooks</li> <li>(pre-commit) Add <code>codespell</code> pre-commit hook</li> <li>(parser) Use a private method to set the parser instead.</li> <li>(discovery) Actually start discovering tools when running <code>toolr</code></li> <li>(typing) Fix typing</li> <li>(samples) Fix sample cases to respect the required signature</li> <li>(rust) Address clippy errors</li> <li>(ci) Define the pre-commit cache to be inside the workspace</li> <li>(ci) Parallelize package builds</li> <li>(ci) Use OIDC to authenticate codecov</li> <li>(tests) Add default pytest flags to config</li> <li>(dependencies) Add <code>pytest-subtests</code> to dev dependencies</li> <li>(tests) Add <code>argv</code> tests</li> <li>(logs) Logging utils module testing</li> <li>(tests) Add test coverage for the <code>__main__</code> module</li> <li>(tests) Improve test coverage of the context object</li> <li>(README.md) Fix logo file path</li> <li>(mypy) Have mypy ignore <code>tests/support/3rd-party-pkg/.*</code></li> <li>(tests) Add test coverage to <code>setup_consoles</code></li> <li>(pyproject.toml) Define the 3rd-party test package as editable</li> <li>(ci) Improved parallelization</li> <li>(pre-commit) Update hook versions</li> <li>(tests) Split <code>tests/test_context.py</code> into several test modules</li> <li>(docs) Add <code>ruff</code> as a docs dependency</li> <li>(ci) Add and use <code>.github/actions/setup-virtualenv</code></li> <li>(ci) Push built packages to test.pypi.org on the default branch</li> <li>(docs) Fix logo URL in readme</li> <li>(gitignore) Ignore <code>*.code-workspace</code></li> <li>(pre-commit) Upgrade pre-commit hook versions</li> <li>(ConsoleVerbosity) Move <code>ConsoleVerbosity</code> to  <code>toolr.utils._console</code></li> <li>(action) Simplify action</li> <li>(release) Update the release process</li> <li>(release) Separate release workflow</li> <li>(security) Include build provenance attestations</li> <li>(debug) Set verbose to true when running in debug mode</li> <li>(oackages) Stop building for <code>s390x</code>.</li> <li>(dependabot) Add <code>dependabot</code> configuration</li> <li>(docs) Add <code>.readthedocs.yaml</code> config file</li> <li>(release) Fix attestations on release workflow</li> <li>(release) Fix generate build matrix step</li> <li>(changelog) Add cliff config file</li> <li>(release) More release workflow fixes</li> <li>(release) Use the global permissions</li> <li>(release) Use GH App to push the tags</li> <li>(release) The action now just configures git with higher privileges</li> <li>(release) Just repeat, it's simpler in the end</li> <li>(release) Use <code>sdist</code> to build wheels</li> <li>(release) Prepare for 0.1.1 release</li> <li>(release) Publish GH release fixes</li> <li>(release) Prepare for 0.1.2 release</li> <li>(release) Change release notes filename name</li> <li>(docs) Update the docs URL to  the right one</li> <li>(release) Remove the PyPi url</li> <li>(release) Revert debug release changes</li> <li>(release) Fix package name to be compliant with PyPi</li> <li>(changelog) Fix white-space issues around changelog generation</li> <li>(prepare-release) Run <code>pre-commit</code> against the prepare release changes</li> <li>(ci) Pre-commit needs to be setup and run in a few places</li> </ul>"},{"location":"changelog/#new-contributors_1","title":"New Contributors","text":"<ul> <li>@s0undt3ch-gh-actions-automations[bot] made their first contribution</li> <li>@dependabot[bot] made their first contribution</li> </ul>"},{"location":"cli/","title":"CLI reference","text":"<p>Every <code>toolr</code> subcommand documented in one place. Top-level flags first, then each subcommand under its namespace. Each section is anchored for grepping.</p>"},{"location":"cli/#top-level","title":"Top-level","text":"<pre><code>toolr --help\n</code></pre> <pre><code>In-project CLI tooling support\n\nUsage: toolr [OPTIONS] [COMMAND]\n\nCommands:\n  example  Example commands\n  project  Operations on the current repo's tools/ directory\n  self     Operations on toolr itself\n\nOptions:\n  -h, --help     Print help\n  -V, --version  Print version\n\nOutput Options:\n  -d, --debug\n          Increase verbosity (also enables `DEBUG` logging).\n  -q, --quiet\n          Suppress non-error output.\n      --timestamps\n          Prepend ISO-8601 timestamps to log lines.\n      --no-timestamps\n          Suppress log-line timestamps (default; overrides `--timestamps`).\n      --timeout-secs &lt;SECONDS&gt;\n          Default timeout applied to every `ctx.run(...)` subprocess (per-call `timeout_secs=` wins\n          when set).\n      --no-output-timeout-secs &lt;SECONDS&gt;\n          Default no-output watchdog applied to every `ctx.run(...)` subprocess \u2014 abort if no\n          stdout/stderr for this many seconds. Per-call `no_output_timeout_secs=` wins when set.\n</code></pre> <ul> <li><code>toolr --version</code> \u2014 print the binary's version.</li> <li><code>toolr --help</code> \u2014 print top-level help. On a leaf command the   long form prints the full docstring (rendered via termimad);   <code>-h</code> prints the same content as a one-line summary.</li> </ul>"},{"location":"cli/#output-options","title":"Output Options","text":"<p>Five root-level flags that tweak how toolr's own output renders and the defaults for any <code>ctx.run(...)</code> subprocess your command starts. They're root-only \u2014 place them before the subcommand (<code>toolr -d --timeout-secs 30 ci hello</code>, not <code>toolr ci hello -d --timeout-secs 30</code>).</p> Flag What it does <code>-d</code> / <code>--debug</code> Increase verbosity. Also enables Python <code>DEBUG</code> logging in the runner. Mutually exclusive with <code>--quiet</code>. <code>-q</code> / <code>--quiet</code> Suppress non-error output. <code>--timestamps</code> / <code>--ts</code> Prepend ISO-8601 timestamps to log lines. <code>--no-timestamps</code> / <code>--nts</code> Suppress log-line timestamps (default; wins over <code>--timestamps</code>). <code>--timeout-secs SECONDS</code> / <code>--timeout</code> Default timeout passed to every <code>ctx.run(...)</code> call. Per-call <code>timeout_secs=</code> still wins. <code>--no-output-timeout-secs SECONDS</code> / <code>--nots</code> Default \"no output for N seconds\" watchdog applied to every <code>ctx.run(...)</code>. Per-call <code>no_output_timeout_secs=</code> still wins. <p>How the timeouts interact with <code>ctx.run</code>:</p> <pre><code>@command(group=\"example\")\ndef slow(ctx):\n    \"\"\"Tools-author writes this, oblivious to root flags.\"\"\"\n    ctx.run(\"sleep\", \"30\")                       # uses --timeout-secs default if set\n    ctx.run(\"sleep\", \"30\", timeout_secs=60)      # per-call wins; ignores root flag\n</code></pre> <p>The defaults flow through the runner spec as <code>ContextSpec.default_timeout_secs</code> / <code>default_no_output_timeout_secs</code>. They're plain numbers in seconds; <code>None</code> (the default-default) means \"no watchdog unless the caller opts in.\"</p>"},{"location":"cli/#toolr-project","title":"<code>toolr project ...</code>","text":"<p>Operations on the current repo's <code>tools/</code> directory.</p>"},{"location":"cli/#project-init","title":"<code>toolr project init</code>","text":"<p>Scaffold <code>tools/</code> in the current directory.</p> <p>Usage: <code>toolr project init [--force] [--no-sync] [--venv-location {cache,in-tree}] [--no-example] [--python &lt;version&gt;] [--quiet]</code></p> <p>Flags:</p> <ul> <li><code>--force</code> \u2014 overwrite an existing <code>tools/</code> directory.</li> <li><code>--no-sync</code> \u2014 skip the automatic <code>uv sync</code> after scaffolding.</li> <li><code>--venv-location</code> \u2014 <code>cache</code> (default) or <code>in-tree</code>. Sets the   <code>[tool.toolr] venv-location</code> value in the generated <code>pyproject.toml</code>.</li> <li><code>--no-example</code> \u2014 skip generating <code>tools/example.py</code>.</li> <li><code>--python</code> \u2014 <code>requires-python</code> value for <code>tools/pyproject.toml</code>   (defaults to the running Python's <code>&gt;=major.minor</code>).</li> <li><code>--quiet</code> \u2014 suppress informational output.</li> </ul> <pre><code>toolr project init --help\n</code></pre> <pre><code>Scaffold tools/ in the current directory\n\nUsage: toolr project init [OPTIONS]\n\nOptions:\n      --force                     Overwrite an existing tools/ directory\n      --no-sync                   Skip the automatic `uv sync` after scaffolding\n      --venv-location &lt;LOCATION&gt;  Where the tools venv should live [default: cache] [possible\n                                  values: cache, in-tree]\n      --no-example                Skip generating tools/example.py\n      --python &lt;VERSION&gt;          `requires-python` value for tools/pyproject.toml (defaults to the\n                                  running Python's &gt;=major.minor)\n  -q, --quiet                     Suppress informational output\n  -h, --help                      Print help\n</code></pre> <p>See also: Quickstart, Project configuration.</p>"},{"location":"cli/#project-deps-sync","title":"<code>toolr project deps sync</code>","text":"<p>Materialise (or refresh) the tools venv via <code>uv sync</code>.</p> <pre><code>toolr project deps sync --help\n</code></pre> <pre><code>Run `uv sync` against tools/\n\nUsage: toolr project deps sync\n\nOptions:\n  -h, --help  Print help\n</code></pre> <p>Runs the full sync flow (see Project configuration \u2192 sync interaction).</p>"},{"location":"cli/#project-venv-path","title":"<code>toolr project venv path</code>","text":"<p>Print the absolute path to the resolved tools venv. Useful for shell-scripting or sanity-checking <code>venv-location</code>.</p> <pre><code>toolr project venv path --help\n</code></pre> <pre><code>Print the absolute path to the tools venv\n\nUsage: toolr project venv path\n\nOptions:\n  -h, --help  Print help\n</code></pre>"},{"location":"cli/#project-venv-shell","title":"<code>toolr project venv shell</code>","text":"<p>Spawn a subshell with the tools venv activated. <code>$VIRTUAL_ENV</code> is set, <code>$PATH</code> is prepended with the venv's <code>bin/</code>, and <code>$TOOLR_VENV</code> carries the venv path for prompt customisation.</p> <pre><code>toolr project venv shell --help\n</code></pre> <pre><code>Spawn a subshell with the tools venv activated\n\nUsage: toolr project venv shell\n\nOptions:\n  -h, --help  Print help\n</code></pre>"},{"location":"cli/#project-manifest-rebuild","title":"<code>toolr project manifest rebuild</code>","text":"<p>Regenerate the static + dynamic manifest in place. Equivalent to what toolr does automatically when it detects drift, but explicit and reportable.</p> <pre><code>toolr project manifest rebuild --help\n</code></pre> <pre><code>Regenerate the static + dynamic manifest in place\n\nUsage: toolr project manifest rebuild\n\nOptions:\n  -h, --help  Print help\n</code></pre>"},{"location":"cli/#toolr-self","title":"<code>toolr self ...</code>","text":"<p>Operations on toolr itself (not on the project).</p>"},{"location":"cli/#self-completion-print","title":"<code>toolr self completion print &lt;shell&gt;</code>","text":"<p>Print the completion script for <code>&lt;shell&gt;</code> to stdout. Useful for piping into your own dotfiles management.</p> <p><code>&lt;shell&gt;</code>: <code>bash</code>, <code>zsh</code>, or <code>fish</code>.</p> <pre><code>toolr self completion print --help\n</code></pre> <pre><code>Print the completion script for a shell to stdout\n\nUsage: toolr self completion print &lt;shell&gt;\n\nArguments:\n  &lt;shell&gt;  Shell to emit a completion script for [possible values: bash, zsh, fish]\n\nOptions:\n  -h, --help  Print help\n</code></pre>"},{"location":"cli/#self-completion-install","title":"<code>toolr self completion install &lt;shell&gt; [--force]</code>","text":"<p>Write the completion script for <code>&lt;shell&gt;</code> into its standard location (<code>~/.bash_completion.d/</code>, <code>~/.zsh/completions/</code>, or <code>~/.config/fish/completions/</code>). <code>--force</code> overwrites an existing file without prompting.</p> <pre><code>toolr self completion install --help\n</code></pre> <pre><code>Install the completion script for a shell into its standard location\n\nUsage: toolr self completion install [OPTIONS] &lt;shell&gt;\n\nArguments:\n  &lt;shell&gt;  Shell to install the completion script for [possible values: bash, zsh, fish]\n\nOptions:\n      --force  Overwrite an existing differing file without prompting\n  -h, --help   Print help\n</code></pre>"},{"location":"cli/#self-cache-list","title":"<code>toolr self cache list</code>","text":"<p>Tabular listing of every cached per-repo venv: source repo, size, last-used timestamp.</p> <pre><code>toolr self cache list --help\n</code></pre> <pre><code>List every cached virtualenv with size and last-use timestamp\n\nUsage: toolr self cache list\n\nOptions:\n  -h, --help  Print help\n</code></pre>"},{"location":"cli/#self-cache-prune","title":"<code>toolr self cache prune</code>","text":"<p>Remove orphan and stale cache entries.</p> <p>Flags:</p> <ul> <li><code>--all</code> \u2014 remove every cache entry (with confirmation).</li> <li><code>--yes</code> / <code>-y</code> \u2014 skip the confirmation prompt when used with <code>--all</code>.</li> <li><code>--dry-run</code> \u2014 show what would be deleted without deleting.</li> <li><code>--stale-after-days &lt;DAYS&gt;</code> \u2014 override the default staleness   threshold (30 days).</li> </ul> <pre><code>toolr self cache prune --help\n</code></pre> <pre><code>Remove orphan and stale cache entries\n\nUsage: toolr self cache prune [OPTIONS]\n\nOptions:\n      --all                      Remove every cache entry\n      --stale-after-days &lt;DAYS&gt;  Override the staleness threshold [default: 30]\n      --dry-run                  Show what would be deleted without deleting\n  -y, --yes                      Skip confirmation when used with --all\n  -h, --help                     Print help\n</code></pre>"},{"location":"cli/#self-build-manifest","title":"<code>toolr self build-manifest &lt;package&gt;</code>","text":"<p>Generate a <code>toolr-manifest.json</code> fragment for a third-party package. Wraps <code>python -m toolr.build</code>; locates a working Python automatically.</p> <p>Flags:</p> <ul> <li><code>--check</code> \u2014 verify the on-disk manifest matches what regeneration   would produce; exit non-zero on drift.</li> <li><code>--output PATH</code> \u2014 write to a specific file instead of the   package's default location.</li> <li><code>--python PATH</code> \u2014 use a specific Python interpreter.</li> </ul> <pre><code>toolr self build-manifest --help\n</code></pre> <pre><code>Generate a third-party manifest fragment for a package\n\nUsage: toolr self build-manifest [OPTIONS] &lt;package&gt;\n\nArguments:\n  &lt;package&gt;  Dotted Python package name to introspect\n\nOptions:\n      --output &lt;PATH&gt;       Override the output path\n      --python &lt;PATH&gt;       Path to a Python interpreter to use\n      --schema-version &lt;N&gt;  Pin the emitted schema version\n      --check               Verify the on-disk manifest matches regeneration\n  -h, --help                Print help\n</code></pre> <p>See also: Third-party packages.</p>"},{"location":"cli/#internal-subcommands","title":"Internal subcommands","text":"<p>Used by other tooling (shell completion scripts, CI). Not part of the user-facing surface; documented here for completeness.</p>"},{"location":"cli/#__complete","title":"<code>toolr __complete &lt;cwd&gt; &lt;args...&gt;</code>","text":"<p>Completion endpoint called by the installed shell scripts. Reads the current repo's manifest, prefix-matches against subcommands and arg values, writes candidates to stdout. Sub-50ms.</p>"},{"location":"cli/#__build-static-manifest","title":"<code>toolr __build-static-manifest</code>","text":"<p>Regenerate only the static layer of the manifest (no Python introspection). Faster than <code>toolr project manifest rebuild</code> but won't pick up dynamically-registered commands.</p>"},{"location":"cli/#__install-uv-now","title":"<code>toolr __install-uv-now</code>","text":"<p>Force-install a managed copy of uv into <code>$XDG_DATA_HOME/toolr/bin/</code>, bypassing the usual consent prompt. Used by the install scripts and by <code>toolr project deps sync</code> after a consent flow.</p>"},{"location":"concepts/","title":"How toolr is laid out","text":"<p>A short tour of the pieces that show up when you use toolr in a repo. No deep dives \u2014 each item links to its own page.</p>"},{"location":"concepts/#the-toolr-binary","title":"The <code>toolr</code> binary","text":"<p>A single self-contained Rust executable. It parses your command line, loads your repo's manifest to know what commands exist, then spawns a Python subprocess to actually run them. See Installation.</p>"},{"location":"concepts/#the-tools-directory","title":"The <code>tools/</code> directory","text":"<p>A directory at the root of your repo where your commands live. Each <code>*.py</code> file under it can register one or more command groups. The directory is a PEP 420 namespace package \u2014 no <code>__init__.py</code> is needed. See Writing commands.</p>"},{"location":"concepts/#toolspyprojecttoml","title":"<code>tools/pyproject.toml</code>","text":"<p>Declares the Python dependencies your commands need (the <code>toolr</code> package itself, plus anything your <code>tools/*.py</code> files import) and toolr-specific options like the venv layout. Maintained alongside your code. See Project configuration.</p>"},{"location":"concepts/#the-tools-venv","title":"The tools venv","text":"<p>A Python virtualenv managed by uv, materialised from <code>tools/pyproject.toml</code>. By default lives in <code>$XDG_CACHE_HOME/toolr/&lt;repo-key&gt;/venv/</code> (one per repo); opt into in-tree <code>tools/.venv/</code> via <code>[tool.toolr] venv-location = \"in-tree\"</code>. Created by <code>toolr project deps sync</code> (or automatically by <code>toolr project init</code>). See Project configuration.</p>"},{"location":"concepts/#the-manifest","title":"The manifest","text":"<p><code>tools/.toolr-manifest.json</code> \u2014 the cached structure of every group and command the toolr binary knows about. Toolr regenerates it when <code>tools/</code> or your dependencies change, so tab completion and <code>--help</code> stay sub-50ms. Has two layers (static + dynamic). It's a pure cache \u2014 don't commit it to git (<code>toolr project init</code> adds it to <code>tools/.gitignore</code> for you). See Internals \u2192 Manifest layers.</p>"},{"location":"concepts/#tab-completion","title":"Tab completion","text":"<p>Shell scripts for bash, zsh, and fish that call a hidden <code>toolr __complete</code> endpoint on every Tab press. Install once per shell with <code>toolr self completion install &lt;shell&gt;</code>.</p>"},{"location":"concepts/#the-per-repo-cache","title":"The per-repo cache","text":"<p><code>$XDG_CACHE_HOME/toolr/</code> \u2014 one entry per repo, each holding its venv + a <code>meta.json</code> sidecar with timestamps. Inspect with <code>toolr self cache list</code>; prune orphans or stale entries with <code>toolr self cache prune</code>. See Internals \u2192 Cache layout.</p>"},{"location":"contributing/","title":"Contributing to ToolR","text":"<p>First off, thank you for considering contributing to ToolR! It's people like you that make ToolR such a great tool.</p>"},{"location":"contributing/#table-of-contents","title":"Table of Contents","text":"<ul> <li>Code of Conduct</li> <li>Getting Started</li> <li>Development Setup</li> <li>How to Contribute</li> <li>Pull Request Process</li> <li>Coding Standards</li> <li>Testing Guidelines</li> <li>Commit Message Guidelines</li> <li>Security</li> <li>Development Workflow</li> <li>Code Review Guidelines</li> </ul>"},{"location":"contributing/#code-of-conduct","title":"Code of Conduct","text":"<p>This project and everyone participating in it is governed by respect, professionalism, and inclusivity. By participating, you are expected to uphold these values. Please report unacceptable behavior by opening a GitHub Issue or contacting the project maintainers.</p>"},{"location":"contributing/#our-standards","title":"Our Standards","text":"<p>Positive behaviors include:</p> <ul> <li>Using welcoming and inclusive language</li> <li>Being respectful of differing viewpoints and experiences</li> <li>Gracefully accepting constructive criticism</li> <li>Focusing on what is best for the community</li> <li>Showing empathy towards other community members</li> </ul> <p>Unacceptable behaviors include:</p> <ul> <li>Trolling, insulting/derogatory comments, and personal or political attacks</li> <li>Public or private harassment</li> <li>Publishing others' private information without explicit permission</li> <li>Other conduct which could reasonably be considered inappropriate in a professional setting</li> </ul>"},{"location":"contributing/#getting-started","title":"Getting Started","text":""},{"location":"contributing/#prerequisites","title":"Prerequisites","text":"<p>Before contributing, make sure you have:</p> <ul> <li>Python 3.11+ installed</li> <li>Rust toolchain (for building native extensions)</li> <li>mise - Development environment manager</li> <li>gh CLI (optional, for GitHub Actions utilities)</li> <li>git for version control</li> </ul>"},{"location":"contributing/#first-time-contributors","title":"First-time Contributors","text":"<p>New to open source? Here are some resources:</p> <ul> <li>How to Contribute to Open Source</li> <li>First Timers Only</li> <li>GitHub's Guide to Contributing</li> </ul> <p>Look for issues labeled <code>good first issue</code> or <code>help wanted</code>.</p>"},{"location":"contributing/#development-setup","title":"Development Setup","text":""},{"location":"contributing/#1-fork-and-clone","title":"1. Fork and Clone","text":"<pre><code># Fork the repository on GitHub, then clone your fork\ngit clone https://github.com/YOUR_USERNAME/ToolR.git\ncd ToolR\n\n# Add upstream remote\ngit remote add upstream https://github.com/s0undt3ch/ToolR.git\n</code></pre>"},{"location":"contributing/#2-install-mise","title":"2. Install mise","text":"<p>mise manages our development environment, including Python, Rust, and other tools (see https://mise.jdx.dev/getting-started.html).</p> <pre><code>curl https://mise.run | sh\n\n# Install project tools\nmise install\n\n# Activate the environment\nmise activate\n</code></pre> <p>This will automatically:</p> <ul> <li>Install the correct Python version</li> <li>Install the Rust toolchain</li> <li>Install uv package manager</li> <li>Set up other project dependencies</li> </ul>"},{"location":"contributing/#3-install-dependencies","title":"3. Install Dependencies","text":"<pre><code># Sync Python dependencies and create virtual environment\nuv sync --all-extras --dev\n</code></pre>"},{"location":"contributing/#4-install-prek-pre-commit-hooks","title":"4. Install prek (Pre-commit Hooks)","text":"<p>We use prek instead of standard pre-commit:</p> <pre><code># Install prek hooks\nprek install --install-hooks\n</code></pre>"},{"location":"contributing/#5-verify-setup","title":"5. Verify Setup","text":"<pre><code># Run tests\nuv run pytest\n\n# Run all pre-commit checks\nprek run --all-files\n</code></pre>"},{"location":"contributing/#how-to-contribute","title":"How to Contribute","text":""},{"location":"contributing/#reporting-bugs","title":"Reporting Bugs","text":"<p>GitHub Issues are used for bug tracking. Before creating a bug report:</p> <ul> <li>Search existing issues to avoid duplicates</li> <li>Use the latest version of ToolR</li> <li>Collect information about your environment</li> </ul> <p>When filing a bug report, include:</p> <ul> <li>Clear title and description</li> <li>Steps to reproduce the issue</li> <li>Expected vs actual behavior</li> <li>Environment details (OS, Python version, ToolR version)</li> <li>Error messages (full stack traces)</li> <li>Screenshots if applicable</li> </ul> <p>Create a new issue to report bugs.</p>"},{"location":"contributing/#suggesting-enhancements","title":"Suggesting Enhancements","text":"<p>Feature requests are also tracked as GitHub Issues. When creating an enhancement suggestion:</p> <ul> <li>Use a clear title describing the enhancement</li> <li>Provide a detailed description of the proposed functionality</li> <li>Explain why this would be useful to most ToolR users</li> <li>List examples of how the enhancement would be used</li> <li>Note any alternatives you've considered</li> </ul> <p>Create a new issue to suggest features.</p>"},{"location":"contributing/#pull-requests","title":"Pull Requests","text":"<p>GitHub Pull Requests are used for code contributions. Follow the pull request process below.</p>"},{"location":"contributing/#your-first-code-contribution","title":"Your First Code Contribution","text":"<ol> <li>Find an issue to work on (or create one)</li> <li>Comment on the issue to let others know you're working on it</li> <li>Create a branch from <code>main</code>:</li> </ol> <pre><code>git checkout -b feature/your-feature-name\n# or\ngit checkout -b fix/your-bug-fix\n</code></pre> <ol> <li>Make your changes following our coding standards</li> <li>Write/update tests to cover your changes</li> <li>Run the test suite to ensure nothing breaks</li> <li>Commit your changes with descriptive messages</li> <li>Push to your fork and submit a pull request</li> </ol>"},{"location":"contributing/#pull-request-process","title":"Pull Request Process","text":""},{"location":"contributing/#before-submitting","title":"Before Submitting","text":"<ul> <li>[ ] Run all tests and ensure they pass</li> <li>[ ] Run prek to check linting, formatting, and types</li> <li>[ ] Update documentation if needed</li> <li>[ ] Add tests for new functionality</li> <li>[ ] Ensure commits follow our commit message guidelines</li> <li>[ ] Rebase on latest main if needed</li> </ul> <p>Note: Do NOT manually update <code>CHANGELOG.md</code> - it's automatically generated from commit messages using git-cliff.</p>"},{"location":"contributing/#submitting","title":"Submitting","text":"<ol> <li>Push your branch to your fork</li> <li>Open a pull request against the <code>main</code> branch</li> <li>Write a clear PR description:</li> <li>What changes were made</li> <li>Why the changes are needed</li> <li>Any breaking changes</li> <li>Link related issues (e.g., \"Closes #123\")</li> <li>Respond to feedback and update as needed</li> </ol>"},{"location":"contributing/#pr-review-process","title":"PR Review Process","text":"<ul> <li>Maintainers will review your PR within a few days</li> <li>You may be asked to make changes</li> <li>Once approved, maintainers will merge your PR</li> <li>Your contribution will be included in the next release!</li> </ul>"},{"location":"contributing/#ci-checks","title":"CI Checks","text":"<p>All PRs must pass:</p> <ul> <li>\u2705 Tests (pytest on Linux/macOS/Windows)</li> <li>\u2705 Linting (ruff, via prek)</li> <li>\u2705 Type checking (mypy, via prek)</li> <li>\u2705 Formatting (ruff format, via prek)</li> <li>\u2705 Rust checks (cargo clippy, cargo check, via prek)</li> <li>\u2705 GitHub Actions (actionlint, via prek)</li> <li>\u2705 Shell scripts (shellcheck, via prek)</li> <li>\u2705 Security (CodeQL, dependency review)</li> <li>\u2705 Spelling (codespell, typos, via prek)</li> <li>\u2705 Markdown (markdownlint, via prek)</li> <li>\u2705 Documentation (builds without errors)</li> </ul> <p>All these checks run automatically via prek hooks and CI.</p>"},{"location":"contributing/#coding-standards","title":"Coding Standards","text":""},{"location":"contributing/#python-code","title":"Python Code","text":"<ul> <li>Follow PEP 8 for style (enforced by ruff)</li> <li>Use type hints for all function signatures</li> <li>Use <code>from __future__ import annotations</code> at the top of files</li> <li>Maximum line length: 120 characters</li> <li>Use ruff for linting and formatting (automated by prek)</li> <li>Use mypy for type checking (automated by prek)</li> </ul> <pre><code>from __future__ import annotations\n\nfrom typing import Any\n\ndef greet(name: str, *, enthusiastic: bool = False) -&gt; str:\n    \"\"\"Greet someone by name.\n\n    Args:\n        name: The name of the person to greet.\n        enthusiastic: Whether to add extra enthusiasm.\n\n    Returns:\n        A greeting message.\n    \"\"\"\n    greeting = f\"Hello, {name}\"\n    return f\"{greeting}!\" if enthusiastic else greeting\n</code></pre>"},{"location":"contributing/#rust-code","title":"Rust Code","text":"<ul> <li>Follow Rust style guidelines</li> <li>Use <code>cargo fmt</code> for formatting (automated by prek)</li> <li>Use <code>cargo clippy</code> for linting (automated by prek)</li> <li>Write doc comments for public APIs</li> <li>Add tests for new functionality</li> </ul> <pre><code>/// Greet someone by name.\n///\n/// # Arguments\n///\n/// * `name` - The name of the person to greet\n///\n/// # Returns\n///\n/// A greeting message\npub fn greet(name: &amp;str) -&gt; String {\n    format!(\"Hello, {}\", name)\n}\n</code></pre>"},{"location":"contributing/#documentation","title":"Documentation","text":"<ul> <li>Use Google-style docstrings for Python</li> <li>Use Rustdoc for Rust code</li> <li>Include examples in docstrings</li> <li>Update docs/ when adding features</li> <li>Keep README.md up to date</li> </ul>"},{"location":"contributing/#pre-commit-hooks","title":"Pre-commit Hooks","text":"<p>All formatting and linting is handled automatically by prek:</p> <pre><code># Run all checks\nprek run --all-files\n\n# Run specific hook\nprek run ruff-format\n\n# Bypass hooks (use sparingly!)\ngit commit --no-verify\n</code></pre>"},{"location":"contributing/#testing-guidelines","title":"Testing Guidelines","text":""},{"location":"contributing/#writing-tests","title":"Writing Tests","text":"<ul> <li>Write tests for all new functionality</li> <li>Update tests when changing behavior</li> <li>Test edge cases and error conditions</li> <li>Use descriptive test names</li> <li>Keep tests focused (one concept per test)</li> </ul>"},{"location":"contributing/#running-tests","title":"Running Tests","text":"<pre><code># Run all tests\nuv run pytest\n\n# Run specific test file\nuv run pytest tests/test_version.py\n\n# Run with coverage\nuv run coverage run -m pytest\nuv run coverage report\n\n# Run Rust tests\ncargo test\n\n# Run specific Rust test\ncargo test test_name\n</code></pre>"},{"location":"contributing/#test-organization","title":"Test Organization","text":"<pre><code>tests/\n\u251c\u2500\u2500 cli/                 # CLI argument parsing tests\n\u251c\u2500\u2500 context/             # Context and runtime tests\n\u251c\u2500\u2500 parser/              # Parser tests\n\u251c\u2500\u2500 registry/            # Command discovery and registry tests\n\u251c\u2500\u2500 utils/               # Utility function tests\n\u251c\u2500\u2500 support/             # Test fixtures and support files\n\u251c\u2500\u2500 conftest.py          # Shared fixtures\n\u2514\u2500\u2500 test_*.py            # Additional test modules\n</code></pre>"},{"location":"contributing/#property-based-testing","title":"Property-Based Testing","text":"<p>ToolR uses Hypothesis for property-based testing (fuzzing):</p> <pre><code>from hypothesis import given\nfrom hypothesis import strategies as st\n\n@given(st.text())\ndef test_handles_any_input(text: str) -&gt; None:\n    \"\"\"Test that the function handles any text input.\"\"\"\n    result = process_text(text)\n    assert isinstance(result, str)\n</code></pre>"},{"location":"contributing/#commit-message-guidelines","title":"Commit Message Guidelines","text":"<p>We follow Conventional Commits for clear commit history and automated changelogs.</p>"},{"location":"contributing/#format","title":"Format","text":"<pre><code>&lt;type&gt;(&lt;scope&gt;): &lt;subject&gt;\n\n&lt;body&gt;\n\n&lt;footer&gt;\n</code></pre>"},{"location":"contributing/#types","title":"Types","text":"<ul> <li>feat: New feature</li> <li>fix: Bug fix</li> <li>docs: Documentation changes</li> <li>style: Code style changes (formatting, etc.)</li> <li>refactor: Code refactoring</li> <li>perf: Performance improvements</li> <li>test: Adding or updating tests</li> <li>build: Build system changes</li> <li>ci: CI configuration changes</li> <li>chore: Other changes (dependencies, etc.)</li> </ul>"},{"location":"contributing/#examples","title":"Examples","text":"<pre><code>feat(cli): add --verbose flag for detailed output\n\nAdd a --verbose flag to enable detailed logging output.\nThis helps users debug issues with command execution.\n\nCloses #123\n\n---\n\nfix(version): correct git describe pattern for multi-digit versions\n\nThe pattern v[0-9].[0-9].[0-9] only matched single digits.\nChanged to v[0-9]*.[0-9]*.[0-9]* to support versions like v0.11.0.\n\nFixes #456\n\n---\n\ndocs: update contributing guidelines with commit conventions\n</code></pre>"},{"location":"contributing/#breaking-changes","title":"Breaking Changes","text":"<p>For breaking changes, add <code>BREAKING CHANGE:</code> in the footer:</p> <pre><code>feat(api)!: remove deprecated get_version function\n\nBREAKING CHANGE: The get_version() function has been removed.\nUse version.current() instead.\n</code></pre>"},{"location":"contributing/#why-conventional-commits","title":"Why Conventional Commits?","text":"<ul> <li>Automated changelog generation using git-cliff</li> <li>Semantic versioning automation</li> <li>Clear history for understanding project evolution</li> <li>Better collaboration through standardized messages</li> </ul>"},{"location":"contributing/#security","title":"Security","text":""},{"location":"contributing/#reporting-vulnerabilities","title":"Reporting Vulnerabilities","text":"<p>DO NOT open a public issue for security vulnerabilities.</p> <p>Instead, use GitHub Security Advisories to privately report security issues.</p> <p>See our Security Policy for more details.</p>"},{"location":"contributing/#security-best-practices","title":"Security Best Practices","text":"<p>When contributing:</p> <ul> <li>Never commit secrets (API keys, passwords, tokens)</li> <li>Pin action versions to commit SHAs in workflows</li> <li>Validate user input to prevent injection attacks</li> <li>Use secure defaults in configurations</li> <li>Review dependencies for known vulnerabilities</li> </ul>"},{"location":"contributing/#development-workflow","title":"Development Workflow","text":""},{"location":"contributing/#branch-naming","title":"Branch Naming","text":"<ul> <li><code>feature/description</code> - New features</li> <li><code>fix/description</code> - Bug fixes</li> <li><code>docs/description</code> - Documentation updates</li> <li><code>refactor/description</code> - Code refactoring</li> <li><code>test/description</code> - Test additions/updates</li> </ul>"},{"location":"contributing/#daily-development","title":"Daily Development","text":"<pre><code># Update your fork\ngit checkout main\ngit pull upstream main\ngit push origin main\n\n# Create feature branch\ngit checkout -b feature/my-feature\n\n# Make changes, commit often\ngit add .\ngit commit -m \"feat: add my feature\"\n\n# Run checks before pushing\nprek run --all-files\n\n# Push to your fork\ngit push origin feature/my-feature\n\n# Open a pull request on GitHub\n</code></pre>"},{"location":"contributing/#keeping-your-branch-updated","title":"Keeping Your Branch Updated","text":"<pre><code># Fetch latest changes\ngit fetch upstream\n\n# Rebase your branch\ngit rebase upstream/main\n\n# Force push if already pushed\ngit push --force-with-lease origin feature/my-feature\n</code></pre>"},{"location":"contributing/#code-review-guidelines","title":"Code Review Guidelines","text":""},{"location":"contributing/#for-contributors","title":"For Contributors","text":"<ul> <li>Respond promptly to review feedback</li> <li>Ask questions if feedback is unclear</li> <li>Make requested changes or explain why not</li> <li>Mark conversations as resolved when addressed</li> <li>Be respectful and professional</li> </ul>"},{"location":"contributing/#for-reviewers","title":"For Reviewers","text":"<ul> <li>Review thoroughly but constructively</li> <li>**Provide specific feedback</li> </ul>"},{"location":"migration/","title":"Migration: nested-group method form \u2192 dotted strings","text":"<p>Toolr originally let you declare a subgroup by calling <code>.command_group(...)</code> on a captured parent binding:</p> <pre><code>docker = command_group(\"docker\", \"Docker\", \"Container utilities\")\ndocker_image = docker.command_group(\"image\", \"Image\")\n</code></pre> <p>That form is deprecated and will be removed in toolr 1.0. Every call emits a <code>ToolrDeprecationWarning</code> at runtime, so the offenders are easy to spot.</p> <p>The bound <code>@group.command</code> decorator on a captured <code>CommandGroup</code> is not deprecated and continues to be supported \u2014 see Groups &amp; commands for the canonical single-file usage and Scaling command groups across files for the string-keyed form you'd use across multiple files.</p>"},{"location":"migration/#whats-changing","title":"What's changing","text":"Legacy (deprecated) Replacement <code>parent.command_group(\"child\", ...)</code> <code>command_group(\"parent.child\", ...)</code> <code>command_group(\"child\", parent=parent_var)</code> <code>command_group(\"parent.child\", ...)</code> <p><code>@group.command</code> and <code>@group.command(\"custom-name\")</code> continue to work without warnings.</p>"},{"location":"migration/#recipe","title":"Recipe","text":"<p>Replace the bound subgroup-method call with a dotted <code>command_group(...)</code> declaration. The child names its parent inline:</p> <p>Before:</p> <pre><code>docker = command_group(\"docker\", \"Docker\", \"Container utilities\")\ndocker_image = docker.command_group(\"image\", \"Image\")\n\n\n@docker_image.command\ndef build(ctx, tag: str): ...\n</code></pre> <p>After:</p> <pre><code>docker = command_group(\"docker\", \"Docker\", \"Container utilities\")\ndocker_image = command_group(\"docker.image\", \"Image\")\n\n\n@docker_image.command\ndef build(ctx, tag: str): ...\n</code></pre> <p>The <code>parent=\"parent\"</code> keyword is an equivalent alternative if you prefer to keep the child's leaf name unprefixed:</p> <pre><code>command_group(\"image\", parent=\"docker\", description=\"\u2026\")\n</code></pre>"},{"location":"migration/#why-migrate","title":"Why migrate","text":"<p>The legacy decorators won't ship in toolr 1.0 \u2014 migrating now means no last-minute scramble. The captured-binding form for subgroups also reads awkwardly once nesting gets deeper than one level (<code>a.command_group(\"b\").command_group(\"c\", ...)</code>); dotted strings flatten that to <code>command_group(\"a.b.c\", ...)</code> with no chain.</p> <p>The remaining rationale for the string-keyed <code>@command(group=\"\u2026\")</code> form lives in Scaling command groups across files \u2014 that decorator is a choice, not a deprecation step.</p> <p>If you hit anything the recipe above doesn't cover \u2014 for instance, projects that subclass <code>CommandGroup</code> or wrap the decorators \u2014 open an issue on GitHub so we can document the path.</p>"},{"location":"project-config/","title":"Project configuration","text":"<p>Every repo using toolr has a <code>tools/pyproject.toml</code>. It declares the Python dependencies your commands need (the <code>toolr</code> package itself, plus anything your <code>tools/*.py</code> files import) and a handful of toolr-specific options.</p> <p>A minimal file:</p> <pre><code>[project]\nname = \"tools\"\nversion = \"0.0.0\"\nrequires-python = \"&gt;=3.11\"\ndependencies = [\n    \"toolr\",\n]\n\n[tool.toolr]\nvenv-location = \"cache\"\n</code></pre> <p>That's enough for <code>toolr project deps sync</code> to materialise a working venv and for the binary to dispatch commands.</p>"},{"location":"project-config/#project-block","title":"<code>[project]</code> block","text":"<p>Standard PEP 621 metadata. Toolr inspects:</p> <ul> <li><code>requires-python</code> \u2014 used as a fallback for the venv's Python   version when <code>[tool.toolr] python-version</code> is not set.</li> <li><code>dependencies</code> \u2014 passed straight through to <code>uv sync</code>. Add every   package your <code>tools/*.py</code> files import. The <code>toolr</code> package itself   must be present \u2014 toolr refuses to operate against a venv that   doesn't have it installed.</li> </ul> <p>Other PEP 621 fields (<code>name</code>, <code>version</code>, <code>description</code>, \u2026) are informational; toolr doesn't read them.</p>"},{"location":"project-config/#tooltoolr-options","title":"<code>[tool.toolr]</code> options","text":""},{"location":"project-config/#venv-location","title":"<code>venv-location</code>","text":"<p>Where the tools venv is materialised.</p> <ul> <li>Default: <code>\"cache\"</code> \u2014 <code>$XDG_CACHE_HOME/toolr/&lt;repo-key&gt;/venv/</code>   (per-repo, shared across worktrees of the same checkout, easy to   prune via <code>toolr self cache prune</code>).</li> <li>Alternative: <code>\"in-tree\"</code> \u2014 <code>tools/.venv/</code> inside the repo.   Useful when the cache directory is volatile (CI runners, ephemeral   containers) or you want the venv visible alongside the source.</li> </ul> <pre><code>[tool.toolr]\nvenv-location = \"in-tree\"\n</code></pre> <p>The default <code>tools/.gitignore</code> written by <code>toolr project init</code> already excludes <code>.venv/</code>, so the in-tree venv won't leak into git.</p>"},{"location":"project-config/#editable-install","title":"<code>editable-install</code>","text":"<p>A list of paths to <code>uv pip install -e</code> after the initial sync. Typically used to pick up an editable install of the surrounding project so commands can <code>import the_app</code> directly.</p> <pre><code>[tool.toolr]\neditable-install = [\".\"]\n</code></pre> <p>Each entry is resolved relative to the repo root. The post-sync hook is best-effort \u2014 failures log a warning but don't fail the sync.</p>"},{"location":"project-config/#python-version","title":"<code>python-version</code>","text":"<p>Explicit override of the venv's Python version. Falls back to <code>[project] requires-python</code> when unset.</p> <pre><code>[tool.toolr]\npython-version = \"3.13\"\n</code></pre> <p>Use this when your tools need a newer Python than your application code, or vice versa.</p>"},{"location":"project-config/#toolsuvlock","title":"<code>tools/uv.lock</code>","text":"<p>Created by the first <code>uv sync</code> invocation. Commit it to git \u2014 that's how everyone on the team ends up with the same resolved versions.</p> <p>Regenerated by either:</p> <ul> <li><code>toolr project deps sync</code> \u2014 full sync (re-resolve + apply).</li> <li><code>uv lock</code> directly inside the <code>tools/</code> directory \u2014 re-resolve   without touching the venv.</li> </ul>"},{"location":"project-config/#interaction-with-toolr-project-deps-sync","title":"Interaction with <code>toolr project deps sync</code>","text":"<p>The sync flow is:</p> <ol> <li>Discover or install uv.</li> <li>Resolve the venv location (<code>venv-location</code> + repo-key, or    <code>tools/.venv/</code>).</li> <li>Materialise / refresh the venv with <code>uv sync</code>.</li> <li>Apply each entry in <code>editable-install</code> via <code>uv pip install -e</code>.</li> <li>Verify the <code>toolr</code> Python package is present in the venv; refuse    to proceed otherwise.</li> </ol> <p><code>toolr project init</code> runs this same flow automatically after writing the scaffold (skippable with <code>--no-sync</code>).</p>"},{"location":"project-config/#validating-your-configuration","title":"Validating your configuration","text":"<p>A quick sanity check:</p> <pre><code>toolr project venv path\n</code></pre> <p>Prints the absolute path where toolr expects the venv to live. If the printed path doesn't match the directory you intend (cache vs in-tree), revisit <code>venv-location</code>.</p>"},{"location":"quickstart/","title":"Quickstart","text":"<p>The fastest path from zero to a running command. About two minutes end-to-end. For a full install matrix, see Installation.</p>"},{"location":"quickstart/#1-install-the-toolr-binary","title":"1. Install the toolr binary","text":"<p>On Linux or macOS:</p> <pre><code>curl -fsSL https://raw.githubusercontent.com/s0undt3ch/ToolR/main/installation/install.sh | sh\n</code></pre> <p>On Windows (PowerShell):</p> <pre><code>irm https://raw.githubusercontent.com/s0undt3ch/ToolR/main/installation/install.ps1 | iex\n</code></pre> <p>Verify:</p> <pre><code>toolr --version\n</code></pre>"},{"location":"quickstart/#2-scaffold-tools-in-your-repo","title":"2. Scaffold <code>tools/</code> in your repo","text":"<p>From your repo root:</p> <pre><code>toolr project init\n</code></pre> <p>This writes three files under <code>tools/</code>:</p> <ul> <li><code>pyproject.toml</code> \u2014 declares the venv's dependencies and project options.</li> <li><code>.gitignore</code> \u2014 ignores in-tree venvs.</li> <li><code>example.py</code> \u2014 four sample commands you can run, edit, or delete.</li> </ul> <p>It then runs <code>uv sync</code> to materialise the tools venv. The next command you run uses that venv automatically.</p>"},{"location":"quickstart/#3-run-your-first-command","title":"3. Run your first command","text":"<pre><code>toolr example hello --name you\n</code></pre> <pre><code>hello, world\n</code></pre> <p>Inspect the help to see what else the example file gives you:</p> <pre><code>toolr example --help\n</code></pre> <pre><code>Example commands\n\nUsage: toolr example [COMMAND]\n\nCommands:\n  hello    Greet someone.\n  commit   Print the short SHA of the current git HEAD.\n  confirm  Prompt for confirmation before doing something destructive.\n  setlog   Set a log level (for demonstration only \u2014 does nothing).\n\nOptions:\n  -h, --help  Print help\n</code></pre>"},{"location":"quickstart/#4-edit-toolsexamplepy-or-replace-it","title":"4. Edit <code>tools/example.py</code> (or replace it)","text":"<p>Open the generated file. The scaffold declares a group with <code>example = command_group(\"example\", ...)</code> and attaches commands by decorating functions with <code>@example.command</code>. Each decorated function becomes a CLI subcommand. The first argument is a <code>Context</code> object; the rest become CLI arguments inferred from your type hints and docstring.</p> <p>When your tools grow past a single file and you want commands in one file to attach to a group declared in another, switch to the string-keyed form: <code>@command(group=\"example\")</code>. See Scaling command groups across files.</p> <p>For the full authoring guide, head to Writing commands.</p>"},{"location":"quickstart/#optional-next-steps","title":"Optional next steps","text":"<ul> <li>Install shell tab completion: <code>toolr self completion install bash</code> (or <code>zsh</code> / <code>fish</code>).</li> <li>Learn the mental model: How toolr is laid out \u2014 one   page covering the binary, the tools venv, the manifest, and the cache.</li> <li>Ship your tools with a third-party PyPI package: see   Third-party packages.</li> </ul>"},{"location":"third-party/","title":"Third-party packages","text":"<p>Toolr supports third-party command packages \u2014 installable Python packages that contribute commands to the <code>toolr</code> CLI when present in the tools venv.</p> <p>There are two registration mechanisms, in order of preference:</p> <ol> <li>Static manifest fragment (recommended) \u2014 the package ships a    <code>toolr-manifest.json</code> next to its source. Toolr's binary discovers    it via glob during manifest build; no import required.</li> <li>Entry-point fallback (legacy) \u2014 the package declares    <code>[project.entry-points.\"toolr.tools\"]</code> in its <code>pyproject.toml</code>.    Toolr discovers it via <code>importlib.metadata</code> and imports the named    module to collect registrations.</li> </ol>"},{"location":"third-party/#why-ship-a-static-manifest","title":"Why ship a static manifest","text":"<p>Tab completion and <code>--help</code> need the command tree available before Python starts. With an entry-point registration, toolr has to spawn Python and import every entry-point's module to learn what's there \u2014 costly on every shell tab press.</p> <p>A <code>toolr-manifest.json</code> is a sub-millisecond <code>glob()</code> + JSON parse, no Python involved. For interactive use the difference is the boundary between \"instant\" and \"noticeable lag\".</p>"},{"location":"third-party/#the-toolr-manifestjson-fragment-format","title":"The <code>toolr-manifest.json</code> fragment format","text":"<p>A fragment is a JSON object that declares groups and commands. Toolr validates it against a schema version and merges it into the project's manifest at build time.</p> <p>Minimal shape:</p> <pre><code>{\n  \"toolr_schema_version\": 1,\n  \"groups\": [\n    {\n      \"name\": \"my-pkg\",\n      \"title\": \"My Package\",\n      \"description\": \"Commands contributed by my-pkg.\"\n    }\n  ],\n  \"commands\": [\n    {\n      \"name\": \"hello\",\n      \"group\": \"my-pkg\",\n      \"module\": \"my_pkg.commands\",\n      \"function\": \"hello\",\n      \"summary\": \"Say hello.\",\n      \"description\": \"\",\n      \"arguments\": [\n        {\n          \"name\": \"name\",\n          \"kind\": \"keyword\",\n          \"help\": \"Name to greet.\",\n          \"default\": \"world\",\n          \"type_annotation\": \"str\",\n          \"allowed_values\": []\n        }\n      ]\n    }\n  ]\n}\n</code></pre> <p>The file lives at <code>&lt;package_dir&gt;/toolr-manifest.json</code> \u2014 i.e. next to <code>my_pkg/__init__.py</code>. Toolr's manifest builder finds it via the glob <code>&lt;tools-venv&gt;/lib/python*/site-packages/*/toolr-manifest.json</code>.</p>"},{"location":"third-party/#the-python-build-helper","title":"The Python build helper","text":"<p>Most packages won't write the JSON by hand \u2014 they declare commands with the usual <code>command_group</code> / <code>@command</code> API and let <code>toolr.build</code> introspect.</p> <pre><code>from toolr.build import build_manifest\n\nresult = build_manifest(\"my_pkg\")\nprint(f\"Wrote {result.output_path}\")\n</code></pre> <p>See <code>toolr.build</code> in the API reference for the full signature.</p> <p>Or via the bundled CLI:</p> <pre><code>python -m toolr.build my_pkg\n</code></pre> <p>Re-run whenever your <code>command_group</code> / <code>@command</code> registrations change.</p>"},{"location":"third-party/#-check-for-ci","title":"<code>--check</code> for CI","text":"<p>In a CI job, pass <code>--check</code> to verify the committed manifest matches what regeneration would produce:</p> <pre><code>python -m toolr.build my_pkg --check\n</code></pre> <p>Exit code 0 if in sync, non-zero (with a diff on stderr) if drifted.</p>"},{"location":"third-party/#the-rust-cli-wrapper","title":"The rust CLI wrapper","text":"<p>If you're outside the package's own venv, the toolr binary will find a working Python and run the build for you:</p> <pre><code>toolr self build-manifest my_pkg --check\n</code></pre> <p>Same flags as <code>python -m toolr.build</code>. See CLI reference \u2192 <code>self build-manifest</code>.</p>"},{"location":"third-party/#entry-point-fallback-legacy","title":"Entry-point fallback (legacy)","text":"<p>For packages that haven't migrated to static manifests yet, toolr falls back to entry-points:</p> <pre><code># pyproject.toml in the third-party package\n[project.entry-points.\"toolr.tools\"]\ncommands = \"my_pkg.commands\"\n</code></pre> <p>When toolr's binary doesn't find a <code>toolr-manifest.json</code> next to the package, it spawns Python and imports <code>my_pkg.commands</code>. The module's import-time <code>command_group</code> / <code>@command</code> calls register the commands into the global registry.</p> <p>This works but is slower at completion time \u2014 every tab press pays the import cost. Once a package has any users, ship a static manifest instead.</p>"},{"location":"third-party/#working-example-in-the-repo","title":"Working example in the repo","text":"<p><code>tests/support/3rd-party-pkg/</code> in the toolr repo is a complete third-party package fixture. It uses the entry-point mechanism (older style) and is exercised by the integration tests; treat it as a copy-pasteable starting point.</p>"},{"location":"third-party/#command-resolution","title":"Command resolution","text":"<p>When multiple sources contribute commands with the same name:</p> <ul> <li>Project commands (defined in your <code>tools/</code>) always win \u2014 they   override anything from a third-party package.</li> <li>Group augmentation: if a third-party package targets an   existing group name, its commands are added to that group rather   than creating a duplicate.</li> <li>Between third-party packages: order is undefined \u2014 packages   that share group/command names will produce a manifest-build error,   pointing you to fix one of them.</li> </ul>"},{"location":"third-party/#distribution-checklist","title":"Distribution checklist","text":"<ul> <li>Include <code>toolr-manifest.json</code> in your package via   <code>package_data</code> (setuptools), <code>include</code> (hatch), or the equivalent   in your build backend. Verify it's in the built wheel before   publishing.</li> <li>Pin a compatible <code>toolr</code> version in your package's dependencies.</li> <li>Toolr migrates older fragment schemas in-process when a newer   binary meets an older fragment, but pin defensively if you're   shipping pre-1.0.</li> </ul>"},{"location":"installation/","title":"Installation","text":"<p><code>toolr</code> is distributed as two complementary PyPI packages that live in different venvs:</p> Package What it is How you use it <code>toolr</code> The Rust CLI binary \u2014 <code>toolr ...</code> on the shell. Install once, globally, on PATH (pip, install.sh, mise, release archive). <code>toolr-py</code> The Python runtime: <code>import toolr</code>, <code>Context</code>, <code>command_group</code>, <code>_rust_utils</code>. Add to your project's <code>tools/pyproject.toml</code> so it resolves into the tools venv. <p>A typical project setup uses both:</p> <ol> <li>Install the <code>toolr</code> CLI on PATH once (any method below).</li> <li>Add <code>toolr-py</code> to <code>tools/pyproject.toml</code> so <code>tools/*.py</code> scripts can    <code>from toolr import Context, command_group</code>.</li> </ol> <p>The CLI shells out into a per-project tools venv at execute time; it doesn't share its own venv with your tool scripts.</p>"},{"location":"installation/#requirements","title":"Requirements","text":"<ul> <li>Python 3.11 or later is needed at execute time. The toolr binary   itself is standalone, but every user command runs inside a Python   subprocess.</li> <li>uv is needed to materialise the   tools venv (<code>uv sync</code>). If you don't have it on PATH, toolr can   install a managed copy on first use.</li> </ul>"},{"location":"installation/#install-the-cli-binary-toolr","title":"Install the CLI binary (<code>toolr</code>)","text":"<p>Pick whichever method matches your environment.</p>"},{"location":"installation/#curl-sh-linux-macos","title":"<code>curl ... | sh</code> (Linux + macOS)","text":"<pre><code>curl -fsSL https://raw.githubusercontent.com/s0undt3ch/ToolR/main/installation/install.sh | sh\n</code></pre> <p>Pass <code>--version X.Y.Z</code> after <code>sh -s --</code> to pin a specific release, or <code>--prefix /custom/bin</code> to choose an install directory. Default prefix is <code>$XDG_BIN_HOME</code> (or <code>~/.local/bin</code>).</p>"},{"location":"installation/#powershell-windows","title":"PowerShell (Windows)","text":"<pre><code>irm https://raw.githubusercontent.com/s0undt3ch/ToolR/main/installation/install.ps1 | iex\n</code></pre>"},{"location":"installation/#mise","title":"mise","text":"<pre><code>mise plugin add toolr https://github.com/s0undt3ch/ToolR.git#installation/mise\nmise use --global toolr@latest\n</code></pre> <p>The plugin source ships in this repo under <code>installation/mise/</code>. See the dedicated mise page for <code>.mise.toml</code> / <code>.tool-versions</code> integration and task-runner examples.</p>"},{"location":"installation/#pip","title":"pip","text":"<pre><code>pip install toolr\n</code></pre> <p>This installs only the Rust CLI binary. The wheel has no Python source and no <code>import toolr</code> \u2014 that's what <code>toolr-py</code> is for (see below). <code>python -m toolr</code> was removed in the rust front-end rewrite; use the <code>toolr</code> executable instead.</p>"},{"location":"installation/#github-release-archives","title":"GitHub release archives","text":"<p>Download <code>toolr-&lt;version&gt;-&lt;target-triple&gt;.tar.gz</code> (or <code>.zip</code> for Windows) from https://github.com/s0undt3ch/ToolR/releases and extract it onto <code>$PATH</code> manually. Each archive ships with a <code>.sha256</code> sibling for verification.</p>"},{"location":"installation/#enable-import-toolr-in-your-tool-scripts-toolr-py","title":"Enable <code>import toolr</code> in your tool scripts (<code>toolr-py</code>)","text":"<p>The <code>toolr-py</code> wheel provides the Python runtime your <code>tools/*.py</code> scripts use \u2014 the <code>toolr</code> package (<code>Context</code>, <code>command_group</code>, helpers under <code>toolr.utils</code>) and the <code>_rust_utils</code> extension module the framework calls internally.</p> <p>Add it as a dependency of your project's tools venv:</p> tools/pyproject.toml<pre><code>[project]\nname = \"my-project-tools\"\nrequires-python = \"&gt;=3.11\"\ndependencies = [\n    \"toolr-py\",\n]\n</code></pre> <p><code>uv sync</code> (or whatever resolver you use for <code>tools/</code>) will pull it in. The <code>toolr</code> CLI on PATH will then find <code>import toolr</code> inside your tools venv when it executes commands.</p> <p>Why two packages?</p> <p>The CLI is a Rust binary built with <code>maturin --bindings bin</code>, which produces a Python-version-independent wheel that ships only the executable. The runtime users <code>import</code> is a separate pyo3-extension wheel built for each (CPython, ABI) pair. Splitting them lets the CLI be one global install while the runtime tracks each project's tools venv.</p>"},{"location":"installation/#supply-chain-verification-slsa-attestations","title":"Supply-chain verification (SLSA attestations)","text":"<p>Every release archive is signed with a SLSA build-provenance attestation produced by the GitHub-hosted release workflow. The <code>install.sh</code> and <code>install.ps1</code> scripts verify the attestation automatically when the <code>gh</code> CLI is on PATH. To require verification:</p> <pre><code>sh installation/install.sh --verify-attestation=require   # POSIX\n./installation/install.ps1 -VerifyAttestation require     # Windows\n</code></pre> <p>Or verify a downloaded archive manually:</p> <pre><code>gh attestation verify toolr-1.2.3-aarch64-apple-darwin.tar.gz \\\n  --repo s0undt3ch/ToolR\n</code></pre>"},{"location":"installation/#verifying-the-install","title":"Verifying the install","text":"<pre><code>toolr --version\n</code></pre> <pre><code>toolr --help\n</code></pre> <pre><code>In-project CLI tooling support\n\nUsage: toolr [OPTIONS] [COMMAND]\n\nCommands:\n  example  Example commands\n  project  Operations on the current repo's tools/ directory\n  self     Operations on toolr itself\n\nOptions:\n  -h, --help     Print help\n  -V, --version  Print version\n\nOutput Options:\n  -d, --debug\n          Increase verbosity (also enables `DEBUG` logging).\n  -q, --quiet\n          Suppress non-error output.\n      --timestamps\n          Prepend ISO-8601 timestamps to log lines.\n      --no-timestamps\n          Suppress log-line timestamps (default; overrides `--timestamps`).\n      --timeout-secs &lt;SECONDS&gt;\n          Default timeout applied to every `ctx.run(...)` subprocess (per-call `timeout_secs=` wins\n          when set).\n      --no-output-timeout-secs &lt;SECONDS&gt;\n          Default no-output watchdog applied to every `ctx.run(...)` subprocess \u2014 abort if no\n          stdout/stderr for this many seconds. Per-call `no_output_timeout_secs=` wins when set.\n</code></pre>"},{"location":"installation/#development-install","title":"Development install","text":"<pre><code>git clone https://github.com/s0undt3ch/ToolR.git\ncd ToolR\nuv sync --dev\ncargo build --release --bin toolr\n</code></pre> <p>The <code>target/release/toolr</code> binary is what you'll exercise during development; put <code>target/release/</code> on your PATH (or alias <code>toolr</code> to it) while iterating.</p>"},{"location":"installation/mise/","title":"mise","text":"<p>mise is a polyglot tool-version manager. The <code>toolr</code> repository ships an asdf-style plugin under <code>installation/mise/</code> that lets mise install and manage <code>toolr</code> binaries.</p>"},{"location":"installation/mise/#why","title":"Why","text":"<ul> <li>Version pinning per project. Different repos can use different   <code>toolr</code> versions without manual <code>PATH</code> juggling.</li> <li>Reproducibility for teams. Commit a <code>.mise.toml</code> or   <code>.tool-versions</code> and everyone on the project ends up on the same   <code>toolr</code> release.</li> <li>Multi-version side-by-side. Install several releases at once;   switch with <code>mise use toolr@X.Y.Z</code>.</li> <li>CI-tested. The plugin is exercised by   <code>install-smoke.yml</code>   on every release, so the version you install is the version we tested.</li> </ul>"},{"location":"installation/mise/#install-the-plugin","title":"Install the plugin","text":"<pre><code>mise plugin add toolr https://github.com/s0undt3ch/ToolR.git#installation/mise\n</code></pre> <p>The <code>#installation/mise</code> suffix tells mise to use the asdf-plugin layout inside the <code>installation/mise/</code> subdirectory of the toolr repo.</p>"},{"location":"installation/mise/#install-toolr","title":"Install toolr","text":"<pre><code># Install a specific version\nmise install toolr@0.11.0\n\n# Install whatever the latest GitHub release is\nmise install toolr@latest\n\n# Use a version globally\nmise use --global toolr@0.11.0\n\n# Pin a version to the current directory\nmise use toolr@0.11.0\n</code></pre> <p>Verify:</p> <pre><code>toolr --version\n</code></pre>"},{"location":"installation/mise/#project-configuration","title":"Project configuration","text":""},{"location":"installation/mise/#misetoml-recommended","title":"<code>.mise.toml</code> (recommended)","text":"<pre><code>[tools]\ntoolr = \"0.11.0\"\n</code></pre> <p>Then run <code>mise install</code> from the project root. mise resolves the version from <code>.mise.toml</code> and installs it on demand.</p>"},{"location":"installation/mise/#tool-versions-asdf-style-legacy","title":"<code>.tool-versions</code> (asdf-style, legacy)","text":"<pre><code>toolr 0.11.0\n</code></pre> <p>mise also reads asdf's <code>.tool-versions</code> files, so existing asdf users can keep their pin format unchanged.</p>"},{"location":"installation/mise/#combining-with-mise-tasks","title":"Combining with mise tasks","text":"<p><code>mise</code> can run repo-scoped tasks. Once <code>toolr</code> is on PATH via the plugin, wire it into tasks like any other binary:</p> <pre><code>[tools]\ntoolr = \"0.11.0\"\n\n[tasks.test]\ndescription = \"Run tests\"\nrun = \"toolr test run\"\n\n[tasks.lint]\ndescription = \"Run linters\"\nrun = \"toolr lint check\"\n\n[tasks.ci]\ndescription = \"Run CI checks\"\ndepends = [\"lint\", \"test\"]\n</code></pre> <p>Then:</p> <pre><code>mise run ci\n</code></pre>"},{"location":"installation/mise/#common-commands","title":"Common commands","text":"<pre><code># List all upstream versions\nmise list-all toolr\n\n# List locally installed versions\nmise list toolr\n\n# Show the active version in the current directory\nmise current toolr\n\n# Show the install dir for a version\nmise where toolr\n\n# Uninstall a version\nmise uninstall toolr@0.11.0\n</code></pre>"},{"location":"installation/mise/#troubleshooting","title":"Troubleshooting","text":""},{"location":"installation/mise/#toolr-command-not-found","title":"<code>toolr: command not found</code>","text":"<p>Make sure mise's shim/activate hook is wired into your shell:</p> <pre><code>eval \"$(mise activate bash)\"   # or zsh / fish\n</code></pre> <p>Or invoke through mise directly:</p> <pre><code>mise exec toolr -- toolr --help\n</code></pre>"},{"location":"installation/mise/#debug-an-install","title":"Debug an install","text":"<pre><code>MISE_DEBUG=1 mise install toolr@0.11.0\n</code></pre>"},{"location":"installation/mise/#reinstall","title":"Reinstall","text":"<pre><code>mise uninstall toolr@0.11.0\nmise install toolr@0.11.0\n</code></pre>"},{"location":"installation/mise/#plugin-internals","title":"Plugin internals","text":"<p>The plugin's source is in <code>installation/mise/</code> inside the toolr repo. The <code>bin/</code> directory contains the asdf-style hooks (<code>list-all</code>, <code>download</code>, <code>install</code>) that mise invokes:</p> <ol> <li><code>list-all</code> queries the GitHub releases API for available    <code>toolr</code> versions.</li> <li><code>download</code> fetches the archive for the host's target triple.</li> <li><code>install</code> extracts the binary into the mise-managed install    directory.</li> </ol> <p>The plugin installs the standalone <code>toolr</code> binary \u2014 the same artifact shipped by <code>installation/install.sh</code> and the GitHub release archives.</p>"},{"location":"internals/","title":"Internals","text":"<p>Reference material for contributors and curious users. None of this is required reading for writing commands \u2014 head to Writing commands if that's what you want.</p> <ul> <li>Manifest layers \u2014 static + dynamic, hashing, rebuild   semantics.</li> <li>Cache layout \u2014 <code>$XDG_CACHE_HOME/toolr/</code>, <code>meta.json</code>,   orphan/stale detection.</li> <li>Pre-commit integration \u2014 the shipped   <code>.pre-commit-hooks.yaml</code>, what hooks toolr provides.</li> <li>Diagnostics \u2014 pre-flight + post-mortem missing-deps,   <code>TOOLR_NO_PREFLIGHT_DEPS</code>.</li> </ul> <p>For the original design specs see <code>specs/rust-front-end/</code> in the repo.</p>"},{"location":"internals/cache/","title":"Cache layout","text":"<p>Cached per-repo virtualenvs live at <code>$XDG_CACHE_HOME/toolr/</code> (or <code>~/.cache/toolr/</code> on platforms without <code>XDG_CACHE_HOME</code>).</p>"},{"location":"internals/cache/#layout","title":"Layout","text":"<pre><code>$XDG_CACHE_HOME/toolr/\n\u251c\u2500\u2500 &lt;repo-key-1&gt;/\n\u2502   \u251c\u2500\u2500 meta.json\n\u2502   \u2514\u2500\u2500 venv/\n\u251c\u2500\u2500 &lt;repo-key-2&gt;/\n\u2502   \u251c\u2500\u2500 meta.json\n\u2502   \u2514\u2500\u2500 venv/\n\u2514\u2500\u2500 bin/\n    \u2514\u2500\u2500 uv          # toolr's managed copy of uv (optional)\n</code></pre> <p><code>&lt;repo-key&gt;</code> is a stable, content-addressable identifier derived from the repo's canonical path and Python version. Multiple worktrees of the same repo share a single cache entry.</p>"},{"location":"internals/cache/#metajson","title":"<code>meta.json</code>","text":"<p>Written when toolr creates a venv; updated on every invocation.</p> <pre><code>{\n  \"repo_path\": \"/Users/you/code/my-repo\",\n  \"toolr_version\": \"0.11.0\",\n  \"python_version\": \"3.11.11\",\n  \"created_at\": \"2026-04-02T14:23:11Z\",\n  \"last_used_at\": \"2026-05-12T09:18:42Z\"\n}\n</code></pre> <ul> <li><code>repo_path</code> \u2014 the absolute path of the repo this venv was   built for. Used to detect orphans.</li> <li><code>toolr_version</code> / <code>python_version</code> \u2014 recorded for debugging.</li> <li><code>created_at</code> \u2014 set once when the venv is first materialised.</li> <li><code>last_used_at</code> \u2014 touched on every toolr invocation that hits   this venv. Used to detect stale entries.</li> </ul>"},{"location":"internals/cache/#orphan-detection","title":"Orphan detection","text":"<p>A cache entry is orphan when <code>meta.repo_path</code> no longer exists on disk \u2014 typically because the repo was moved, renamed, or deleted. <code>toolr self cache prune</code> lists orphans and (with confirmation) removes them.</p>"},{"location":"internals/cache/#stale-detection","title":"Stale detection","text":"<p>A cache entry is stale when <code>last_used_at</code> is older than the configured threshold (default 30 days). Override with <code>--stale-after-days N</code> on <code>toolr self cache prune</code>.</p>"},{"location":"internals/cache/#pruning","title":"Pruning","text":"<pre><code>toolr self cache list                       # show every entry\ntoolr self cache prune                      # remove orphans + stale\ntoolr self cache prune --dry-run            # show what would be removed\ntoolr self cache prune --all --yes          # nuke everything\ntoolr self cache prune --stale-after-days 7 # override threshold\n</code></pre> <p>See CLI reference \u2192 <code>self cache prune</code> for the full flag matrix.</p>"},{"location":"internals/cache/#passive-size-hint","title":"Passive size hint","text":"<p>When the cache exceeds a soft threshold (or accumulates orphans), toolr prints a one-line hint to stderr on the next invocation:</p> <pre><code>toolr: cache is 1.4 GB across 18 entries (6 orphan); run `toolr self cache prune` to clean up.\n</code></pre> <p>Suppress with <code>TOOLR_NO_CACHE_HINT=1</code> in your environment.</p> <p>The hint logic is conservative \u2014 it only prints once per (cache size, orphan count) bucket, so you won't get nagged on every invocation.</p>"},{"location":"internals/diagnostics/","title":"Diagnostics","text":"<p>When a command fails because of missing dependencies, toolr aims for a one-line diagnostic that names the missing package and the fix \u2014 not a 50-line Python traceback at the user.</p>"},{"location":"internals/diagnostics/#pre-flight-check","title":"Pre-flight check","text":"<p>Before spawning the Python subprocess that runs your command, the binary inspects the target command's recorded top-level imports (captured by the static parser) and probes the resolved tools venv for each:</p> <pre><code>&lt;venv&gt;/lib/python*/site-packages/&lt;module&gt;/__init__.py\n&lt;venv&gt;/lib/python*/site-packages/&lt;module&gt;.py\n</code></pre> <p>If any required import is missing, toolr aborts before invoking Python and prints:</p> <pre><code>toolr: pre-flight check failed: `requests` is not installed in the tools venv.\ntoolr:   command: my-group fetch\ntoolr:   venv:    /Users/you/.cache/toolr/&lt;repo-key&gt;/venv\ntoolr:   fix:     toolr project deps sync\ntoolr: (exit 78)\n</code></pre> <p>Exit code 78 distinguishes pre-flight failures from runtime errors (per the <code>sysexits.h</code> <code>EX_CONFIG</code> convention). CI can branch on this code to surface configuration vs application failures.</p>"},{"location":"internals/diagnostics/#post-mortem-interception","title":"Post-mortem interception","text":"<p>For imports that the static parser couldn't see (deferred imports inside function bodies, conditional imports, etc.), toolr also intercepts <code>ImportError</code> from the Python subprocess. When detected, it appends the same <code>toolr project deps sync</code> suggestion to the captured traceback before re-emitting it:</p> <pre><code>ModuleNotFoundError: No module named 'foo'\n\ntoolr: hint: the import above failed. If `foo` should be in the tools\ntoolr:       venv, run `toolr project deps sync`.\n</code></pre> <p>The original traceback is preserved \u2014 toolr only adds a hint underneath.</p>"},{"location":"internals/diagnostics/#disabling-pre-flight","title":"Disabling pre-flight","text":"<p>The pre-flight check can produce false positives in edge cases \u2014 PEP 420 namespace packages without <code>__init__.py</code>, packages that ship a single <code>*.pth</code> file, etc. Set:</p> <pre><code>export TOOLR_NO_PREFLIGHT_DEPS=1\n</code></pre> <p>\u2026to skip pre-flight entirely. Post-mortem interception still fires, so missing imports are still reported with a fix hint.</p> <p>Accepted truthy values: any non-empty string except <code>0</code>.</p>"},{"location":"internals/diagnostics/#why-not-just-rely-on-python-tracebacks","title":"Why not just rely on Python tracebacks?","text":"<p>Two reasons:</p> <ol> <li>Speed. Pre-flight is a few <code>stat()</code> calls; spawning Python    to discover a missing module costs hundreds of ms.</li> <li>Signal-to-noise. A Python traceback for \"module not found\"    buries the actual fix in 20 lines of stack frames. Toolr's    diagnostic puts the fix on line two.</li> </ol>"},{"location":"internals/manifest/","title":"Manifest layers","text":"<p><code>tools/.toolr-manifest.json</code> is the cached structure of every group and command the toolr binary knows about. Tab completion, <code>--help</code>, and clap's subcommand-tree build all read from this single file \u2014 no Python imports involved in the hot path.</p> <p>It's a cache \u2014 don't commit it</p> <p>The manifest is regenerated automatically when the static or dynamic hashes drift, and the static layer alone rebuilds in ~10 ms on typical projects. <code>toolr project init</code> adds the file to <code>tools/.gitignore</code> for you. Committing it just produces noisy merge conflicts on every <code>tools/</code> edit without buying anything the auto-rebuild doesn't already cover.</p>"},{"location":"internals/manifest/#file-shape","title":"File shape","text":"<pre><code>{\n  \"schema_version\": 1,\n  \"static_hash\": \"&lt;blake3 hex&gt;\",\n  \"dynamic_hash\": \"&lt;blake3 hex&gt;\",\n  \"groups\": [...],\n  \"commands\": [...]\n}\n</code></pre> <ul> <li><code>schema_version</code> \u2014 single integer; toolr refuses to load a   manifest with a higher schema than it understands.</li> <li><code>static_hash</code> \u2014 blake3 over the sorted <code>(path, contents)</code> of   every <code>tools/**/*.py</code> file. Drives static-layer rebuilds.</li> <li><code>dynamic_hash</code> \u2014 blake3 over the tools venv's installed   package set. Drives dynamic-layer rebuilds.</li> <li><code>groups</code> / <code>commands</code> \u2014 the actual command tree. Each   entry carries an <code>origin</code> field (<code>\"static\"</code> or <code>\"dynamic\"</code>)   recording which layer produced it.</li> </ul>"},{"location":"internals/manifest/#group-schema","title":"Group schema","text":"<pre><code>{\n  \"name\": \"image\",\n  \"title\": \"Image\",\n  \"description\": \"Image subcommands\",\n  \"parent\": \"docker\",\n  \"origin\": \"static\"\n}\n</code></pre> <ul> <li><code>name</code> \u2014 the leaf name (not the full path). For nested groups   this is just the segment (<code>\"image\"</code>); the full hierarchy is the   parent path joined to the name.</li> <li><code>parent</code> \u2014 <code>null</code> for top-level groups, otherwise the dotted   <code>full_path</code> of the parent group (<code>\"docker\"</code> for <code>docker image</code>,   <code>\"docker.image\"</code> for <code>docker image build</code>). The clap CLI builder   walks this tree recursively when materialising subcommands.</li> </ul>"},{"location":"internals/manifest/#command-schema","title":"Command schema","text":"<pre><code>{\n  \"name\": \"build\",\n  \"group\": \"docker.image\",\n  \"module\": \"tools.docker\",\n  \"function\": \"build\",\n  \"summary\": \"Build a docker image.\",\n  \"description\": \"\",\n  \"arguments\": [...],\n  \"imports\": [],\n  \"origin\": \"static\"\n}\n</code></pre> <ul> <li><code>group</code> \u2014 the full path of the owning group. Top-level   commands have a single segment (<code>\"ci\"</code>); nested commands have the   dotted parent path (<code>\"docker.image\"</code>).</li> </ul>"},{"location":"internals/manifest/#argument-schema","title":"Argument schema","text":"<pre><code>{\n  \"name\": \"tag\",\n  \"kind\": \"positional\",\n  \"help\": \"Tag to assign.\",\n  \"default\": null,\n  \"type_annotation\": \"str\",\n  \"resolved_type\": { \"kind\": \"str\" },\n  \"path_constraints\": null,\n  \"allowed_values\": []\n}\n</code></pre> <ul> <li><code>kind</code> \u2014 one of <code>\"positional\"</code>, <code>\"optional\"</code>, <code>\"flag\"</code>,   <code>\"repeated\"</code> (list[T] keyword), or <code>\"var_positional\"</code> (<code>*args</code>).</li> <li><code>default</code> \u2014 string-encoded default value (or <code>null</code>). Numeric   defaults render as e.g. <code>\"5\"</code>; enum-attribute defaults like   <code>Operation.ADD</code> are resolved to the serialised member value   (<code>\"add\"</code>).</li> <li><code>type_annotation</code> \u2014 textual rendering of the annotation as   written in source (<code>\"int\"</code>, <code>\"list[str]\"</code>, <code>\"pathlib.Path\"</code>). Used   for <code>--help</code> and diagnostics.</li> <li><code>resolved_type</code> \u2014 structured form of the annotation, drives   the clap value_parser layer. Shape:</li> </ul> <pre><code>{ \"kind\": \"int\" }\n{ \"kind\": \"list\", \"value\": { \"kind\": \"int\" } }\n{ \"kind\": \"tuple\", \"value\": [{ \"kind\": \"str\" }, { \"kind\": \"int\" }] }\n{ \"kind\": \"literal\", \"value\": [\"a\", \"b\"] }\n{ \"kind\": \"enum\", \"value\": { \"name\": \"Operation\", \"values\": [\"add\", \"subtract\"] } }\n{ \"kind\": \"optional\", \"value\": { \"kind\": \"path\" } }\n</code></pre> <p>See <code>src/parser/types.rs</code> for the full enum. <code>null</code> means the type   layer couldn't resolve a supported type \u2014 third-party fragments   built against the legacy schema fall through to string semantics. - <code>path_constraints</code> \u2014 for path-typed parameters annotated with   <code>arg(must_exist=True, ...)</code>. Object with optional <code>must_exist</code>,   <code>must_be_file</code>, <code>must_be_dir</code> booleans. <code>null</code> when no constraints   were declared. - <code>allowed_values</code> \u2014 for <code>Literal[...]</code> / <code>Enum</code> types, the   values clap validates against. Also used by tab completion.</p>"},{"location":"internals/manifest/#static-layer","title":"Static layer","text":"<p>Built from <code>tools/**/*.py</code> via the <code>ruff_python_parser</code> Rust crate. Pure AST traversal \u2014 never imports user code, so it's safe to run without a working venv. Captures:</p> <ul> <li><code>command_group(...)</code> declarations (including the deprecated   binding-style <code>var = command_group(...)</code>).</li> <li><code>@command(group=\"\u2026\")</code> decorations (and the deprecated   <code>@&lt;binding&gt;.command</code> decorator).</li> <li>Function signatures (positional vs keyword, defaults, annotations).</li> <li>Google-style docstrings (summary, description, <code>Args:</code> block).</li> <li>Local <code>Literal[...]</code> and <code>enum.Enum</code> definitions (resolved across   files via a symbol table).</li> </ul> <p>The static layer is rebuilt when toolr detects <code>static_hash</code> drift against the on-disk files.</p>"},{"location":"internals/manifest/#dynamic-layer","title":"Dynamic layer","text":"<p>Built by spawning <code>python -m toolr._introspect --tools-root &lt;tools&gt;</code> inside the resolved tools venv. The helper:</p> <ol> <li>Inserts <code>&lt;tools&gt;/..</code> on <code>sys.path</code> so <code>import tools</code> works.</li> <li>Imports every <code>tools.*</code> module \u2014 registering every    <code>command_group</code> / <code>@command</code> call.</li> <li>Walks <code>importlib.metadata.entry_points(group=\"toolr.tools\")</code> for    third-party packages without a static manifest fragment.</li> <li>Dumps a JSON payload to stdout describing the merged registry.</li> </ol> <p>The dynamic layer fills in things the static parser can't see: cross- package re-exports, runtime-generated commands, and third-party packages that haven't shipped a static manifest fragment.</p> <p>Toolr regenerates the dynamic layer when:</p> <ul> <li>The venv contents change (<code>dynamic_hash</code> drifts) \u2014 typically after   <code>toolr project deps sync</code>.</li> <li>A command is invoked and the binary detects drift on entry.</li> </ul>"},{"location":"internals/manifest/#manual-rebuild","title":"Manual rebuild","text":"<pre><code>toolr project manifest rebuild\n</code></pre> <p>Runs both layers and writes the merged result. Used by the shipped pre-commit hook (see Pre-commit integration) and available for explicit invocation when you want to be sure the manifest is current \u2014 for example before publishing a release that includes new commands.</p>"},{"location":"internals/manifest/#hashing-details","title":"Hashing details","text":"<p>Both hashes use blake3, written as lowercase hex.</p> <ul> <li><code>static_hash</code> input: every file under <code>tools/</code> (excluding   <code>__pycache__</code>, <code>.toolr-manifest.json</code>, and dot-prefixed names),   sorted by path, each entry hashed as   <code>len(path_bytes) || path_bytes || len(contents) || contents</code>.</li> <li><code>dynamic_hash</code> input: sorted listing of <code>&lt;venv&gt;/lib/python*/site-   packages/*</code> entries, each entry's name + metadata file mtime   rounded to the nearest second.</li> </ul> <p>This gives stable, content-addressable rebuild decisions across machines and across CI / dev environments.</p>"},{"location":"internals/pre-commit/","title":"Pre-commit integration","text":"<p>Toolr no longer ships a <code>.pre-commit-hooks.yaml</code>. The only hook it used to provide \u2014 <code>toolr-manifest</code>, which rebuilt the manifest on <code>tools/*.py</code> changes \u2014 only made sense when the manifest was tracked in git. It isn't, anymore: <code>tools/.toolr-manifest.json</code> is a pure cache that the binary auto-regenerates on hash drift, and toolr's <code>project init</code> adds it to <code>tools/.gitignore</code> by default.</p> <p>If your project chooses to commit the manifest anyway, wire your own local hook:</p> <pre><code># .pre-commit-config.yaml\n- repo: local\n  hooks:\n    - id: toolr-manifest\n      name: Regenerate toolr manifest\n      entry: toolr project manifest rebuild\n      language: system\n      pass_filenames: false\n      files: ^tools/.*\\.py$\n</code></pre> <p>That's the same definition toolr used to ship; it works identically when invoked locally.</p>"},{"location":"internals/pre-commit/#why-the-change","title":"Why the change","text":"<ul> <li>The static manifest builds in ~10 ms on most projects; tab   completion stays sub-50 ms regardless of staleness.</li> <li>The committed file caused merge conflicts on every PR that touched   <code>tools/</code>, and the diffs weren't human-meaningful.</li> <li>The binary's hash-drift detector already keeps the on-disk file   current when it is present.</li> </ul> <p>See Internals \u2192 Manifest layers for the regeneration flow.</p>"},{"location":"reference/","title":"API reference","text":"<p>The user-facing Python API. For internal-only modules see the source under <code>crates/toolr-py/python/toolr/</code>.</p> <ul> <li><code>Context</code> \u2014 passed to every command function as <code>ctx</code>.</li> <li><code>command_group</code> \u2014 declared at module scope in   <code>tools/*.py</code> files.</li> <li><code>command</code> \u2014 string-path decorator for attaching   commands to a group without holding its binding.</li> <li><code>arg</code> \u2014 annotation for advanced argument options.</li> <li><code>testing</code> \u2014 helpers for testing your commands.</li> <li><code>build</code> \u2014 emit a <code>toolr-manifest.json</code> for a   third-party package.</li> </ul>"},{"location":"reference/arg/","title":"arg","text":"<p>Create an :class:<code>ArgumentAnnotation</code> for use with <code>typing.Annotated</code>.</p> <p>Parameters:</p> Name Type Description Default <code>aliases</code> <code>list[str] | None</code> <p>Extra short or long flag spellings (e.g. <code>[\"-n\", \"--who\"]</code>). Single-character entries become clap shorts; longer entries become aliases.</p> <code>None</code> <code>metavar</code> <code>str | None</code> <p>Custom placeholder shown in <code>--help</code> (e.g. <code>\"PATH\"</code> \u2192 <code>--config &lt;PATH&gt;</code>).</p> <code>None</code> <code>env</code> <code>str | None</code> <p>Read the default from this environment variable when the flag isn't passed. Maps to clap <code>Arg::env(...)</code>.</p> <code>None</code> <code>hide</code> <code>bool</code> <p>When <code>True</code>, omit the argument from <code>--help</code> output. Still parseable on the command line.</p> <code>False</code> <code>help_section</code> <code>ArgSection | None</code> <p>An :class:<code>ArgSection</code> returned by :func:<code>arg_section</code>. Groups related arguments under a named heading in <code>--help</code>.</p> <code>None</code> <code>display_order</code> <code>int | None</code> <p>Integer ordering hint for <code>--help</code> rendering. Lower values render first; arguments without a value fall back to source order.</p> <code>None</code> <code>conflicts_with</code> <code>list[str] | None</code> <p>Names of other parameters that may not be used together with this one.</p> <code>None</code> <code>requires</code> <code>list[str] | None</code> <p>Names of other parameters that must also be set when this one is.</p> <code>None</code> <code>must_exist</code> <code>bool</code> <p>For path-typed params: reject paths that don't exist on disk. Useful for \"input file\" style arguments.</p> <code>False</code> <code>must_be_file</code> <code>bool</code> <p>For path-typed params: also require the path is a regular file. Implies <code>must_exist=True</code>.</p> <code>False</code> <code>must_be_dir</code> <code>bool</code> <p>For path-typed params: also require the path is a directory. Implies <code>must_exist=True</code>.</p> <code>False</code> <code>required</code> <code>bool | None</code> <p>Deprecated. Removed in 1.0. Use <code>T | None</code> or <code>*args: T</code> to express optional / zero-or-more.</p> <code>None</code> <code>action</code> <code>str | None</code> <p>Deprecated. Removed in 1.0. <code>bool</code> defaults imply flag actions; <code>list[T]</code> implies append; <code>Count</code> implies counting.</p> <code>None</code> <code>choices</code> <code>list[Any] | None</code> <p>Deprecated. Removed in 1.0. Use <code>Literal[\"a\",\"b\"]</code> or an :class:<code>enum.Enum</code> subclass instead.</p> <code>None</code> <code>nargs</code> <code>NargsType | None</code> <p>Deprecated. Removed in 1.0. <code>T | None</code>, <code>*args: T</code>, and <code>tuple[T1, T2]</code> cover the cases.</p> <code>None</code> <code>group</code> <code>str | None</code> <p>Deprecated. Removed in 1.0. Use <code>conflicts_with=[...]</code> for mutex relationships and <code>help_section=</code> for display grouping.</p> <code>None</code> Source code in <code>crates/toolr-py/python/toolr/utils/_signature.py</code> <pre><code>def arg(  # noqa: PLR0913 \u2014 kwargs surface mirrors the clap features we expose; each is intentionally distinct.\n    *,\n    # Active kwargs (plumbed through to clap).\n    aliases: list[str] | None = None,\n    metavar: str | None = None,\n    env: str | None = None,\n    hide: bool = False,\n    help_section: ArgSection | None = None,\n    display_order: int | None = None,\n    conflicts_with: list[str] | None = None,\n    requires: list[str] | None = None,\n    must_exist: bool = False,\n    must_be_file: bool = False,\n    must_be_dir: bool = False,\n    # Deprecated legacy kwargs. Each emits a `ToolrDeprecationWarning`\n    # and (when applicable) maps onto the new field internally.\n    required: bool | None = None,\n    action: str | None = None,\n    choices: list[Any] | None = None,\n    nargs: NargsType | None = None,\n    group: str | None = None,\n) -&gt; ArgumentAnnotation:\n    \"\"\"Create an :class:`ArgumentAnnotation` for use with ``typing.Annotated``.\n\n    Args:\n        aliases: Extra short or long flag spellings (e.g. ``[\"-n\", \"--who\"]``).\n            Single-character entries become clap shorts; longer entries\n            become aliases.\n        metavar: Custom placeholder shown in ``--help``\n            (e.g. ``\"PATH\"`` \u2192 ``--config &lt;PATH&gt;``).\n        env: Read the default from this environment variable when the\n            flag isn't passed. Maps to clap ``Arg::env(...)``.\n        hide: When ``True``, omit the argument from ``--help`` output.\n            Still parseable on the command line.\n        help_section: An :class:`ArgSection` returned by\n            :func:`arg_section`. Groups related arguments under a\n            named heading in ``--help``.\n        display_order: Integer ordering hint for ``--help`` rendering.\n            Lower values render first; arguments without a value fall\n            back to source order.\n        conflicts_with: Names of other parameters that may not be used\n            together with this one.\n        requires: Names of other parameters that must also be set when\n            this one is.\n        must_exist: For path-typed params: reject paths that don't\n            exist on disk. Useful for \"input file\" style arguments.\n        must_be_file: For path-typed params: also require the path\n            is a regular file. Implies ``must_exist=True``.\n        must_be_dir: For path-typed params: also require the path\n            is a directory. Implies ``must_exist=True``.\n        required: **Deprecated.** Removed in 1.0. Use ``T | None`` or\n            ``*args: T`` to express optional / zero-or-more.\n        action: **Deprecated.** Removed in 1.0. ``bool`` defaults imply\n            flag actions; ``list[T]`` implies append; ``Count`` implies\n            counting.\n        choices: **Deprecated.** Removed in 1.0. Use ``Literal[\"a\",\"b\"]``\n            or an :class:`enum.Enum` subclass instead.\n        nargs: **Deprecated.** Removed in 1.0. ``T | None``,\n            ``*args: T``, and ``tuple[T1, T2]`` cover the cases.\n        group: **Deprecated.** Removed in 1.0. Use\n            ``conflicts_with=[...]`` for mutex relationships and\n            ``help_section=`` for display grouping.\n    \"\"\"\n    if required is not None:\n        _deprecated(\n            \"required\",\n            replacement=(\n                \"Use `T | None` (with a default of None) for optional args, or `*args: T` for zero-or-more positionals.\"\n            ),\n        )\n    if action is not None:\n        _deprecated(\n            \"action\",\n            replacement=(\n                \"Action is inferred from the parameter type: `bool` \u2192 flag, \"\n                \"`list[T]` \u2192 append, `toolr.types.Count` \u2192 count.\"\n            ),\n        )\n    if choices is not None:\n        _deprecated(\n            \"choices\",\n            replacement=(\n                \"Use `Literal['a', 'b']` or an `enum.Enum` subclass instead \u2014 \"\n                \"the choices come from the type annotation.\"\n            ),\n        )\n    if nargs is not None:\n        _deprecated(\n            \"nargs\",\n            replacement=(\n                \"Use `T | None` (zero-or-one), `*args: T` (zero-or-more), \"\n                \"or `tuple[T1, T2, ...]` (fixed arity) \u2014 all driven by the type.\"\n            ),\n        )\n    if group is not None:\n        _deprecated(\n            \"group\",\n            replacement=(\n                \"Use `conflicts_with=[...]` for mutually-exclusive arguments \"\n                \"or `help_section=` (via `arg_section(...)`) for display grouping.\"\n            ),\n        )\n    return ArgumentAnnotation(\n        aliases=aliases,\n        metavar=metavar,\n        env=env,\n        hide=hide,\n        help_section=help_section,\n        display_order=display_order,\n        conflicts_with=conflicts_with,\n        requires=requires,\n        must_exist=must_exist,\n        must_be_file=must_be_file,\n        must_be_dir=must_be_dir,\n        required=required,\n        action=action,\n        choices=choices,\n        nargs=nargs,\n        group=group,\n    )\n</code></pre>"},{"location":"reference/arg_section/","title":"arg_section","text":"<p>Construct an :class:<code>ArgSection</code> for use as <code>arg(help_section=...)</code>.</p> <p>Parameters:</p> Name Type Description Default <code>title</code> <code>str</code> <p>Short heading shown in <code>--help</code> above the section's arguments. Renders as a clap help-heading.</p> required <code>description</code> <code>str | None</code> <p>Optional one-line prose displayed under the heading. Markdown is supported via the same termimad renderer toolr uses elsewhere.</p> <code>None</code> <p>Returns:</p> Type Description <code>ArgSection</code> <p>An <code>ArgSection</code> instance \u2014 store it at module scope and pass</p> <code>ArgSection</code> <p>it (by reference, not by re-instantiating) to every member</p> <code>ArgSection</code> <p>argument.</p> Source code in <code>crates/toolr-py/python/toolr/utils/_signature.py</code> <pre><code>def arg_section(title: str, *, description: str | None = None) -&gt; ArgSection:\n    \"\"\"Construct an :class:`ArgSection` for use as ``arg(help_section=...)``.\n\n    Args:\n        title: Short heading shown in ``--help`` above the section's\n            arguments. Renders as a clap help-heading.\n        description: Optional one-line prose displayed under the\n            heading. Markdown is supported via the same termimad\n            renderer toolr uses elsewhere.\n\n    Returns:\n        An ``ArgSection`` instance \u2014 store it at module scope and pass\n        it (by reference, not by re-instantiating) to every member\n        argument.\n    \"\"\"\n    return ArgSection(title=title, description=description)\n</code></pre>"},{"location":"reference/build/","title":"build","text":"<p>Build a toolr manifest fragment for a third-party package.</p> <p>Walks the package's <code>command_group</code> / <code>@group.command</code> registry to produce a static <code>toolr-manifest.json</code> that the Rust binary can discover and merge without any further Python introspection.</p>"},{"location":"reference/build/#toolr.build.BuildResult","title":"BuildResult  <code>dataclass</code>","text":"<pre><code>BuildResult(\n    fragment: dict[str, Any],\n    output_path: Path,\n    drift: bool = False,\n)\n</code></pre> <p>Result of <code>build_manifest</code>.</p>"},{"location":"reference/build/#toolr.build.BuildResult.drift","title":"drift  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>drift: bool = False\n</code></pre> <p>True only when <code>check=True</code> and the regenerated fragment differs from the file currently on disk.</p>"},{"location":"reference/build/#toolr.build.BuildManifestError","title":"BuildManifestError","text":"<p>               Bases: <code>Exception</code></p> <p>Raised when the manifest cannot be built (no commands, etc.).</p>"},{"location":"reference/build/#toolr.build.build_manifest","title":"build_manifest","text":"<pre><code>build_manifest(\n    package_name: str,\n    *,\n    output_path: Path | None = None,\n    schema_version: int | None = None,\n    check: bool = False,\n) -&gt; BuildResult\n</code></pre> <p>Generate a manifest fragment for <code>package_name</code>.</p> <p>Parameters:</p> Name Type Description Default <code>package_name</code> <code>str</code> <p>Dotted name of the package to introspect.</p> required <code>output_path</code> <code>Path | None</code> <p>Where to write <code>toolr-manifest.json</code>. Defaults to <code>&lt;package_dir&gt;/toolr-manifest.json</code>.</p> <code>None</code> <code>schema_version</code> <code>int | None</code> <p>Override the schema version written out.</p> <code>None</code> <code>check</code> <code>bool</code> <p>If True, do not write the file; instead, compare the generated fragment against the file currently at <code>output_path</code>. Sets <code>BuildResult.drift=True</code> on mismatch.</p> <code>False</code> <p>Raises:</p> Type Description <code>ModuleNotFoundError</code> <p><code>package_name</code> is not importable.</p> <code>BuildManifestError</code> <p>The package has no toolr commands.</p> Source code in <code>crates/toolr-py/python/toolr/build.py</code> <pre><code>def build_manifest(\n    package_name: str,\n    *,\n    output_path: Path | None = None,\n    schema_version: int | None = None,\n    check: bool = False,\n) -&gt; BuildResult:\n    \"\"\"Generate a manifest fragment for `package_name`.\n\n    Args:\n        package_name: Dotted name of the package to introspect.\n        output_path: Where to write `toolr-manifest.json`. Defaults to\n            ``&lt;package_dir&gt;/toolr-manifest.json``.\n        schema_version: Override the schema version written out.\n        check: If True, do not write the file; instead, compare the\n            generated fragment against the file currently at\n            `output_path`. Sets ``BuildResult.drift=True`` on mismatch.\n\n    Raises:\n        ModuleNotFoundError: `package_name` is not importable.\n        BuildManifestError: The package has no toolr commands.\n    \"\"\"\n    module = importlib.import_module(package_name)\n    package_root = _resolve_package_root(module, package_name)\n    if output_path is None:\n        output_path = package_root / \"toolr-manifest.json\"\n    version = schema_version if schema_version is not None else MANIFEST_SCHEMA_VERSION\n\n    fragment = _collect_fragment(package_name, version)\n    if not fragment[\"groups\"] and not fragment[\"commands\"]:\n        err_msg = f\"package `{package_name}` declares no toolr commands - nothing to write\"\n        raise BuildManifestError(err_msg)\n    _validate_fragment(fragment)\n\n    serialized = json.dumps(fragment, indent=2, sort_keys=True) + \"\\n\"\n\n    if check:\n        existing = output_path.read_text() if output_path.is_file() else \"\"\n        drift = existing != serialized\n        return BuildResult(fragment=fragment, output_path=output_path, drift=drift)\n\n    output_path.write_text(serialized)\n    return BuildResult(fragment=fragment, output_path=output_path)\n</code></pre>"},{"location":"reference/command/","title":"command","text":"<p>Register a function as a toolr CLI command.</p> <p>String-path attachment to a group, used as an alternative to the legacy <code>@&lt;binding&gt;.command</code> decorator. Lets you declare commands in any file without importing a shared <code>CommandGroup</code> binding \u2014 the <code>group=</code> string is the only contract.</p> <p>Usage::</p> <pre><code>from toolr import command, command_group\n\ncommand_group(\"ci.helm-diff-pr-comment\", docstring=__doc__)\n\n@command(group=\"ci.helm-diff-pr-comment\")\ndef backend(ctx, env): ...\n\n# Optional explicit command name (otherwise the function\n# name is hyphenated and used).\n@command(\"snippet-checker\", group=\"ci.helm-diff-pr-comment\")\ndef check_snippets(ctx): ...\n</code></pre> <p>Parameters:</p> Name Type Description Default <code>name_or_func</code> <code>str | Callable[..., Any] | None</code> <p>When called as a bare decorator (<code>@command</code>), this is the wrapped function. When called with parentheses (<code>@command(\"name\", group=...)</code>), this is the override for the command's CLI name; the function name (hyphenated) is used otherwise.</p> <code>None</code> <code>group</code> <code>str | None</code> <p>Dotted full path of the target group (e.g. <code>\"ci\"</code> or <code>\"ci.helm-diff-pr-comment\"</code>). A matching <code>command_group(...)</code> declaration must exist elsewhere in <code>tools/</code>; otherwise manifest-build fails with a clear error.</p> <code>None</code> <code>aliases</code> <code>list[str] | None</code> <p>Reserved for future use; currently no-op (tracked with the rest of the <code>arg(aliases=...)</code> plumbing in issue #198).</p> <code>None</code> <p>Returns:</p> Type Description <code>Callable[..., Any]</code> <p>The decorated function unchanged; the decorator's only job is</p> <code>Callable[..., Any]</code> <p>to record metadata that the static parser picks up. At runtime</p> <code>Callable[..., Any]</code> <p>toolr calls the function directly with the parsed args.</p> Source code in <code>crates/toolr-py/python/toolr/_decorators.py</code> <pre><code>def command(\n    name_or_func: str | Callable[..., Any] | None = None,\n    *,\n    group: str | None = None,\n    aliases: list[str] | None = None,\n) -&gt; Callable[..., Any]:\n    \"\"\"Register a function as a toolr CLI command.\n\n    String-path attachment to a group, used as an alternative to the\n    legacy ``@&lt;binding&gt;.command`` decorator. Lets you declare commands\n    in any file without importing a shared ``CommandGroup`` binding \u2014\n    the ``group=`` string is the only contract.\n\n    Usage::\n\n        from toolr import command, command_group\n\n        command_group(\"ci.helm-diff-pr-comment\", docstring=__doc__)\n\n        @command(group=\"ci.helm-diff-pr-comment\")\n        def backend(ctx, env): ...\n\n        # Optional explicit command name (otherwise the function\n        # name is hyphenated and used).\n        @command(\"snippet-checker\", group=\"ci.helm-diff-pr-comment\")\n        def check_snippets(ctx): ...\n\n    Args:\n        name_or_func: When called as a bare decorator (``@command``),\n            this is the wrapped function. When called with parentheses\n            (``@command(\"name\", group=...)``), this is the override\n            for the command's CLI name; the function name (hyphenated)\n            is used otherwise.\n        group: Dotted full path of the target group\n            (e.g. ``\"ci\"`` or ``\"ci.helm-diff-pr-comment\"``). A\n            matching ``command_group(...)`` declaration must exist\n            elsewhere in ``tools/``; otherwise manifest-build fails\n            with a clear error.\n        aliases: Reserved for future use; currently no-op (tracked\n            with the rest of the ``arg(aliases=...)`` plumbing in\n            issue #198).\n\n    Returns:\n        The decorated function unchanged; the decorator's only job is\n        to record metadata that the static parser picks up. At runtime\n        toolr calls the function directly with the parsed args.\n    \"\"\"\n    # Bare form: @command def f(...) \u2014 no kwargs allowed in this shape.\n    if callable(name_or_func):\n        if group is not None or aliases is not None:\n            err_msg = (\n                '@command(...) with kwargs must be used as `@command(group=\"\u2026\")`'\n                \" \u2014 drop the parens-less form or move the kwargs into them.\"\n            )\n            raise TypeError(err_msg)\n        return name_or_func\n\n    # Parameterised form: @command(...) \u2014 returns a decorator. The\n    # decorator itself is currently a passthrough; the static parser\n    # is what consumes the `group=` / `name` strings.\n    def decorator(func: Callable[..., Any]) -&gt; Callable[..., Any]:\n        return func\n\n    return decorator\n</code></pre>"},{"location":"reference/command_group/","title":"command_group","text":"<p>Register a new command group.</p> <p>The <code>name</code> may be either a bare leaf name (<code>\"ci\"</code>) or a dotted path (<code>\"ci.helm-diff-pr-comment\"</code>). When dotted, everything before the final dot is the parent's full path; explicit <code>parent=</code> is ignored in that case.</p> <p>If you pass <code>docstring</code>, you won't be allowed to pass <code>description</code> or <code>long_description</code>. Those will be parsed by docstring-parser. The first line of the docstring will be used as the description, the rest will be used as the long description.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>Name of the command group; may include dotted parent path.</p> required <code>title</code> <code>str</code> <p>Optional short title shown in --help. Defaults to the leaf name when omitted.</p> <code>''</code> <code>description</code> <code>str | None</code> <p>Description for the command group</p> <code>None</code> <code>long_description</code> <code>str | None</code> <p>Long description for the command group</p> <code>None</code> <code>docstring</code> <code>str | None</code> <p>Docstring for the command group</p> <code>None</code> <code>parent</code> <code>str | None</code> <p>Optional parent command path using dot notation (e.g. \"tools.docker.build\")</p> <code>None</code> <p>Returns:</p> Type Description <code>CommandGroup</code> <p>A CommandGroup instance</p> Source code in <code>crates/toolr-py/python/toolr/_decorators.py</code> <pre><code>def command_group(\n    name: str,\n    title: str = \"\",\n    description: str | None = None,\n    long_description: str | None = None,\n    docstring: str | None = None,\n    parent: str | None = None,\n) -&gt; CommandGroup:\n    \"\"\"Register a new command group.\n\n    The ``name`` may be either a bare leaf name (``\"ci\"``) or a dotted\n    path (``\"ci.helm-diff-pr-comment\"``). When dotted, everything\n    before the final dot is the parent's full path; explicit\n    ``parent=`` is ignored in that case.\n\n    If you pass ``docstring``, you won't be allowed to pass ``description`` or ``long_description``.\n    Those will be parsed by [docstring-parser](https://pypi.org/project/docstring-parser/).\n    The first line of the docstring will be used as the description, the rest will be used as the long description.\n\n    Args:\n        name: Name of the command group; may include dotted parent path.\n        title: Optional short title shown in --help. Defaults to the\n            leaf name when omitted.\n        description: Description for the command group\n        long_description: Long description for the command group\n        docstring: Docstring for the command group\n        parent: Optional parent command path using dot notation (e.g. \"tools.docker.build\")\n\n    Returns:\n        A CommandGroup instance\n\n    \"\"\"\n    # Dotted-path form: split the leaf off the parent path. Explicit\n    # `parent=` kwarg takes a back seat to the dotted form so users\n    # pick one style and stick with it.\n    if \".\" in name:\n        parent_from_path, _, leaf = name.rpartition(\".\")\n        if parent is not None and parent != parent_from_path:\n            log.warning(\n                \"command_group(%r, parent=%r): explicit parent overridden by dotted path; using %r\",\n                name,\n                parent,\n                parent_from_path,\n            )\n        name = leaf\n        parent = parent_from_path\n    if parent is not None and not parent.startswith(\"tools.\"):\n        parent = f\"tools.{parent}\"\n    elif parent is None:\n        parent = \"tools\"\n    if not title:\n        title = name\n\n    collector = _get_command_group_storage()\n\n    group: CommandGroup | None = collector.get(f\"{parent}.{name}\")\n    if group is not None:\n        # In this case, we return the existing group\n        log.debug(\"Command group '%s' already exists, returning existing group\", f\"{parent}.{name}\")\n        return group\n\n    if docstring is not None:\n        if description is not None or long_description is not None:\n            err_msg = \"You can't pass both docstring and description or long_description\"\n            raise ValueError(err_msg)\n        parsed_docstring = Docstring.parse(docstring)\n        description = parsed_docstring.short_description\n        long_description = parsed_docstring.long_description\n    elif description is None:\n        err_msg = \"You must at least pass either the 'docstring' or 'description' argument\"\n        raise ValueError(err_msg)\n\n    if TYPE_CHECKING:\n        assert description is not None\n\n    # Create the command group\n    collector[f\"{parent}.{name}\"] = group = CommandGroup(\n        name=name,\n        title=title,\n        description=description,\n        parent=parent,\n        long_description=long_description,\n    )\n    return group\n</code></pre>"},{"location":"reference/context/","title":"Context","text":"<p>               Bases: <code>Struct</code></p> <p>Context object passed to every command group function as the first argument.</p>"},{"location":"reference/context/#toolr.Context.prompt","title":"prompt","text":"<pre><code>prompt(\n    prompt: TextType,\n    expected_type: type[bool],\n    *,\n    default: bool | None = None,\n    show_default: bool = True,\n) -&gt; bool\n</code></pre><pre><code>prompt(\n    prompt: TextType,\n    expected_type: type[str],\n    *,\n    password: bool = True,\n    default: str | None = None,\n    case_sensitive: bool = True,\n) -&gt; str\n</code></pre><pre><code>prompt(\n    prompt: TextType,\n    expected_type: type[int],\n    *,\n    choices: list[str] | None = None,\n    default: int | None = None,\n    show_default: bool = True,\n    show_choices: bool = True,\n) -&gt; int\n</code></pre><pre><code>prompt(\n    prompt: TextType,\n    expected_type: type[float],\n    *,\n    choices: list[str] | None = None,\n    default: float | None = None,\n    show_default: bool = True,\n    show_choices: bool = True,\n) -&gt; float\n</code></pre><pre><code>prompt(\n    prompt: TextType,\n    expected_type: None = None,\n    *,\n    choices: list[str] | None = None,\n    default: None = None,\n    case_sensitive: bool = True,\n    show_default: bool = True,\n    show_choices: bool = True,\n) -&gt; str\n</code></pre> <pre><code>prompt(\n    prompt: TextType,\n    expected_type: type[str | int | float | bool]\n    | None = None,\n    *,\n    password: bool = False,\n    case_sensitive: bool = True,\n    choices: list[str] | None = None,\n    default: str | int | float | bool | None = None,\n    show_default: bool = True,\n    show_choices: bool = True,\n) -&gt; str | int | float | bool\n</code></pre> <p>Prompt the user for input.</p> <p>This is a wrapper around rich.prompt.Prompt.ask.</p> <p>See rich.prompt.Prompt.ask for more details.</p> Source code in <code>crates/toolr-py/python/toolr/_context.py</code> <pre><code>def prompt(\n    self,\n    prompt: TextType,\n    expected_type: type[str | int | float | bool] | None = None,\n    *,\n    password: bool = False,\n    case_sensitive: bool = True,\n    choices: list[str] | None = None,\n    default: str | int | float | bool | None = None,\n    show_default: bool = True,\n    show_choices: bool = True,\n) -&gt; str | int | float | bool:\n    \"\"\"\n    Prompt the user for input.\n\n    This is a wrapper around [rich.prompt.Prompt.ask][rich.prompt].\n\n    See [rich.prompt.Prompt.ask][rich.prompt] for more details.\n    \"\"\"\n    return self._prompt(\n        prompt,\n        expected_type,\n        password=password,\n        case_sensitive=case_sensitive,\n        choices=choices,\n        default=default,\n        show_default=show_default,\n        show_choices=show_choices,\n    )\n</code></pre>"},{"location":"reference/context/#toolr.Context.print","title":"print","text":"<pre><code>print(\n    *args: ConsoleRenderable | RichCast | str, **kwargs: Any\n) -&gt; None\n</code></pre> <p>Print to stdout.</p> <p>This is a wrapper around :func:<code>rich.console.Console.print</code>.</p> <p>See :func:<code>rich.console.Console.print</code> for more details.</p> Source code in <code>crates/toolr-py/python/toolr/_context.py</code> <pre><code>def print(self, *args: Any, **kwargs: Any) -&gt; None:\n    \"\"\"\n    Print to stdout.\n\n    This is a wrapper around :func:`rich.console.Console.print`.\n\n    See :func:`rich.console.Console.print` for more details.\n    \"\"\"\n    self._console_stdout.print(*args, **kwargs)\n</code></pre>"},{"location":"reference/context/#toolr.Context.debug","title":"debug","text":"<pre><code>debug(*args: Any, **kwargs: Any) -&gt; None\n</code></pre> <p>Print debug message to stderr.</p> <p>This is a wrapper around rich.console.Console.log.</p> <p>See rich.console.Console.log for more details.</p> Source code in <code>crates/toolr-py/python/toolr/_context.py</code> <pre><code>def debug(self, *args: Any, **kwargs: Any) -&gt; None:\n    \"\"\"\n    Print debug message to stderr.\n\n    This is a wrapper around [rich.console.Console.log][rich.console.Console.log].\n\n    See [rich.console.Console.log][rich.console.Console.log] for more details.\n    \"\"\"\n    if self.verbosity &gt;= ConsoleVerbosity.VERBOSE:\n        kwargs.update(style=\"log-debug\", _stack_offset=2)\n        self._console_stderr.log(*args, **kwargs)\n</code></pre>"},{"location":"reference/context/#toolr.Context.info","title":"info","text":"<pre><code>info(*args: Any, **kwargs: Any) -&gt; None\n</code></pre> <p>Print info message to stderr.</p> <p>This is a wrapper around rich.console.Console.log.</p> <p>See rich.console.Console.log for more details.</p> Source code in <code>crates/toolr-py/python/toolr/_context.py</code> <pre><code>def info(self, *args: Any, **kwargs: Any) -&gt; None:\n    \"\"\"\n    Print info message to stderr.\n\n    This is a wrapper around [rich.console.Console.log][rich.console.Console.log].\n\n    See [rich.console.Console.log][rich.console.Console.log] for more details.\n    \"\"\"\n    if self.verbosity &gt;= ConsoleVerbosity.NORMAL:\n        kwargs.update(style=\"log-info\", _stack_offset=2)\n        self._console_stderr.log(*args, **kwargs)\n</code></pre>"},{"location":"reference/context/#toolr.Context.warn","title":"warn","text":"<pre><code>warn(*args: Any, **kwargs: Any) -&gt; None\n</code></pre> <p>Print warning message to stderr.</p> <p>This is a wrapper around rich.console.Console.log.</p> <p>See rich.console.Console.log for more details.</p> Source code in <code>crates/toolr-py/python/toolr/_context.py</code> <pre><code>def warn(self, *args: Any, **kwargs: Any) -&gt; None:\n    \"\"\"\n    Print warning message to stderr.\n\n    This is a wrapper around [rich.console.Console.log][rich.console.Console.log].\n\n    See [rich.console.Console.log][rich.console.Console.log] for more details.\n    \"\"\"\n    kwargs.update(style=\"log-warning\", _stack_offset=2)\n    self._console_stderr.log(*args, **kwargs)\n</code></pre>"},{"location":"reference/context/#toolr.Context.error","title":"error","text":"<pre><code>error(*args: Any, **kwargs: Any) -&gt; None\n</code></pre> <p>Print error message to stderr.</p> <p>This is a wrapper around rich.console.Console.log.</p> <p>See rich.console.Console.log for more details.</p> Source code in <code>crates/toolr-py/python/toolr/_context.py</code> <pre><code>def error(self, *args: Any, **kwargs: Any) -&gt; None:\n    \"\"\"\n    Print error message to stderr.\n\n    This is a wrapper around [rich.console.Console.log][rich.console.Console.log].\n\n    See [rich.console.Console.log][rich.console.Console.log] for more details.\n    \"\"\"\n    kwargs.update(style=\"log-error\", _stack_offset=2)\n    self._console_stderr.log(*args, **kwargs)\n</code></pre>"},{"location":"reference/context/#toolr.Context.exit","title":"exit","text":"<pre><code>exit(\n    status: int = 0, message: str | None = None\n) -&gt; NoReturn\n</code></pre> <p>Exit the command execution.</p> Source code in <code>crates/toolr-py/python/toolr/_context.py</code> <pre><code>def exit(self, status: int = 0, message: str | None = None) -&gt; NoReturn:\n    \"\"\"\n    Exit the command execution.\n    \"\"\"\n    if message is not None:\n        if status == 0:\n            style = \"exit-ok\"\n        else:\n            style = \"exit-failure\"\n        self._console_stderr.print(message, style=style)\n    self.parser.exit(status)\n</code></pre>"},{"location":"reference/context/#toolr.Context.run","title":"run","text":"<pre><code>run(\n    *cmdline: str,\n    stream_output: bool = True,\n    capture_output: bool = False,\n    timeout_secs: float | None = None,\n    no_output_timeout_secs: float | None = None,\n    **kwargs: Any,\n) -&gt; CommandResult[str] | CommandResult[bytes]\n</code></pre> <p>Run a command with the given arguments.</p> <p>Thin wrapper around the internal command-runner that provides a simpler interface for command functions.</p> <p>Parameters:</p> Name Type Description Default <code>cmdline</code> <code>str</code> <p>Command line to run</p> <code>()</code> <code>stream_output</code> <code>bool</code> <p>Whether to stream output to stdout/stderr</p> <code>True</code> <code>capture_output</code> <code>bool</code> <p>Whether to capture output to return</p> <code>False</code> <code>timeout_secs</code> <code>float | None</code> <p>Maximum time to wait for command completion</p> <code>None</code> <code>no_output_timeout_secs</code> <code>float | None</code> <p>Maximum time to wait without output</p> <code>None</code> <code>kwargs</code> <code>Any</code> <p>Additional keyword arguments passed to the internal runner.</p> <code>{}</code> <p>Returns:</p> Type Description <code>CommandResult[str] | CommandResult[bytes]</code> <p>CommandResult instance.</p> Source code in <code>crates/toolr-py/python/toolr/_context.py</code> <pre><code>def run(\n    self,\n    *cmdline: str,\n    stream_output: bool = True,\n    capture_output: bool = False,\n    timeout_secs: float | None = None,\n    no_output_timeout_secs: float | None = None,\n    **kwargs: Any,\n) -&gt; CommandResult[str] | CommandResult[bytes]:\n    \"\"\"Run a command with the given arguments.\n\n    Thin wrapper around the internal command-runner that provides a\n    simpler interface for command functions.\n\n    Args:\n        cmdline: Command line to run\n        stream_output: Whether to stream output to stdout/stderr\n        capture_output: Whether to capture output to return\n        timeout_secs: Maximum time to wait for command completion\n        no_output_timeout_secs: Maximum time to wait without output\n        kwargs: Additional keyword arguments passed to the internal runner.\n\n    Returns:\n        CommandResult instance.\n    \"\"\"\n    self.info(f\"\"\"Running '{\" \".join(cmdline)}'\"\"\")\n    # Per-call values win over the Context defaults set by\n    # `toolr --timeout-secs` / `--no-output-timeout-secs`. The\n    # defaults only fill in when the caller passes nothing.\n    if timeout_secs is None:\n        timeout_secs = self.default_timeout_secs\n    if no_output_timeout_secs is None:\n        no_output_timeout_secs = self.default_no_output_timeout_secs\n    return command.run(\n        cmdline,\n        stream_output=stream_output,\n        capture_output=capture_output,\n        timeout_secs=timeout_secs,\n        no_output_timeout_secs=no_output_timeout_secs,\n        **kwargs,\n    )\n</code></pre>"},{"location":"reference/context/#toolr.Context.chdir","title":"chdir","text":"<pre><code>chdir(path: str | Path) -&gt; Iterator[Path]\n</code></pre> <p>Change the working directory for this context.</p> <p>Parameters:</p> Name Type Description Default <code>path</code> <code>str | Path</code> <p>The new working directory path</p> required <p>Returns:</p> Type Description <code>Iterator[Path]</code> <p>Iterator yielding the new working directory as a Path object</p> <p>This is a context manager, so it should be used with 'with':</p> <p>.. code-block:: python</p> <pre><code>with ctx.chdir(\"/some/path\") as p:\n    # Do something in /some/path\n    # p is the Path object for /some/path\n</code></pre> Source code in <code>crates/toolr-py/python/toolr/_context.py</code> <pre><code>@contextmanager\ndef chdir(self, path: str | pathlib.Path) -&gt; Iterator[pathlib.Path]:\n    \"\"\"Change the working directory for this context.\n\n    Args:\n        path: The new working directory path\n\n    Returns:\n        Iterator yielding the new working directory as a Path object\n\n    This is a context manager, so it should be used with 'with':\n\n    .. code-block:: python\n\n        with ctx.chdir(\"/some/path\") as p:\n            # Do something in /some/path\n            # p is the Path object for /some/path\n    \"\"\"\n    cwd = pathlib.Path.cwd()\n    if isinstance(path, str):\n        path = pathlib.Path(path)\n    try:\n        os.chdir(path)\n        yield path\n    finally:\n        if not cwd.exists():\n            self.error(f\"Unable to change back to path {cwd}\")\n        else:\n            os.chdir(cwd)\n</code></pre>"},{"location":"reference/context/#toolr.Context.which","title":"which","text":"<pre><code>which(\n    name: str,\n    mode: int = F_OK | X_OK,\n    path: str | None = None,\n) -&gt; str | None\n</code></pre> <p>Find the path to an executable in the system PATH.</p> <p>This is a wrapper around shutil.which.</p> <p>See shutil.which for more details.</p> Source code in <code>crates/toolr-py/python/toolr/_context.py</code> <pre><code>def which(self, name: str, mode: int = os.F_OK | os.X_OK, path: str | None = None) -&gt; str | None:\n    \"\"\"\n    Find the path to an executable in the system PATH.\n\n    This is a wrapper around [shutil.which][shutil.which].\n\n    See [shutil.which][shutil.which] for more details.\n    \"\"\"\n    return shutil.which(name, mode=mode, path=path)\n</code></pre>"},{"location":"reference/testing/","title":"testing","text":"<p>Utilities for testing ToolR and supported commands.</p> <p>Survives the retirement of the Python CLI frontend (<code>_parser.py</code> / <code>_registry.py</code>). Instead of the legacy in-process <code>CommandRegistry</code>, this helper patches the registry storage in :mod:<code>toolr._decorators</code> and drives the same Python-side discovery that the Rust binary's dynamic manifest layer uses (<code>toolr._introspect.build_payload</code>).</p> <p>The API surface remains the same: <code>with CommandsTester(search_path=...) as t</code> yields an isolated collector available via <code>t.collected_command_groups()</code>.</p>"},{"location":"reference/testing/#toolr.testing.CommandsTester","title":"CommandsTester","text":"<p>Helper class to simplify testing command discovery.</p> <p>Patches :func:<code>toolr._decorators._get_command_group_storage</code> so each test gets a fresh dict, then (in :meth:<code>discover</code>) imports modules under the search path's <code>tools/</code> package and loads <code>toolr.commands</code> entry points to populate that dict. Tests then read it via :meth:<code>collected_command_groups</code>.</p>"},{"location":"reference/testing/#toolr.testing.CommandsTester.collected_command_groups","title":"collected_command_groups","text":"<pre><code>collected_command_groups() -&gt; dict[str, object]\n</code></pre> <p>Get the collected command groups.</p> Source code in <code>crates/toolr-py/python/toolr/testing.py</code> <pre><code>def collected_command_groups(self) -&gt; dict[str, object]:\n    \"\"\"\n    Get the collected command groups.\n    \"\"\"\n    return {**self.command_group_collector}\n</code></pre>"},{"location":"reference/testing/#toolr.testing.CommandsTester.discover","title":"discover","text":"<pre><code>discover() -&gt; None\n</code></pre> <p>Trigger Python-side discovery against <code>search_path</code>.</p> <p>Drives the same import-and-walk pipeline that <code>python -m toolr._introspect</code> uses on behalf of the Rust binary. Registers <code>tools/*.py</code> modules and any installed <code>toolr.commands</code> entry points (unless the <code>skip_loading_entry_points</code> flag was set on construction).</p> Source code in <code>crates/toolr-py/python/toolr/testing.py</code> <pre><code>def discover(self) -&gt; None:\n    \"\"\"Trigger Python-side discovery against ``search_path``.\n\n    Drives the same import-and-walk pipeline that\n    ``python -m toolr._introspect`` uses on behalf of the Rust binary.\n    Registers ``tools/*.py`` modules and any installed\n    ``toolr.commands`` entry points (unless the\n    ``skip_loading_entry_points`` flag was set on construction).\n    \"\"\"\n    # Imported lazily to avoid registering modules at testing.py import time.\n    from toolr._introspect import _import_tools_modules  # noqa: PLC0415\n    from toolr._introspect import _load_entry_points  # noqa: PLC0415\n\n    warnings: list[str] = []\n    _import_tools_modules(warnings)\n    if not self.skip_loading_entry_points:\n        _load_entry_points(warnings)\n</code></pre>"},{"location":"reference/testing/#toolr.testing.CommandsTester.__enter__","title":"__enter__","text":"<pre><code>__enter__() -&gt; Self\n</code></pre> <p>Enter the context manager.</p> Source code in <code>crates/toolr-py/python/toolr/testing.py</code> <pre><code>def __enter__(self) -&gt; Self:\n    \"\"\"\n    Enter the context manager.\n    \"\"\"\n    sys.modules.clear()\n    sys.modules.update(self.sys_modules)\n    os.chdir(self.search_path)\n    if self.skip_loading_entry_points:\n        self.entry_points_patcher.start()\n    self.command_group_patcher.start()\n    # Replace sys.path with the search path plus the site-packages\n    # entries from the saved sys_path; drop anything that points\n    # back at the host repo (which would shadow the fixture's\n    # ``tools/`` tree with the repo's own).\n    site_pkg_entries = [p for p in self.sys_path if \"site-packages\" in p or \"dist-packages\" in p]\n    sys.path[:] = [str(self.search_path), *site_pkg_entries]\n    return self\n</code></pre>"},{"location":"reference/testing/#toolr.testing.CommandsTester.__exit__","title":"__exit__","text":"<pre><code>__exit__(*args: object) -&gt; None\n</code></pre> <p>Exit the context manager.</p> Source code in <code>crates/toolr-py/python/toolr/testing.py</code> <pre><code>def __exit__(self, *args: object) -&gt; None:\n    \"\"\"\n    Exit the context manager.\n    \"\"\"\n    os.chdir(self.cwd)\n    self.command_group_patcher.stop()\n    if self.skip_loading_entry_points:\n        self.entry_points_patcher.stop()\n    self.command_group_collector.clear()\n    sys.path[:] = self.sys_path\n</code></pre>"},{"location":"writing-commands/","title":"Writing commands","text":"<p>The authoring guide for tools-authors. Read in order, or jump to the section you need:</p> <ol> <li>Groups &amp; commands \u2014 declaring a <code>command_group</code> and    attaching commands to it via <code>@command(group=\"\u2026\")</code>.</li> <li>Arguments \u2014 turning function parameters into CLI    arguments via type hints (positional / optional / flag /    <code>Literal[...]</code> / <code>Enum</code>).</li> <li>Docstrings \u2014 Google-style docstrings drive    <code>--help</code> output.</li> <li>Using <code>ctx</code> \u2014 print, run subprocesses, prompt for    input, exit cleanly.</li> <li>Annotations \u2014 <code>arg()</code> for aliases, choices,    mutually exclusive groups.</li> <li>Nested groups \u2014 multi-level command hierarchies.</li> <li>Known bugs \u2014 GA-blocking gaps in the current    rust-front-end build.</li> </ol> <p>Every example on these pages is a real file under <code>docs/writing-commands/files/</code> that toolr can actually execute against the documentation fixture.</p> <p>Migrating from the legacy decorators?</p> <p>If your project still uses the old <code>group = command_group(...)</code> + <code>@group.command</code> style, see the migration guide. The legacy decorators are deprecated and will be removed in toolr 1.0; every legacy call emits a runtime warning telling you exactly where to edit.</p>"},{"location":"writing-commands/across-files/","title":"Scaling command groups across files","text":"<p>The bound-decorator form shown in Groups &amp; commands is the canonical shape for tools that live in a single file. When your tools spread across multiple files, importing the <code>CommandGroup</code> binding around your project gets noisy. Toolr's string-keyed decorator was built for that case.</p>"},{"location":"writing-commands/across-files/#the-form","title":"The form","text":"<p>Declare the group in one file:</p> <pre><code># tools/_common.py\nfrom toolr import command_group\n\ncommand_group(\"ci\", title=\"CI\", description=\"Continuous-integration helpers\")\n</code></pre> <p>Attach commands from any other file \u2014 no shared import:</p> <pre><code># tools/helm.py\nfrom toolr import Context, command\n\n\n@command(group=\"ci\")\ndef helm_diff(ctx: Context) -&gt; None:\n    \"\"\"Diff the cluster against rendered manifests.\"\"\"\n    ...\n\n\n@command(group=\"ci\")\ndef deploy(ctx: Context, env: str) -&gt; None:\n    \"\"\"Deploy to `env`.\"\"\"\n    ...\n</code></pre> <p>Both files contribute to the same <code>ci</code> group. <code>toolr ci --help</code> lists <code>helm-diff</code> and <code>deploy</code> regardless of which file the static parser visited first.</p>"},{"location":"writing-commands/across-files/#why-use-it","title":"Why use it","text":"<p>Files become decoupled. Each file is self-contained. No need to import (or otherwise share) a <code>CommandGroup</code> binding across modules just to attach a command to its group.</p> <p>Order independence. Toolr does a two-pass static parse: every <code>command_group(...)</code> declaration is collected first, then every <code>@command(group=...)</code> reference is resolved against the registry. The order files are scanned in doesn't matter \u2014 <code>tools/helm.py</code> can attach to a <code>ci</code> group declared in <code>tools/_common.py</code> regardless of which file the parser visits first.</p> <p>Typo safety. Misspelled group references fail at manifest-build time with the nearest match suggested:</p> <pre><code>error: unknown group references (1):\n  - tools.helm::deploy: references group `c1` which has no\n    `command_group(...)` declaration. Did you mean `ci`?\n</code></pre> <p>The bound-decorator form catches the same kind of mistake at import time (as a <code>NameError</code> on the binding), so both forms fail fast \u2014 they just fail in different places. The string-keyed form fails during manifest build, which is the same step that surfaces every other static error and reads them all at once.</p> <p>Overriding the CLI name. Pass the name as the first positional argument:</p> <pre><code>@command(\"snippet-checker\", group=\"ci\")\ndef check_snippets(ctx: Context) -&gt; None: ...\n# \u2192 toolr ci snippet-checker\n</code></pre>"},{"location":"writing-commands/across-files/#when-to-choose-which-form","title":"When to choose which form","text":"Situation Form All commands for a group live in the same file <code>group = command_group(...)</code> + <code>@group.command</code> Commands span multiple files, or you want one file to attach to a group declared elsewhere <code>command_group(\"name\", ...)</code> in one file, <code>@command(group=\"name\")</code> in the others Nested groups (subgroups) <code>command_group(\"parent.child\", ...)</code> \u2014 see Nested groups <p>Both forms produce identical CLIs. Picking the wrong one for your project size doesn't produce different behaviour, just different ergonomics \u2014 the rule of thumb is \"use the bound form until imports start to chafe, then switch.\"</p>"},{"location":"writing-commands/across-files/#mixing-styles","title":"Mixing styles","text":"<p>A single project can use both \u2014 for example, a flat in-file group in one tools module and a string-keyed group spanning several modules in another. Toolr resolves both through the same registry, so a group declared with the bound form is reachable from a string key in a different file, and vice versa.</p> <p>Next: Nested groups \u2192</p>"},{"location":"writing-commands/annotations/","title":"Annotations","text":""},{"location":"writing-commands/annotations/#annotations","title":"Annotations","text":"<p>When you need more than the defaults \u2014 alternate flag spellings, env-var defaults, mutually-exclusive groups, grouped <code>--help</code> sections, filesystem constraints \u2014 wrap the parameter's type with <code>typing.Annotated[...]</code> and a call to <code>arg(...)</code>.</p> <pre><code>from typing import Annotated\nfrom toolr import arg, command, command_group\n\ncommand_group(\"git\", title=\"Git helpers\")\n\n\n@command(group=\"git\")\ndef show(\n    ctx,\n    sha: Annotated[str, arg(aliases=[\"-s\"], metavar=\"SHA\", env=\"GIT_SHA\")] = \"HEAD\",\n) -&gt; None: ...\n</code></pre>"},{"location":"writing-commands/annotations/#available-kwargs","title":"Available kwargs","text":"Kwarg What it does <code>aliases=[\"-n\", \"--also\"]</code> Extra short / long flag spellings. Single-char entries become clap shorts; longer entries become long aliases. <code>metavar=\"NAME\"</code> Custom placeholder shown in <code>--help</code> (<code>&lt;NAME&gt;</code> instead of <code>&lt;sha&gt;</code>). <code>env=\"VAR\"</code> Read the default from this env var when the flag isn't passed. <code>hide=True</code> Omit from <code>--help</code> output. Still parseable on the CLI. <code>help_section=\u2026</code> Group related args under a named <code>--help</code> heading. See below. <code>display_order=N</code> Lower values render first in <code>--help</code>. <code>conflicts_with=[...]</code> Mutex relationships: at most one of these flags per invocation. <code>requires=[...]</code> If this flag is set, every name listed must also be set. <code>path_must_exist=True</code> Reject paths that don't exist (Path types only). <code>path_must_be_file=True</code> Reject non-files; implies <code>path_must_exist</code>. <code>path_must_be_dir=True</code> Reject non-dirs; implies <code>path_must_exist</code>."},{"location":"writing-commands/annotations/#aliases-short-flags-alternate-long-flags","title":"Aliases (short flags + alternate long flags)","text":"<pre><code>@command(group=\"git\")\ndef diff(\n    ctx,\n    base: Annotated[str, arg(aliases=[\"-b\", \"--from\"])] = \"HEAD~1\",\n) -&gt; None: ...\n</code></pre> <p><code>--base</code>, <code>-b</code>, and <code>--from</code> all reach the same parameter. Hyphens at the start of each alias are stripped; one-character names become shorts.</p>"},{"location":"writing-commands/annotations/#env-var-defaults","title":"Env-var defaults","text":"<pre><code>@command(group=\"cloud\")\ndef deploy(\n    ctx,\n    api_token: Annotated[str, arg(env=\"DEPLOY_API_TOKEN\")],\n) -&gt; None: ...\n</code></pre> <p>When the user doesn't pass <code>--api-token</code>, clap reads <code>$DEPLOY_API_TOKEN</code> instead. If both the env var and a <code>default=</code> are absent and the flag isn't passed, the parameter remains required.</p>"},{"location":"writing-commands/annotations/#mutually-exclusive-arguments-conflicts_with","title":"Mutually exclusive arguments \u2014 <code>conflicts_with</code>","text":"<pre><code>@command(group=\"example\")\ndef hello(\n    ctx,\n    name: str,\n    verbose: Annotated[bool, arg(conflicts_with=[\"quiet\"])] = False,\n    quiet: Annotated[bool, arg(conflicts_with=[\"verbose\"])] = False,\n) -&gt; None: ...\n</code></pre> <p>Passing <code>--verbose --quiet</code> together fails CLI validation:</p> <pre><code>error: the argument '--verbose' cannot be used with '--quiet'\n</code></pre> <p><code>requires=[...]</code> is the inverse: if you pass this flag, every name in the list must also be set. Useful for paired options like <code>--ssl-cert</code> + <code>--ssl-key</code>.</p>"},{"location":"writing-commands/annotations/#display-grouping-with-help_section","title":"Display grouping with <code>help_section</code>","text":"<p>Big functions accumulate flags; bare <code>--help</code> listings get hard to scan. Group related flags under a named heading by declaring an <code>arg_section</code> at module scope, then pointing each member's annotation at it:</p> <pre><code>from toolr import arg, arg_section, command, command_group\n\nLOGGING = arg_section(\n    \"Logging Options\",\n    description=\"Control verbosity and output format.\",\n)\n\ncommand_group(\"deploy\", title=\"Deploy\")\n\n\n@command(group=\"deploy\")\ndef push(\n    ctx,\n    target: str,\n    verbose: Annotated[bool, arg(help_section=LOGGING)] = False,\n    quiet: Annotated[bool, arg(help_section=LOGGING)] = False,\n    log_file: Annotated[str | None, arg(help_section=LOGGING, env=\"DEPLOY_LOG\")] = None,\n) -&gt; None: ...\n</code></pre> <p>The <code>LOGGING</code> object is reusable \u2014 share it across commands in the same file. Two <code>arg_section(\"X\")</code> calls don't make the same section; define the object once and pass it by reference.</p>"},{"location":"writing-commands/annotations/#inline-form","title":"Inline form","text":"<p>If you don't want a module-level constant for a one-off heading, pass the call directly:</p> <pre><code>verbose: Annotated[bool, arg(help_section=arg_section(\"Logging Options\"))] = False\n</code></pre> <p>Or, for the simplest case where you just want a title and no description, a bare string is accepted:</p> <pre><code>verbose: Annotated[bool, arg(help_section=\"Logging Options\")] = False\n</code></pre>"},{"location":"writing-commands/annotations/#deprecated-kwargs-removed-in-10","title":"Deprecated kwargs (removed in 1.0)","text":"<p>These still parse but emit a <code>ToolrDeprecationWarning</code>:</p> <ul> <li><code>required=</code> \u2014 use <code>T | None</code> or <code>*args: T</code>.</li> <li><code>choices=</code> \u2014 use <code>Literal[\"a\",\"b\"]</code> or an <code>Enum</code>.</li> <li><code>nargs=</code> \u2014 use <code>T | None</code> / <code>*args: T</code> / <code>tuple[T1, T2]</code>.</li> <li><code>action=</code> \u2014 <code>bool</code> infers flag, <code>list[T]</code> infers append, <code>Count</code> infers count.</li> <li><code>group=</code> \u2014 use <code>conflicts_with=[\u2026]</code> for mutex, <code>help_section=</code> for display grouping.</li> <li><code>must_exist=</code> / <code>must_be_file=</code> / <code>must_be_dir=</code> \u2014 rename to <code>path_must_exist</code> / <code>path_must_be_file</code> / <code>path_must_be_dir</code>.</li> </ul> <p>Next: Nested groups \u2192</p>"},{"location":"writing-commands/arguments/","title":"Arguments","text":"<p>Every parameter of a command function becomes a CLI argument. Toolr infers the shape (positional vs optional, with-value vs flag, enum vs free-form) from the parameter's type hint, default value, and syntactic position.</p> <p>The first parameter (<code>ctx: Context</code>) is always toolr's, never a CLI argument.</p>"},{"location":"writing-commands/arguments/#supported-types","title":"Supported types","text":"<p>Toolr enforces a closed set of parameter types. Anything outside this table is rejected at manifest-build time with an error pointing at <code>toolr.types</code> as the extension namespace.</p> Annotation Validated by Wire format Python receives <code>int</code> clap JSON number <code>int</code> <code>float</code> clap JSON number <code>float</code> <code>bool</code> clap JSON bool <code>bool</code> <code>str</code> none (passthrough) JSON string <code>str</code> <code>pathlib.Path</code> clap (custom parser) string <code>pathlib.Path</code> <code>toolr.types.AbsolutePath</code> clap (absolutise vs cwd) absolute string <code>pathlib.Path</code> <code>toolr.types.ResolvedPath</code> clap (<code>canonicalize()</code>) resolved string <code>pathlib.Path</code> <code>toolr.types.DateTime</code> clap (chrono RFC 3339) string <code>datetime.datetime</code> <code>toolr.types.Date</code> clap (chrono ISO date) string <code>datetime.date</code> <code>toolr.types.Time</code> clap (chrono ISO time) string <code>datetime.time</code> <code>toolr.types.UUID</code> clap (<code>uuid</code> crate) string <code>uuid.UUID</code> <code>toolr.types.IPv4</code> clap (<code>std::net::Ipv4Addr</code>) string <code>ipaddress.IPv4Address</code> <code>toolr.types.IPv6</code> clap (<code>std::net::Ipv6Addr</code>) string <code>ipaddress.IPv6Address</code> <code>toolr.types.Email</code> clap (<code>email_address</code> crate) string <code>str</code> (pre-validated) <code>toolr.types.Version</code> clap (<code>pep440_rs</code> crate) string <code>packaging.version.Version</code> <code>toolr.types.Count</code> clap (<code>ArgAction::Count</code>) integer <code>int</code> <code>Literal[\"a\", \"b\"]</code> clap (allowed-values) string <code>Literal</code> value <code>Enum</code> subclass clap (member values) string enum member <code>list[T]</code> (T above) clap per-element JSON array <code>list[T]</code> <code>tuple[T1, T2, \u2026]</code> clap arity, msgspec per-slot JSON array <code>tuple[T1, T2]</code> <code>*args: T</code> clap (trailing variadic) JSON array splatted <code>T...</code> <code>T \\| None</code> clap (<code>required=false</code>) typed or absent <code>T</code> or <code>None</code> <p>Bad input fails fast at the clap parse layer \u2014 no Python spawn:</p> <pre><code>$ toolr math add A 3\nerror: invalid value 'A' for '&lt;a&gt;': invalid digit found in string\n</code></pre>"},{"location":"writing-commands/arguments/#richer-types-via-toolrtypes","title":"Richer types via <code>toolr.types</code>","text":"<p>Stdlib primitives are recognised natively. Anything richer is opt-in through the <code>toolr.types</code> namespace, which makes the supported set discoverable (<code>dir(toolr.types)</code>) and stops uncoupled annotations from quietly drifting:</p> <pre><code>from toolr.types import DateTime, UUID, Email\n\ndef schedule(ctx: Context, when: DateTime, job_id: UUID, owner: Email) -&gt; None: ...\n</code></pre> <p>Each name is a stdlib alias at runtime (<code>DateTime is datetime.datetime</code>, <code>UUID is uuid.UUID</code>, \u2026) \u2014 toolr-specific only at the import-path level. If you annotate with a type toolr doesn't recognise (e.g. <code>datetime.datetime</code> directly, or a custom dataclass), manifest-build rejects the file with a pointer to <code>toolr.types</code> for the extension namespace.</p>"},{"location":"writing-commands/arguments/#positional-arguments","title":"Positional arguments","text":"<p>Parameters without a default value become required positional CLI arguments. The annotation is reported in <code>--help</code> and used by toolr for shell completion.</p> <pre><code>from __future__ import annotations\n\nfrom toolr import Context\nfrom toolr import command\nfrom toolr import command_group\n\ncommand_group(\"math\", \"Math Commands\", \"Basic mathematical operations\")\n\n\n@command(group=\"math\")\ndef add(ctx: Context, a: int, b: int):\n    \"\"\"Add two numbers together.\n\n    Args:\n        a: First number.\n        b: Second number.\n    \"\"\"\n    result = a + b\n    ctx.info(f\"{a} + {b} = {result}\")\n    return result\n</code></pre> <pre><code>toolr math add --help\n</code></pre> <pre><code>Add two numbers together.\n\nUsage: toolr math add &lt;a&gt; &lt;b&gt;\n\nArguments:\n  &lt;a&gt;\n          First number.\n\n  &lt;b&gt;\n          Second number.\n\nOptions:\n  -h, --help\n          Print help\n</code></pre>"},{"location":"writing-commands/arguments/#optional-arguments-with-a-default-value","title":"Optional arguments (with a default value)","text":"<p>Parameters with a default value become <code>--name VALUE</code> flags. The type hint dictates the value type:</p> <pre><code>from __future__ import annotations\n\nfrom toolr import Context\nfrom toolr import command\nfrom toolr import command_group\n\ncommand_group(\"greeting\", \"Greeting Commands\", \"Commands for greeting users\")\n\n\n@command(group=\"greeting\")\ndef hello(ctx: Context, name: str = \"World\"):\n    \"\"\"Say hello.\n\n    Args:\n        name: The name of the person to greet.\n    \"\"\"\n    ctx.info(\"Hello\", name, \"!\")\n</code></pre> <pre><code>toolr greeting hello --help\n</code></pre> <pre><code>Say hello.\n\nUsage: toolr greeting hello [OPTIONS]\n\nOptions:\n  -h, --help\n          Print help\n\n      --name &lt;name&gt;\n          The name of the person to greet.\n\n          [default: World]\n</code></pre>"},{"location":"writing-commands/arguments/#boolean-flags","title":"Boolean flags","text":"<p>A <code>bool</code> annotation with a default of <code>False</code> is declared as a flag:</p> <pre><code>from __future__ import annotations\n\nfrom toolr import Context\nfrom toolr import command\nfrom toolr import command_group\n\ncommand_group(\"example\", \"Example Commands\", \"Example command group\")\n\n\n@command(group=\"example\")\ndef process(ctx: Context, verbose: bool = False, dry_run: bool = False):\n    \"\"\"Process something with optional flags.\n\n    Args:\n        verbose: Whether to print verbose output.\n        dry_run: Whether to perform a dry run (no changes will be made).\n    \"\"\"\n    if verbose:\n        ctx.info(\"Verbose mode enabled\")\n\n    if dry_run:\n        ctx.info(\"Dry run mode - no changes will be made\")\n        return\n\n    ctx.info(\"Processing...\")\n</code></pre>"},{"location":"writing-commands/arguments/#literal-for-choice-restricted-values","title":"<code>Literal[...]</code> for choice-restricted values","text":"<p>A <code>Literal[\"a\", \"b\", \"c\"]</code> annotation produces a <code>--name {a,b,c}</code> flag that validates against the allowed values and shows them in <code>--help</code>.</p> <pre><code>from __future__ import annotations\n\nfrom typing import Literal\n\nfrom toolr import Context\nfrom toolr import command\nfrom toolr import command_group\n\ncommand_group(\"logs\", title=\"Logs\", description=\"Logging utilities\")\n\n\n@command(group=\"logs\")\ndef set_level(\n    ctx: Context,\n    level: Literal[\"debug\", \"info\", \"warning\", \"error\"] = \"info\",\n) -&gt; None:\n    \"\"\"Set the active log level.\n\n    Args:\n        level: Which level to use.\n    \"\"\"\n    ctx.print(f\"log level is now: {level}\")\n</code></pre> <pre><code>toolr logs set-level --help\n</code></pre> <pre><code>Set the active log level.\n\nUsage: toolr logs set-level [OPTIONS]\n\nOptions:\n  -h, --help\n          Print help\n\n      --level &lt;level&gt;\n          Which level to use.\n\n          [default: info]\n          [possible values: debug, info, warning, error]\n</code></pre>"},{"location":"writing-commands/arguments/#enums","title":"Enums","text":"<p>A parameter annotated with an <code>enum.Enum</code> (or <code>StrEnum</code>) subclass behaves the same as <code>Literal[...]</code> \u2014 the choices are the enum members, the resolved value is the enum instance. See the <code>Operation</code> enum on <code>docs/writing-commands/files/docstrings-example.py</code> for a full example.</p>"},{"location":"writing-commands/arguments/#listt-for-repeated-values","title":"<code>list[T]</code> for repeated values","text":"<p>Annotate a parameter as <code>list[T]</code> to accept <code>--name VALUE</code> repeated multiple times (each invocation appends).</p> <pre><code>from __future__ import annotations\n\nfrom toolr import Context\nfrom toolr import command\nfrom toolr import command_group\n\ncommand_group(\"files\", \"File Commands\", \"File operations\")\n\n\n@command(group=\"files\")\ndef process_files(ctx: Context, files: list[str]):\n    \"\"\"Process multiple files.\n\n    Args:\n        files: The files to process.\n    \"\"\"\n    for file in files:\n        ctx.info(f\"Processing {file}...\")\n</code></pre>"},{"location":"writing-commands/arguments/#args-for-variadic-positionals","title":"<code>*args</code> for variadic positionals","text":"<p>Capture an arbitrary number of positional arguments with <code>*args</code>. The annotation on the parameter is the element type.</p> <pre><code>from __future__ import annotations\n\nfrom toolr import Context\nfrom toolr import command\nfrom toolr import command_group\n\ncommand_group(\"files\", \"File Commands\", \"File operations\")\n\n\n@command(group=\"files\")\ndef process_files(ctx: Context, *files: str):\n    \"\"\"Process multiple files.\n\n    Args:\n        files: The files to process.\n    \"\"\"\n    for file in files:\n        ctx.info(f\"Processing {file}...\")\n</code></pre>"},{"location":"writing-commands/arguments/#path-constraints","title":"Path constraints","text":"<p><code>pathlib.Path</code> (and its <code>toolr.types.AbsolutePath</code> / <code>ResolvedPath</code> variants) accept additional opt-in filesystem checks through <code>Annotated[Path, arg(...)]</code>:</p> Constraint Effect <code>arg(path_must_exist=True)</code> reject paths that don't exist on disk <code>arg(path_must_be_file=True)</code> reject anything that isn't a regular file (implies <code>path_must_exist</code>) <code>arg(path_must_be_dir=True)</code> reject anything that isn't a directory (implies <code>path_must_exist</code>) <pre><code>from pathlib import Path\nfrom typing import Annotated\nfrom toolr import arg\n\ndef read_config(\n    ctx: Context,\n    config: Annotated[Path, arg(path_must_be_file=True)],\n    workdir: Annotated[Path, arg(path_must_be_dir=True)],\n) -&gt; None:\n    ...\n</code></pre> <p>The constraints fire at clap-parse time \u2014 bad invocations error in microseconds with a precise message:</p> <pre><code>$ toolr fs read /tmp/missing.txt /tmp\nerror: invalid value '/tmp/missing.txt' for '&lt;config&gt;':\npath does not exist: /tmp/missing.txt\n</code></pre>"},{"location":"writing-commands/arguments/#module-level-type-aliases","title":"Module-level type aliases","text":"<p>Repeating the same <code>Annotated[\u2026]</code> blob across several command signatures gets noisy. Define it once at module scope; toolr's static parser follows the alias to its underlying type:</p> <pre><code>from typing import Annotated, TypeAlias\n\nfrom toolr import Context, arg, command, command_group\n\nCommitHash: TypeAlias = Annotated[\n    str | None,\n    arg(help=\"A 40-char git SHA, or None for HEAD.\"),\n]\n\ncommand_group(\"git\", title=\"Git helpers\")\n\n\n@command(group=\"git\")\ndef show(ctx: Context, sha: CommitHash = None) -&gt; None: ...\n\n\n@command(group=\"git\")\ndef diff(ctx: Context, base: CommitHash = None) -&gt; None: ...\n</code></pre> <p>Both <code>show</code> and <code>diff</code> end up with the same <code>--sha</code> / <code>--base</code> treatment, with the alias's <code>arg(...)</code> metadata applied to each. Aliases compose with any of the types in the matrix above (<code>list[\u2026]</code>, <code>Literal[\u2026]</code>, <code>T | None</code>, <code>toolr.types.*</code>).</p>"},{"location":"writing-commands/arguments/#heterogeneous-tuples","title":"Heterogeneous tuples","text":"<p>A <code>tuple[T1, T2, \u2026]</code> parameter declares a fixed-arity positional group; toolr enforces the count at clap-parse time and msgspec validates each slot against its declared type:</p> <pre><code>def link(ctx: Context, mapping: tuple[str, int]) -&gt; None: ...\n</code></pre> <pre><code>toolr graph link foo 7      # OK\ntoolr graph link foo bar    # error: invalid value 'bar' for slot 1: invalid digit\ntoolr graph link foo        # error: missing slot 1\n</code></pre> <p>The same shape works for keyword args too:</p> <pre><code>def deploy(ctx: Context, port_range: tuple[int, int] = (8000, 8100)) -&gt; None: ...\n# \u2192 toolr cluster deploy --port-range 9000 9100\n</code></pre> <p>clap consumes two values per <code>--port-range</code> occurrence; msgspec coerces each slot to <code>int</code> against the function's hint.</p>"},{"location":"writing-commands/arguments/#counting-flags","title":"Counting flags","text":"<p><code>toolr.types.Count</code> turns a parameter into a \"repeat the short form to count\" flag, matching the classic <code>-vvv</code> pattern:</p> <pre><code>from typing import Annotated\nfrom toolr import arg, command\nfrom toolr.types import Count\n\n@command(group=\"example\")\ndef serve(ctx: Context, verbose: Annotated[Count, arg(aliases=[\"-v\"])] = 0) -&gt; None:\n    ctx.print(f\"verbosity level: {verbose}\")\n</code></pre> <pre><code>toolr example serve            # verbosity level: 0\ntoolr example serve -v         # verbosity level: 1\ntoolr example serve -vvv       # verbosity level: 3\n</code></pre> <p>The Python runtime value is plain <code>int</code> (<code>Count</code> is just an <code>int</code> alias); the rust side wires <code>clap::ArgAction::Count</code> based on the annotation.</p> <p>Next: Docstrings \u2192</p>"},{"location":"writing-commands/context/","title":"Using <code>ctx</code>","text":"<p>Every command function receives a <code>Context</code> as its first parameter. It's the only object you need to talk to the terminal, run subprocesses, prompt the user, or exit cleanly.</p>"},{"location":"writing-commands/context/#output","title":"Output","text":"<p><code>ctx.print(...)</code> writes Rich-rendered text to stdout. <code>ctx.info(...)</code> and <code>ctx.error(...)</code> write structured log records (respecting <code>--debug</code> / <code>--quiet</code>).</p> <pre><code>@command(group=\"example\")\ndef hello(ctx: Context, name: str = \"world\") -&gt; None:\n    \"\"\"Greet someone.\n\n    Demonstrates the simplest possible toolr command: a function with a\n    single keyword argument, a Google-style docstring, and a call to\n    ``ctx.print``.\n\n    Args:\n        name: The name to greet.\n    \"\"\"\n</code></pre> <pre><code>toolr example hello --name Pedro\n</code></pre> <pre><code>hello, Pedro\n</code></pre> <p>(The example here is the <code>hello</code> function from the scaffold's <code>example.py</code> \u2014 same file that <code>toolr project init</code> generates.)</p>"},{"location":"writing-commands/context/#running-subprocesses","title":"Running subprocesses","text":"<p><code>ctx.run(*cmd, capture_output=True, check=True)</code> is the canonical way to run a subprocess. <code>capture_output=True</code> gives you a result object with <code>.stdout</code> and <code>.stderr</code>; default (no capture) streams the subprocess's output to the user's terminal.</p> <pre><code>@command(group=\"example\")\ndef commit(ctx: Context) -&gt; None:\n    \"\"\"Print the short SHA of the current git HEAD.\n\n    Demonstrates ``ctx.run(..., capture_output=True)`` for capturing\n    subprocess output without streaming it to the terminal.\n    \"\"\"\n    result = ctx.run(\"git\", \"rev-parse\", \"--short\", \"HEAD\", capture_output=True)\n    if result.returncode != 0:\n        ctx.exit(result.returncode, \"git rev-parse failed\")\n    sha = result.stdout.read().strip()\n</code></pre>"},{"location":"writing-commands/context/#prompting-and-exiting","title":"Prompting and exiting","text":"<p><code>ctx.prompt(...)</code> reads a line from stdin (returns the typed string, empty if the user just hits enter). <code>ctx.exit(code, message=None)</code> writes the message to stderr and exits with the given code \u2014 no exception traceback.</p> <pre><code>@command(group=\"example\")\ndef confirm(ctx: Context) -&gt; None:\n    \"\"\"Prompt for confirmation before doing something destructive.\n\n    Demonstrates ``ctx.prompt(...)`` for interactive input and\n    ``ctx.exit(...)`` for early-exit with a non-zero status.\n    \"\"\"\n    answer = ctx.prompt(\"Continue?\", expected_type=bool, default=False)\n    if not answer:\n        ctx.exit(1, \"aborted\")\n</code></pre>"},{"location":"writing-commands/context/#the-full-surface","title":"The full surface","text":"<p>See <code>Context</code> in the API reference for every method and attribute. Highlights:</p> <ul> <li><code>ctx.print(...)</code> / <code>ctx.info(...)</code> / <code>ctx.error(...)</code> / <code>ctx.debug(...)</code></li> <li><code>ctx.run(*cmd, ...)</code> \u2014 subprocess execution</li> <li><code>ctx.prompt(prompt)</code> \u2014 read a line from stdin</li> <li><code>ctx.exit(code, message=None)</code> \u2014 early exit with a message</li> <li><code>ctx.chdir(path)</code> \u2014 context-manager to switch directories</li> <li><code>ctx.repo_root</code> \u2014 the path toolr discovered as the project root</li> </ul> <p>Next: Annotations \u2192</p>"},{"location":"writing-commands/docstrings/","title":"Docstrings","text":"<p>Toolr requires Google-style docstrings on every command. The docstring drives the <code>--help</code> output: the summary line, the long description, and per-argument help text.</p>"},{"location":"writing-commands/docstrings/#anatomy","title":"Anatomy","text":"<pre><code>\"\"\"\nComplete example.\n\nThe purpose is to provide an extensive usage example, kind if like TDD\n\n| Example | Description |\n|---------|-------------|\n| hello   | Say hello.  |\n| goodbye | Say goodbye.|\n| multiply| Multiply two numbers.|\n\n\n\n\"\"\"\n\nfrom __future__ import annotations\n\nimport shutil\nfrom enum import StrEnum\nfrom typing import Annotated\nfrom typing import NoReturn\n\nfrom toolr import Context\nfrom toolr import arg\nfrom toolr import command\nfrom toolr import command_group\n\ncommand_group(\"example\", title=\"Example\", docstring=__doc__)\n\n\n@command(group=\"example\")\ndef hello(ctx: Context) -&gt; NoReturn:\n    \"\"\"\n    Say hello.\n\n    This is the long description about the hello command.\n    \"\"\"\n    ctx.info(\"Hello, world!\")\n\n\n@command(\"goodbye\", group=\"example\")\ndef say_goodbye(ctx: Context, name: str | None = None) -&gt; NoReturn:\n    \"\"\"\n    Say goodbye.\n\n    Args:\n        name: Name to say goodbye to. If not provided, defaults to \"world\".\n    \"\"\"\n    if name is None:\n        name = \"world\"\n    ctx.info(f\"Goodbye, {name}!\")\n\n\n@command(group=\"example\")\ndef multiply(ctx: Context, a: int, b: int, verbose: bool = False) -&gt; NoReturn:\n    \"\"\"\n    Multiply two numbers.\n\n    Args:\n        a: First number.\n        b: Second number.\n        verbose: Whether to print the result calculation. Defaults to False, print only the result.\n    \"\"\"\n    result = a * b\n    if verbose:\n        ctx.info(f\"{a} * {b} = {result}\")\n    else:\n        ctx.info(result)\n\n\nclass Operation(StrEnum):\n    ADD = \"add\"\n    SUBTRACT = \"subtract\"\n    MULTIPLY = \"multiply\"\n    DIVIDE = \"divide\"\n\n\n@command(group=\"example\")\ndef math(\n    ctx: Context,\n    a: int,\n    b: int,\n    operation: Annotated[Operation, arg(aliases=[\"-o\", \"--op\"])] = Operation.ADD,\n    verbose: bool = False,\n) -&gt; NoReturn:\n    \"\"\"\n    Perform a mathematical operation.\n\n    Args:\n        a: First number.\n        b: Second number.\n        operation: Operation to perform.\n        verbose: Whether to print the result calculation. Defaults to False, print only the result.\n    \"\"\"\n    match operation:\n        case Operation.ADD:\n            value = a + b\n            log_msg = f\"{a} + {b} = {value}\"\n        case Operation.SUBTRACT:\n            value = a - b\n            log_msg = f\"{a} - {b} = {value}\"\n        case Operation.MULTIPLY:\n            value = a * b\n            log_msg = f\"{a} * {b} = {value}\"\n        case Operation.DIVIDE:\n            if b == 0:\n                ctx.error(\"Division by zero!\")\n                return\n            value = a / b\n            log_msg = f\"{a} / {b} = {value}\"\n        case _:\n            raise ValueError(f\"Invalid operation: {operation}\")\n    if verbose:\n        ctx.info(log_msg)\n    else:\n        ctx.info(value)\n\n\n@command(group=\"example\")\ndef py_version(ctx: Context) -&gt; NoReturn:\n    \"\"\"\n    Show Python version.\n\n    This command demonstrates how to run subprocess commands and capture their output.\n    \"\"\"\n    python = shutil.which(\"python\")\n    ret = ctx.run(python, \"--version\", capture_output=True, stream_output=False)\n    ctx.info(\"Python version\", ret.stdout.read().strip())\n</code></pre> <p>The module-level docstring of <code>docstrings-example.py</code> is passed to <code>command_group(..., docstring=__doc__)</code> and becomes the group's description. The per-command docstrings become the subcommand descriptions; the <code>Args:</code> block populates <code>--help</code> for each parameter.</p>"},{"location":"writing-commands/docstrings/#rules","title":"Rules","text":"<ul> <li>The first line of the docstring is the command summary used in   the parent group's <code>--help</code> listing.</li> <li>The rest of the body (until <code>Args:</code>) is the long description shown   on <code>&lt;command&gt; --help</code>.</li> <li>The <code>Args:</code> block lists parameters: <code>&lt;name&gt;: &lt;description&gt;</code>. Lines   must follow Google's indentation convention; consult   the napoleon docs   if you need a refresher.</li> </ul>"},{"location":"writing-commands/docstrings/#help-output-rendering","title":"Help-output rendering","text":"<p><code>--help</code> text is rendered as Markdown through termimad, so the body of a docstring can use the usual elements:</p> <ul> <li>Headings (<code>#</code>, <code>##</code>, \u2026) get distinct styling.</li> <li>Inline <code>code spans</code> are highlighted.</li> <li>Fenced code blocks render in a separate frame.</li> <li>Tables work \u2014 the <code>docstrings-example.py</code> group docstring above   ships with one.</li> <li>Lists, bold, italic, and links all render through.</li> </ul> <p>Note the asymmetry between <code>-h</code> and <code>--help</code>:</p> <ul> <li><code>-h</code> on any command prints a single-line summary (the first line   of the docstring) and the args block. Useful for quick lookup.</li> <li><code>--help</code> prints the full docstring \u2014 summary, description, args \u2014   rendered with Markdown.</li> <li>On a parent group, both forms list child commands by their   summary line only; the long description is reserved for leaf   commands.</li> </ul>"},{"location":"writing-commands/docstrings/#failing-without-a-docstring","title":"Failing without a docstring","text":"<p>A command function with no docstring is rejected at manifest-build time: toolr refuses to ship undocumented commands. Add a one-line summary and you're good.</p> <p>Next: Using <code>ctx</code> \u2192</p>"},{"location":"writing-commands/groups/","title":"Groups &amp; commands","text":"<p>Every CLI subcommand starts with a <code>command_group</code> declaration and one or more functions attached to it.</p>"},{"location":"writing-commands/groups/#minimal-example","title":"Minimal example","text":"<pre><code>from __future__ import annotations\n\nfrom toolr import Context\nfrom toolr import command_group\n\nexample = command_group(\"example\", title=\"Example\", description=\"Example commands\")\n\n\n@example.command\ndef echo(ctx: Context, what: str):\n    \"\"\"\n    Command title line.\n\n    This is the command description, it can span several lines.\n\n    Args:\n        what: What to echo.\n    \"\"\"\n    ctx.print(what)\n</code></pre> <p>Place that file at <code>tools/example.py</code> in your repo. After running <code>toolr project manifest rebuild</code> (or letting toolr rebuild automatically on first invocation), <code>toolr example --help</code> lists the new group's commands and <code>toolr example echo \"hello, world\"</code> prints it.</p>"},{"location":"writing-commands/groups/#whats-happening","title":"What's happening","text":"<ul> <li><code>example = command_group(\"example\", title=\"Example\", description=\"\u2026\")</code>   registers a top-level group and returns a binding you can attach   commands to. The first argument (<code>\"example\"</code>) is the invocation   name; the title and description appear in <code>--help</code>.</li> <li><code>@example.command</code> attaches the decorated function to the   registered group. The function name (<code>echo</code>) becomes the CLI name;   underscores are converted to hyphens (<code>render_diff</code> \u2192   <code>toolr example render-diff</code>).</li> <li>The first parameter (<code>ctx: Context</code>) is always the   <code>Context</code> object that toolr injects at execute   time. It's never exposed as a CLI flag.</li> <li>The remaining parameters become CLI arguments inferred from their   type hints and default values. Positional parameters without   defaults become positional CLI args; parameters with defaults   become optional flags. See Arguments for the full   inference rules.</li> </ul>"},{"location":"writing-commands/groups/#overriding-the-cli-name","title":"Overriding the CLI name","text":"<p>To register a command under a name different from its function:</p> <pre><code>@example.command(\"snippet-checker\")\ndef check_snippets(ctx): ...\n# \u2192 toolr example snippet-checker\n</code></pre>"},{"location":"writing-commands/groups/#function-name-to-command-name-conversion","title":"Function-name-to-command-name conversion","text":"<pre><code>\"\"\"Example demonstrating function name to command name conversion.\n\nThis example shows how function names with underscores are automatically converted\nto command names with hyphens when decorating with a bound `@group.command`\nwithout specifying a name.\n\"\"\"\n\nfrom __future__ import annotations\n\nfrom toolr import Context\nfrom toolr import command_group\n\nnames = command_group(\n    \"names\",\n    \"Examples for function name to command name conversion\",\n    \"Various examples for function name to command name conversion\",\n)\n\n\n# Define commands using function names - they will be automatically converted\n@names.command\ndef simple_function(ctx: Context) -&gt; None:  # -&gt; simple-function\n    \"\"\"A simple function.\"\"\"\n\n\n@names.command\ndef function_with_underscores(ctx: Context) -&gt; None:  # -&gt; function-with-underscores\n    \"\"\"A function with underscores in the name.\"\"\"\n\n\n@names.command\ndef multiple_underscores_in_name(ctx: Context) -&gt; None:  # -&gt; multiple-underscores-in-name\n    \"\"\"A function with multiple underscores.\"\"\"\n\n\n@names.command\ndef _leading_underscore(ctx: Context) -&gt; None:  # -&gt; -leading-underscore\n    \"\"\"A function with a leading underscore.\"\"\"\n\n\n@names.command\ndef trailing_underscore_(ctx: Context) -&gt; None:  # -&gt; trailing-underscore-\n    \"\"\"A function with a trailing underscore.\"\"\"\n\n\n@names.command\ndef _both_underscores_(ctx: Context) -&gt; None:  # -&gt; -both-underscores-\n    \"\"\"A function with both leading and trailing underscores.\"\"\"\n</code></pre> <p>Each function name with underscores becomes a hyphenated CLI command: <code>simple_function</code> \u2192 <code>toolr names simple-function</code>, <code>function_with_underscores</code> \u2192 <code>toolr names function-with-underscores</code>.</p>"},{"location":"writing-commands/groups/#when-you-outgrow-a-single-file","title":"When you outgrow a single file","text":"<p>The bound-decorator form above is the canonical shape for tools that live in one file: the <code>example</code> binding sits right next to the commands it owns, so the relationship is obvious from a glance.</p> <p>Tools tend to grow. When you want commands in <code>tools/foo.py</code> to attach to a group declared in <code>tools/_common.py</code>, importing the binding across files gets awkward. Toolr's string-keyed decorator is built for that case:</p> <pre><code># tools/foo.py\nfrom toolr import command\n\n@command(group=\"example\")\ndef run(ctx): ...\n</code></pre> <p>The static parser resolves the <code>group=\"example\"</code> reference at manifest-build time, so the file declaring <code>command_group(\"example\", \u2026)</code> and the file declaring the command don't need to share an import. Read Scaling command groups across files for the full picture, including the typo-safety guarantees the string-keyed form gives you.</p> <p>Next: Arguments \u2192</p>"},{"location":"writing-commands/known-bugs/","title":"Known bugs","text":""},{"location":"writing-commands/known-bugs/#outstanding","title":"Outstanding","text":"<p>None tracked at the moment. See the GitHub issues for the live list.</p>"},{"location":"writing-commands/known-bugs/#closed","title":"Closed","text":"<ul> <li><code>arg()</code> metadata fully plumbed end-to-end (closes #198).   <code>aliases</code>, <code>metavar</code>, <code>env</code>, <code>hide</code>, <code>display_order</code>,   <code>conflicts_with</code>, <code>requires</code>, and <code>help_section</code> all flow from the   Python annotation through the static parser into clap. Path   constraints renamed to <code>path_must_exist</code> / <code>path_must_be_file</code> /   <code>path_must_be_dir</code>. Old names accepted with a deprecation warning.</li> <li>Positional <code>int</code> / <code>float</code> coercion: typed clap value-parsers   serialise typed JSON, msgspec validates against function hints on   the Python side.</li> <li><code>bool = False</code> parameters render as no-value <code>--verbose</code> flags.</li> <li><code>dry_run</code> parameters expose <code>--dry-run</code> on the CLI.</li> <li>Enum-typed defaults render their resolved member value   (<code>[default: add]</code>, not <code>[default: &lt;expr&gt;]</code>).</li> <li>Nested groups (<code>docker.command_group(\"image\")</code>) build a proper   subcommand tree at the CLI surface.</li> </ul> <p>If you hit something that doesn't match the documented behaviour, file an issue.</p>"},{"location":"writing-commands/nesting/","title":"Nested groups","text":""},{"location":"writing-commands/nesting/#nested-groups","title":"Nested groups","text":"<p>Subgroups organise related commands under a common parent. Express the relationship in one of two equivalent ways.</p>"},{"location":"writing-commands/nesting/#dotted-path-inside-command_group","title":"Dotted path inside <code>command_group(...)</code>","text":"<p>The shortest spelling. Everything before the final dot is the parent's full path; the leaf becomes the child group's name.</p> <pre><code>\"\"\"Nested command groups example for the writing-commands chapter.\"\"\"\n\nfrom __future__ import annotations\n\nfrom toolr import Context\nfrom toolr import command\nfrom toolr import command_group\n\ncommand_group(\"docker\", title=\"Docker\", description=\"Container utilities\")\ncommand_group(\"docker.image\", description=\"Image subcommands\")\ncommand_group(\"docker.container\", description=\"Container subcommands\")\n\n\n@command(group=\"docker.image\")\ndef build(ctx: Context, tag: str) -&gt; None:\n    \"\"\"Build a docker image.\n\n    Args:\n        tag: Tag to assign to the built image.\n    \"\"\"\n    ctx.print(f\"would build image: {tag}\")\n\n\n@command(group=\"docker.container\")\ndef start(ctx: Context, name: str) -&gt; None:\n    \"\"\"Start a stopped container.\n\n    Args:\n        name: Name (or ID) of the container.\n    \"\"\"\n    ctx.print(f\"would start container: {name}\")\n</code></pre> <p>That file declares:</p> <ul> <li>A top-level <code>docker</code> group.</li> <li>Two subgroups: <code>docker image</code> and <code>docker container</code>.</li> <li>A <code>build</code> command on <code>docker image</code> and a <code>start</code> command on   <code>docker container</code>.</li> </ul> <p>\u2026and produces the CLI hierarchy:</p> <ul> <li><code>toolr docker --help</code> lists the two subgroups (<code>image</code>, <code>container</code>).</li> <li><code>toolr docker image build my-image:latest</code> reaches the <code>build</code> command.</li> <li><code>toolr docker container start my-container</code> reaches the <code>start</code> command.</li> </ul> <p>No fixed depth limit \u2014 <code>command_group(\"a.b.c.d\")</code> works just as well.</p>"},{"location":"writing-commands/nesting/#parent-keyword","title":"<code>parent=\"...\"</code> keyword","text":"<p>When the dotted name reads awkwardly, spell the parent out explicitly:</p> <pre><code>command_group(\"docker\", title=\"Docker\")\ncommand_group(\"image\", parent=\"docker\", description=\"Image subcommands\")\n\n\n@command(group=\"docker.image\")\ndef build(ctx, tag: str) -&gt; None: ...\n</code></pre> <p>The two styles are exchangeable; mixing them inside one project is fine. (Combining a dotted name and a <code>parent=</code> kwarg is allowed but the dotted form wins \u2014 toolr logs a warning at registration time so the conflict is visible.)</p>"},{"location":"writing-commands/nesting/#cross-file-parents","title":"Cross-file parents","text":"<p>A subgroup doesn't have to live in the same file as its parent. Toolr's parser collects every <code>command_group(...)</code> declaration across <code>tools/**/*.py</code> before resolving any parent references, so this works in either file order:</p> <pre><code># tools/_common.py\ncommand_group(\"ci\", docstring=__doc__)\n</code></pre> <pre><code># tools/helm.py\ncommand_group(\"ci.helm-diff\", description=\"Helm diff helpers\")\n\n\n@command(group=\"ci.helm-diff\")\ndef backend(ctx, env: str) -&gt; None: ...\n</code></pre> <p>Either way the resulting CLI is <code>toolr ci helm-diff backend</code>.</p> <p>Shell tab completion</p> <p>Top-level groups, their subgroups, and the commands attached to either tab-complete out of the box. If you're using an older shell-completion script and notice nested subgroups don't complete, run <code>toolr self completion install &lt;shell&gt; --force</code> to refresh it.</p> <p>Legacy method form deprecated</p> <p><code>parent.command_group(\"child\", ...)</code> \u2014 the method call on a captured binding \u2014 still works but emits a <code>ToolrDeprecationWarning</code> and will be removed in toolr 1.0. Replace each call with a dotted <code>command_group(...)</code> declaration. See the migration guide.</p>"}]}